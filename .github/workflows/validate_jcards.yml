name: Validate jCards.js

on:
  pull_request:

jobs:
  validate-jcards:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
    steps:
      - name: Check out repo
        uses: actions/checkout@master
      - name: Validate jCards.js
        run: ./bin/gemp validate-jcards