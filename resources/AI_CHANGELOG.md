# Local changes on top of the pure devs private clone

Base: `PlayersCommittee/gemp-swccg` @ `55c22cf49` (canonical devs repo, compiles clean).
Reference copy of the (broken) public release: `../gemp-swccg-public-PUBLIC-COPY-2026-06-22`.
Everything below is the ONLY divergence from pure devs code — each is reversible.

## 2026-07-08 — ObjectivePlaybook Phase 1b (step 2): Endor hardcoded block COMMENTED OUT — now fully JSON-driven (behavior-neutral)
- Completes the Endor pilot as the template: the objective's data + scoring inputs now come ENTIRELY from its `objective_playbooks.json` profile; the hardcoded analyzer block is compiled out. This is the "comment out old block LAST" step of the agreed order.
- MOD `.../{rando,chosenone}/strategy/ObjectiveAnalyzer.java` — (1) `hydrateFromProfile` requiredCardsOnTable is now AUTHORITATIVE (clear-then-set) when the profile names flip cards, so it replaces the text parser's junk "…are both" conjunction exactly as the old block's `requiredCardsOnTable.clear()` did. (2) The hardcoded `contains("endor operations")` block in parseFlipCondition is wrapped in `if (false /* SUPERSEDED 2026-07-08 */ …)` (compiled-out, kept as revert path, V193 tag preserved for grep).
- Boundary / SAFETY: behavior-neutral by construction. With the block off + Endor's loaderEnabled profile, hydrate sets the identical state: locationFragment "endor" (additive, same as block), requiredCardsOnTable {ominous rumors, establish secret base} (authoritative clear-then-set == block's clear+add), flipCriticalControlSite "endor: bunker", flipCriticalControlCard "establish secret base" (both set when parser left null, == block), flipCriticalControlCardIds {207_25,207_025,601_260} (addAll onto empty == block's clear+add). The +400 already comes from the JSON weight (step 1). TRADEOFF (accepted per single-source directive): if the JSON resource ever fails to load, Endor loses this intel (block no longer a live fallback) — the resource is bundled in the jar and load-verified.
- Verified: compiles clean both bots (MVN_EXIT=0). Static boundary reasoning above; Codex runtime-logic verify requested.
- Revert: flip the `if (false …)` back to live and remove the authoritative clear (or `git revert`).

## 2026-07-08 — ObjectivePlaybook Phase 1b (step 1): active playbook now BUILT FROM JSON for enabled objectives (behavior-neutral)
- Makes the analyzer a real pointer: for a `loaderEnabled` objective, `activePlaybook` is now constructed from the JSON profile (`buildPlaybookFromProfile`) instead of the compiled static. No hardcode commented out yet (that's the next step, per wire → boundary math → comment LAST).
- MOD `objective_playbooks.json` — Endor profile gains `flipGateCardName: "establish secret base"` (the display/detection name; needed before the hardcoded Endor block can be removed).
- MOD `.../{rando,chosenone}/strategy/ObjectiveAnalyzer.java` — ADD `JsonProfile.flipGateCardName` (+ hydrate `flipCriticalControlCard`, guarded on null); ADD curated string→Filter registry `resolveFilter()` (senator, Galactic_Senate, biker_scout, Bunker — the exact keys Codex's 58-file uses), `weight()` helper, and `buildPlaybookFromProfile()` (identity + registry Filters + JSON weights). analyze() computes `prof`/`loaderOn` ONCE and sets `activePlaybook = loaderOn ? buildPlaybookFromProfile(prof) : <compiled-static fallback>`; the same `loaderOn` gates hydration post-parse.
- Boundary / SAFETY: behavior-neutral. The ONLY consumer of `getActivePlaybook()` is DeployEvaluator V193 (both bots), which fires only for Endor (flip-gate site set). Endor's JSON `deployFlipGateSite` = 400 == the compiled ENDOR_PLAYBOOK weight, so the +400 is unchanged. My Lord's activePlaybook is now JSON-built too but UNUSED (its consumed scoring still reads the MY_LORD_PLAYBOOK static + Filters.senator directly — that swap is the next step). All disabled objectives: `loaderOn` false → compiled-static/null fallback, unchanged. `flipCriticalControlCard` still set by the (still-live) hardcoded Endor block; the guard makes hydration idempotent. Compiled MY_LORD/ENDOR statics remain as the loaderEnabled=false fallback.
- Verified: compiles clean both bots (MVN_EXIT=0); only DeployEvaluator V193 consumes activePlaybook; JSON valid. Codex verify pass requested.
- Revert: `git revert`; the compiled-static ternary is intact as the fallback branch.

## 2026-07-08 — ObjectivePlaybook loader FIX: gate hydration to VERIFIED profiles + defer pullableCards (restore behavior-neutrality)
- Follow-up to the Phase 0 loader below. Codex delivered the full 58-profile canonical `objective_playbooks.json` to the runtime path (it landed in commit 814ad6664, replacing the 2-entry pilot file). That made the loader hydrate ALL 58 objectives, not just My Lord + Endor — an UNVERIFIED behavior change for 56 objectives (the "centralized regression generator" risk). Also Endor's profile carries `pullableCards` (biker scout / bunker / endor system / landing platform) the text parser did not, which would change pull behavior.
- MOD `objective_playbooks.json` — ADD `"loaderEnabled": true` to the My Lord + Endor profiles ONLY (the two boundary-math-verified equivalents). The other 56 omit it → treated as disabled.
- MOD `.../{rando,chosenone}/strategy/ObjectiveAnalyzer.java` — `JsonProfile` gains `Boolean loaderEnabled`; analyze() hydrates only when `Boolean.TRUE.equals(loaderEnabled)`. `pullableCards` hydration COMMENTED OUT (deferred — adds pull targets, not neutral, needs per-objective boundary math).
- Boundary / SAFETY: restores true behavior-neutrality. Only My Lord (all hydrated lists empty → no-op) and Endor (locationFragments/requiredCardsOnTable/flipGateSite/flipGateCardIds byte-identical to the hardcoded Endor block → idempotent; new setup slots have no consumers) hydrate. Every other objective: profile found but disabled → no hydration → text parser stands. flip the flag per objective as each passes boundary math.
- Verified: only Endor + My Lord report loaderEnabled; compiles clean both bots (MVN_EXIT=0); canonical file id-check 58 profiles / 288 ids / 0 unresolved.
- Revert: `git revert`; remove the two flags + the gate.

## 2026-07-08 — ObjectivePlaybook: JSON loader (single runtime objective-data source) — Phase 0, behavior-neutral (both bots)
- Steve's ruling: all objective data lives in ONE place (a JSON file) and ObjectiveAnalyzer is just a pointer that reads it. This lands the loader; it does NOT yet replace any hardcoded scoring (per the agreed order: wire slot → boundary math → comment old block LAST).
- ADD `src/gemp-swccg-server/src/main/resources/objective_playbooks.json` — the single runtime source, bundled into the jar. Pilots My Lord + Endor; Codex delivers the full 58. Field contract (per profile): label, blueprintIds, titleFragments, locationFragments, requiredCardsOnTable, pullableCards, flipGateSite, flipGateCardIds, startingLocations/Effects/Interrupts ({blueprintIds,titleFragments}), keyCharacterFilter, keySiteFilter, weights (named floats reusing existing V-tags).
- MOD `.../{rando,chosenone}/strategy/ObjectiveAnalyzer.java` — ADD Gson-backed loader (nested JsonRoot/JsonProfile/JsonCardRef), lazy thread-safe `profiles()` parse of the resource, `findProfile(bpId,title)` (id first, title fragment second), `hydrateFromProfile()` (ADDITIVE + idempotent — never clears a slot; writes the same values the text parser/hardcoded blocks already write), and NEW setup slots startingLocation/Effect/Interrupt Ids+Fragments (+ getters, cleared in reset()). analyze() calls hydrate AFTER parseGameText.
- Boundary / SAFETY: behavior-NEUTRAL. Hydration is additive to Sets and guarded (flipGateSite only when null), so for Endor it writes byte-identical values to the hardcoded block (locFrag "endor", reqCards {ominous rumors, establish secret base}, gate site "endor: bunker", gate ids {207_25,207_025,601_260}); for My Lord all hydrated lists are empty (no-op). HARD FALLBACK: missing/malformed JSON or unlisted objective → empty registry / null profile → text parser output stands unchanged (verified: getResourceAsStream null-guarded, parse wrapped in try/catch). No consumer reads the new slots yet; weights still sourced from the compiled MY_LORD/ENDOR playbooks (Phase 1b swaps those to JSON, then comments out the hardcode).
- Verified: compiles clean both bots (MVN_EXIT=0); resource copied to target/classes; JSON parses (2 profiles). chosenone mirrored (loader is package-agnostic — both bots read the same resource).
- Revert: `git revert`; loader + slots are additive, hydration writes only duplicate values.

## 2026-07-07 — V193 FIX: scope the Endor Bunker flip-gate steer to the Bunker-GATED Establish Secret Base printings (both bots)
- ADJUSTS V193 in place (NOT a new tag — update-old-rule discipline). Behavior-NARROWING, Steve-approved.
- Problem: the flip-gate detection matched the shared title "establish secret base", which is carried by THREE printings with DIFFERENT deploy gates. Verified vs source: base 8_124 (Card8_124.java) deploys on Endor SYSTEM gated on controlling **3 Endor sites** with biker scouts/AT-STs — NOT Bunker; V 207_25 (Card207_025.java) deploys on **Bunker** gated on controlling Bunker; Legacy V 601_260 (Card601_260.java) deploys on Endor system but is gated on "If you control **Bunker**". So the +400 one-shot Bunker steer is correct ONLY for {207_25, 601_260}. A deck running the base 8_124 would misfire the steer (a single Bunker body can't enable a 3-Endor-site gate).
- MOD `.../{rando,chosenone}/strategy/ObjectiveAnalyzer.java` — ADD `flipCriticalControlCardIds` (Set) + `getFlipCriticalControlCardIds()`; the Endor block populates {207_25, 207_025, 601_260} (207_25 hedged both id-pad forms like the IWTM 208_51/208_051 hedge). `flipCriticalControlCard` kept as the human-readable display/log name. Cleared in reset().
- MOD `.../{rando,chosenone}/evaluators/DeployEvaluator.java` — V193 `v193HoldsGateCard` now detects by the scoped id set (blueprint-id exact match via DeckOracle) when non-empty; falls back to the title name when the set is empty (keeps the steer general for any future flip-gate objective that names only a card, not ids).
- Boundary / SAFETY: fires strictly ≤ before. Same +400 magnitude, same site guard, same not-already-controlled guard. Old predicate: holds ANY Establish Secret Base (8_124/207_25/601_260). New predicate: holds a Bunker-gated one (207_25/601_260). Only difference: no longer fires when the deck's sole Establish Secret Base is the base 8_124 (where it was a misfire). The diagnosed game (replay qgdridfo166f27r3) ran 207_25 → still matches → identical behavior there.
- Verified: compiles clean both bots (MVN_EXIT=0). chosenone mirrored (only DeckOracle package differs). work-verifier to follow.
- Revert: `git revert`; the id set is additive and the DeployEvaluator branch falls back to the old title match when the set is empty.

## 2026-07-07 — ObjectivePlaybook #2: Endor Operations → typed analyzer-owned playbook (behavior-preserving)
- SECOND analyzer-owned ObjectivePlaybook (pilot #1 = My Lord, below). Objective = Endor Operations / Imperial Outpost (8_167 / _BACK, DARK).
- MOD `.../rando/strategy/ObjectiveAnalyzer.java` + chosenone mirror — ADD `isEndor` identity flag (title contains "endor operations", set in analyze(), cleared in reset()). ADD static `ENDOR_PLAYBOOK` (identity {8_167, 8_167_BACK} + "endor operations"/"imperial outpost"; keyCharacter `Filters.biker_scout` = back-side drain-protect/retrieve target, not yet scored; keySite `Filters.Bunker` = flip-gate control site; weights `deployFlipGateSite` = V193 +400, senator categories 0). ADD one field `deployFlipGateSite` to `ObjectiveWeights` via a 5-arg constructor; the existing 4-arg (My Lord) delegates with 0 (My Lord call unchanged). `activePlaybook` now selects `ENDOR_PLAYBOOK` when `isEndor`.
- MOD `.../rando/evaluators/DeployEvaluator.java` + chosenone mirror — the V193 flip-gate `+400.0f` literal now reads `getActivePlaybook().weights.deployFlipGateSite` (defensive fallback to 400 literal if playbook null). Log line prints the resolved bonus.
- Why: continues the typed-playbook consolidation. The V193 flip-gate magnitude is now co-located with Endor's identity/facts in one analyzer-owned object, same shape as My Lord. Facts from Codex batch `resources/Objective_Playbook_Facts_2026-07-07.json` (GO_WITH_FIXES), source-verified vs Card8_167.java / Card207_025.java / Card8_124.java.
- Boundary / SAFETY: behavior byte-identical. V193 fires ONLY when the analyzer named a flip-gate site (`getFlipCriticalControlSite() != null`), which today is exclusively Endor Operations, which now also selects `ENDOR_PLAYBOOK` (weight = 400). So the resolved bonus == the old literal on every path V193 can fire. My Lord path unchanged (4-arg ctor → deployFlipGateSite 0, and V193 never fires for it). All other objectives: activePlaybook null, V193 never fires. `deployFlipGateSite` is additive.
- NOTE (separate follow-up commit): the flip-gate CARD match (`flipCriticalControlCard = "establish secret base"`) still matches BOTH base 8_124 (deploys on Endor SYSTEM, gates on 3 Endor sites — NOT Bunker) and V 207_25 (deploys on Bunker). The Bunker +400 steer is only correct for the V path. Scoping fix tracked separately (NOT in this behavior-preserving commit).
- Verified: compiles clean both bots (MVN_EXIT=0, 0 errors). chosenone mirrored (only DeckOracle/ObjectiveAnalyzer package differs). work-verifier to follow.
- Revert: `git revert`; the weight read is a one-line swap back to `400.0f`; the `isEndor`/`ENDOR_PLAYBOOK`/`deployFlipGateSite` additions are additive.

## 2026-07-07 — ObjectivePlaybook pilot: My Lord deploy weights → typed analyzer-owned playbook (behavior-preserving)
- FIRST analyzer-owned ObjectivePlaybook (Steve's ruling: ObjectiveAnalyzer owns objective identity + typed facts + scoring WEIGHTS; evaluators consume at existing call sites). Pilot objective = My Lord, Is That Legal? (12_179).
- MOD `.../rando/strategy/ObjectiveAnalyzer.java` — ADD nested types `NamedCardRef` (blueprint-id OR title-fragment matcher, virtual-reprint proof), `ObjectiveWeights` (per-objective magnitudes), `ObjectivePlaybook` (identity + key-character Filter + key-site Filter + weights). ADD static `MY_LORD_PLAYBOOK` (identity {12_179, 12_179_BACK} + "my lord"/"make it legal"; keyCharacter `Filters.senator`; keySite `Filters.Galactic_Senate`; weights V88 +1500 / V83 -2000 / V108 +500 / V110 -2000). ADD `activePlaybook` field (set in analyze() when isMyLord, reset in reset()) + `getActivePlaybook()` — the analyzer-owned API evaluators/planners consult. The four My Lord deploy magnitudes in `getDeployObjectiveAdjustments` now read `MY_LORD_PLAYBOOK.weights.*` instead of inline literals.
- Why: starts the typed-playbook consolidation. Objective identity + weights are co-located in ONE analyzer-owned object per objective and consumed by the deploy phase, so every future objective follows the same shape. Facts from Codex batch `resources/Objective_Playbook_Facts_2026-07-07.json`, independently source-verified (My Lord = GO: all ids/Filters/V-tag lines/flip-text confirmed vs Card12_179.java).
- Boundary / SAFETY: PURE REFACTOR, behavior byte-identical. weights == the exact old literals (V88 +1500, V83 -2000, V108 +500, V110 -2000). Conditions, reason strings, and the call site are all unchanged. V99 (ungated -1500) and V86 (Invasion) are NOT in the My Lord playbook and are untouched. `activePlaybook`/`getActivePlaybook()` are additive — no consumer reads them yet (future card-selection/move/planner).
- Verified: compiles clean both bots (MVN_EXIT=0, 0 errors). chosenone mirrored (ObjectiveAnalyzer 1492/1492). work-verifier + Codex review to follow.
- Revert: `git revert`; each of the four weight reads is a one-line swap back to its literal in `getDeployObjectiveAdjustments`; the types/field are additive.

## 2026-07-07 — Objective DEPLOY scoring → ObjectiveAnalyzer (the deploy phase now CHECKS the objective brain)
- Follow-on to the identity consolidation below. Per Steve: "move the version logic to a singular ObjectiveAnalyzer ... deploy decision should check objective analyzer and Deck Oracle." The six deploy objective SCORING blocks now live in the analyzer; the deploy phase calls it and applies the result at the same spot.
- MOD `.../rando/strategy/ObjectiveAnalyzer.java` — ADD `getDeployObjectiveAdjustments(game, gameState, playerId, card, blueprint, actionText)` returning `List<ScoreNote>{score,reason}`, plus a `ScoreNote` holder and a private `isSenatorCard()` helper (Keyword.SENATOR OR lore "senator"). It computes V83/V110/V108/V86/V88 (My Lord / Invasion, each gated on `analyzed && isMyLord/isInvasion`) and V99 SENATE GUARD (DELIBERATELY ungated — fires whenever a Galactic Senate is on table, `analyzed` NOT required). Score magnitudes, reason strings, and per-rule detection (V83/V88 use `Filters.senator` keyword-only; V108/V110/V99 use keyword-OR-lore) transcribed verbatim from the old inline blocks.
- MOD `.../rando/evaluators/DeployEvaluator.java` — the six inline blocks (old lines 1403-1770) are replaced by ONE call to `getDeployObjectiveAdjustments()` at the old V83 position, looping `action.addReasoning(note.reason, note.score)`. The old blocks are COMMENTED OUT in place directly below the call (comment-out-superseded rule — a clean build can strip them; DO NOT re-enable alongside the call or scores double).
- Why: this is the "singular ObjectiveAnalyzer" Steve asked for — the deploy phase consults the objective brain instead of scattering objective scoring through the evaluator. Safe because the old region had NO outer control-flow escape between the six blocks (every continue/break is inner-loop-scoped, zero returns), so they were pure additive accumulation — collapsing to one call point cannot change the sum or skip a rule.
- Boundary / SAFETY: behavior-identical. Score APPLICATION stays at the same position (ordering vs vetoes/early-returns unchanged). The 5 objective arms keep the original `isAnalyzed()` gate (via `analyzed && flag`); V99 keeps its original ungated behavior (a method-level `!analyzed` guard was REMOVED after Codex caught it narrowing V99). Added a defensive `v99Senate.getTitle() != null` (NPE→skip on the never-seen null-title case).
- EVOLVES the reorg's SVC-INTEL contract: ObjectiveAnalyzer was tagged "owns NO scores"; it now owns objective DECISION scores. Section note updated in-file.
- Verified: compiles clean both bots (MVN_EXIT=0). Codex/Alfred review across 3 rounds: caught the V99 `!analyzed` narrowing AND the flag-set-before-`analyzed=true` edge (analysis-throws mid-way); both fixed; final verdict **"PASS, ship it."** chosenone re-cloned (drift zero). work-verifier PASS.
- Revert: `git revert` this commit; the old inline blocks are preserved commented-out in DeployEvaluator, and the analyzer method is self-contained.

## 2026-07-07 — Objective-logic consolidation: identity + IWTM steer data → ObjectiveAnalyzer (pure REFACTOR, no behavior change)
- REFACTOR, NOT a new V-tag (consolidates existing V86/V121 Invasion, V83/V88/V108/V110/V109 My Lord, V186 I Want That Map — per the update-old-rule-in-place discipline). Objective-IDENTITY detection that each branch re-matched inline now lives in `ObjectiveAnalyzer`, which is what the deploy/card-selection logic reads. Steve 2026-07-07: "move the version logic to ObjectiveAnalyzer — that's what the deploy evaluator should be looking at."
- MOD `.../rando/strategy/ObjectiveAnalyzer.java` — ADD title-derived flags `isInvasion()` (title contains "invasion"), `isMyLord()` ("my lord" OR "make it legal"), `isWantThatMap()` ("i want that map"), all computed in `analyze()` right after `objectiveTitle` (BEFORE `parseGameText`'s no-flip early return) and cleared in `reset()`. ADD typed IWTM steer slots `getIwtmSystemBpIds()`={208_51,208_051}, `getIwtmSystemTitleFragment()`="starkiller base", `getIwtmPreferredStartingEffect()`="the first order was just the beginning" (populated title-derived so the flip-parse early-return cannot skip them). Each getter carries a reverse-index comment naming its evaluator consumers.
- MOD `.../rando/evaluators/DeployEvaluator.java` — V86 Invasion + V83/V110/V108/V88 My Lord read the analyzer getters instead of matching the objective title inline. V99 SENATE GUARD left DELIBERATELY ungated (it keys on Galactic Senate being on table, not the objective) with an explanatory comment.
- MOD `.../rando/evaluators/CardSelectionEvaluator.java` — V121 Invasion + V88/V109 My Lord read the getters; the V99 CardSelection twin left ungated (keys on the destination candidate title) with a comment; V186 +400 Starkiller-SYSTEM pick reads `getIwtmSystemBpIds()`/`getIwtmSystemTitleFragment()` (keeps the `!contains(":")` guard); V186 +1000 preferred-starting-effect pick reads `getIwtmPreferredStartingEffect()` + `isWantThatMap()`.
- Why: objective playbooks had objective-identity string matches (`contains("invasion")`, `contains("my lord")`, `contains("i want that map")`) duplicated across 9 evaluator branches. Centralizing detection in ObjectiveAnalyzer (the V193 flip-gate template pattern) gives ONE definition per objective + a reverse index of consumers, so a future edit sees every consumer before touching a magnitude.
- Boundary / SAFETY: PURE REFACTOR. Scoring bodies stay IN PLACE — additive-score ordering is unchanged (relocating a branch past a veto/early-return would silently flip decisions, so nothing was moved; only the identity DATA moved). Getter values are byte-identical to the inline checks they replaced (same substring semantics, same null-handling, same gate order). V99 (both bots, Deploy+CardSel) intentionally NOT gated on `isMyLord()`. Typed `Filters.Galactic_Senate` KEPT (not downgraded to a string) per the search-by-type rule.
- Verified: compiles clean in-container (both bots, MVN_EXIT=0, 0 errors). Independent behavior-preservation review by Codex/Alfred (thread `019f3dcb`): **PASS on all three objectives** — no score/ordering/branch-condition drift; ~5% residual flip risk. work-verifier PASS.
- Revert: `git revert` the consolidation commit; the removed inline title-matches are in the diff. The ObjectiveAnalyzer additions are additive (getters + fields) and harmless if consumers are reverted.

## 2026-07-07 — chosenone: full clone from rando (erase accumulated mirror drift)
- CLONE all diverged AI-logic files rando→chosenone (mechanical `models.rando`→`models.chosenone` package/import rename, class bodies identical): `strategy/{ObjectiveAnalyzer,DeckOracle}.java`, `DecisionTracker.java`, `evaluators/{ActionText,Battle,CardSelection,Deploy,DecisionContext,Draw,ForceActivation,Move}Evaluator.java`. Entry points UNTOUCHED (`RandoCalAi` is rando-only; `TheChosenOneAi` is chosenone-only).
- Why: chosenone (the mirror bot) had silently fallen 7+ files behind rando across prior sessions (missing V185/V186/V187 etc.), so "mirror every change" kept breaking and ChosenOne played stale logic. Steve 2026-07-07: "we need to be cloning Chosen One with every iteration ... Simple clone every time" (backup exists). This makes chosenone a true current mirror of rando.
- Boundary: BEHAVIORAL catch-up for chosenone, NOT a pure refactor — kept as its OWN commit (per Codex's review) so the rando refactor commit above stays behavior-neutral. Rando is unchanged by this commit.
- Verified: full module compiles clean (MVN_EXIT=0); zero rando-vs-chosenone logic-file drift remains (line-for-line match, entry points excluded).
- Revert: restore `chosenone/*` from the backup or `git revert` this commit.

## 2026-06-22 — Card image proxy (epic-duel)
- ADD `async/handler/ImageProxyRequestHandler.java` — proxies `res.starwarsccg.org` card images server-side with CORS headers.
- MOD `async/handler/RootUriRequestHandler.java` — routes `/gemp-swccg/imageproxy` to the handler.
- MOD `async/src/main/web/epic-duel/newgui.html` — JS interceptor reroutes the Unity client's card-image fetches through `/imageproxy`.
- ADD `async/src/main/web/epic-duel/StreamingAssets/cardImages.json` — card-id → CDN-URL map.
- Why: the Unity client's image fetches are CORS-blocked by the CDN; proxying server-side fixes it.

## 2026-06-22 — Auto-pass / phase-skip fix (epic-duel) — Option B
- MOD `async/handler/GameRequestHandler.java` `getAutoPassPhases()` (~line 310).
- Change: cookieless clients now `return Collections.emptySet();` (old `return _autoPassDefault;` commented out).
- Why: the epic-duel Unity client sends no `autoPassPhases` cookie, so it fell through to `_autoPassDefault` (all 6 phases) and auto-passed the player's DEPLOY/BATTLE/MOVE — skipping the turn after deploying from Reserve Deck. Commenting out only DEPLOY did NOT fix it. Empty set = player controls every phase. Web GUI unaffected (sends its own cookie).
- Revert: restore `return _autoPassDefault;` in `getAutoPassPhases()`.

## 2026-06-23 — Rando V185: weapon-deployability gate (reserve-deploy)
- MOD `server/.../ai/models/rando/strategy/DeckOracle.java` — ADD `reserveTargetsAreAllUnattachableWeapons(game, playerId, targets)` + private `hasInPlayCharacterAccepting(game, playerId, filter)`. For each parsed pull-target still in Reserve: if it is a non-weapon → not all-weapons (don't block); if a weapon → read its OWN `getMatchingCharacterFilter()` and test whether any of Rando's in-play characters satisfies it via `Filters.filterActive(game, null, and(owner, character, filter))`. A weapon whose filter is `Filters.none` (deploys via game text) is deferred, not blocked. Robust title matching mirrors `hasTargetInZone` inline. (First pass `hasCharacterInPlay()` + `reserveTargetsAreAllWeapons()` — the cruder "any character" check — left commented above per the comment-out-superseded-logic rule.)
- MOD `server/.../ai/models/rando/evaluators/DeployEvaluator.java` — in the V67h `WILL_SUCCEED` branch (~line 798, the live path that scored A Good Friend), after `MEMORY OK`: `v185Game = context.getGame(); if (v185Game != null && reserveTargetsAreAllUnattachableWeapons(v185Game, playerId, parseSourceCardPullTargets(srcGameText)))` → block the pull (`-2000`) and `continue`.
- Why: V67h only verifies a pull target is IN the Reserve Deck, not that it can DEPLOY. `A Good Friend` (225_37) deploys one of {location, epic event, Leia's Lightsaber}; once the location + epic event are down, only the lightsaber remains, and a weapon attaches only to the SPECIFIC characters its own filter accepts. With no such character on the table the deploy has no legal target, so the pull FAILS — wasting the action and revealing/reshuffling the Reserve. Reported by Steve (this lost Rando the game): "no one was on table that could hold a lightsaber, so the pull failed. If Rando had waited for characters to deploy first, he would have been successful."
- REFINED 2026-06-23 (same session, pre-deploy, in place — no new V-tag per the update-old-rule rule): the first pass gated on "Rando has zero characters of ANY kind." Steve flagged this as too crude — a weapon attaches only to specific characters (Leia's Lightsaber → Leia/Ben Solo/Rey ability > 4; Anakin's Lightsaber → Skywalker ability > 3), so Rando can have bodies out yet none able to hold THIS weapon and the pull still dead-fails. Now reads each weapon's own `getMatchingCharacterFilter()` — universal, no hardcoded card/character names.
- Boundary: fires ONLY when EVERY remaining Reserve target is a weapon with NO in-play character its own filter accepts. A non-weapon target still pullable (location/epic event), a weapon with a legal holder already down, or a game-text (`Filters.none`) weapon we can't predict, all leave the pull untouched. `-2000` matches V177's dead-pull penalty (drops below the V60 `+100` baseline; not the absolute `-9999` of a no-target search, leaving a safety valve if the heuristic over-fires). Blocks logged loudly (`V185 WEAPON-NO-HOLDER blocked: ...`).
- Verified: compiles clean (`mvn -pl gemp-swccg-server -am compile`, BUILD SUCCESS) + a 6-agent traced/adversarial review (verdict GO). Confirmed against source: `getMatchingCharacterFilter()` is on `SwccgCardBlueprint:1670` (base impl throws → category-gated to weapons), defaults to `Filters.none`; the matching filter is a pure predicate over the CANDIDATE character (the weapon's own card is never an input), so it evaluates correctly while the weapon is still in Reserve (mirrors `CardTraits.isMatchingCharacter:279`). Leia's Lightsaber → Leia/Ben Solo/Rey ability>4 (`Card225_050.java:41`); Anakin's Lightsaber → Skywalker ability>3 (`Card3_071.java:44`); F-11D Blaster Rifle uses game-text targets with `Filters.none` matching filter → deferred. STILL pending: the live in-game run — the running jar is old code until rebuilt.
- Not yet mirrored: a parallel `chosenone/` copy of `DeckOracle` + `DeployEvaluator` exists (convention: gates are mirrored). `RandoCalAi` is the live AI, so the rando copy is what fixes Steve's game; the chosenone mirror is a pending follow-up.
- Revert: remove `reserveTargetsAreAllUnattachableWeapons` + `hasInPlayCharacterAccepting` from `DeckOracle` and the V185 block in `DeployEvaluator` (both tagged `V185`); the commented-out first-pass helpers above show the prior state.
- NOTE: this install has no `AI_VERSION_HISTORY.md` (the V21–V184 V-tag history lives in the backups / `k2-resources`); the usual "update BOTH changelogs" rule is satisfied here by this single live changelog.

## 2026-06-23 — Rando V186: I Want That Map starting setup (Starkiller Base system + The First Order Was Just The Beginning)
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` `evaluateDeployLocation` (~line 813) — THE location fix. The objective's turn-0 "Choose [Episode VII] location to deploy" is an `ArbitraryCardsSelectionDecision` whose cardIds are temp IDs (`temp0`...). The candidate loop resolves cards via `findCardById(Integer.parseInt(cardId))`, which THROWS for temp IDs before any scoring runs, so every Episode VII candidate kept only the flat +50 base and the pick was effectively RANDOM (the real root cause of Steve's wrong-location report). Added a temp-safe block: resolve each candidate's blueprint from the index-parallel `context.getBlueprints()` list and add +400 to the Starkiller Base SYSTEM (blueprint `208_51`). Gated on `cardId.startsWith("temp")` plus the active objective title containing "i want that map". The action is created (+50) BEFORE the throwing `parseInt`, and `actions.add(action)` is OUTSIDE the try/catch, so the +400 survives. System scores 450 vs 50 for its sites (`208_52..55`) and every other candidate, so it wins decisively. Names the SYSTEM only (its sites are different blueprint IDs) because the system's once-per-turn [download] fetches the SB battleground sites that feed the flip.
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` `evaluateUnknown` turn-0 starting-effect block (~line 7960) — THE effect fix. +1000 to `the first order was just the beginning` when the active objective is I Want That Map. The starting interrupt You Know What I've Come For (208_46) deploys "one Effect immune to Alter"; this effect downloads Jakku/Kijimi battlegrounds to feed the 2-battleground flip. +1000 matches the V80 magnitude; the effect scores ~1100 vs ~100 for the next-best immune-to-Alter Effect, so it wins.
- MOD `server/.../ai/models/rando/strategy/ObjectiveAnalyzer.java` `parseFlipCondition` (after the V160 block) — supporting block, gated on objective title "i want that map": `addLocationFragment("starkiller base")` (marks Starkiller locations objective-relevant for the +150 bonus on LATER real-id deploy/move/protection paths; it is INERT for the turn-0 temp-id pick itself, which the evaluator block above handles) plus adds "the first order was just the beginning" to `requiredCardsOnTable` and `pullableCards` (loss/forfeit protection).
- Why: for the dark "I Want That Map / And Now You'll Give It To Me" objective (208_57) Rando set up wrong. Card text: deploy Tuanul Village + "any other [Episode VII] location" + I Will Finish; flip when First Order controls two battlegrounds with no Resistance Agent at a battleground site. Correct picks: Starkiller Base SYSTEM (208_51) as the other location (its [download] builds the battlegrounds) and The First Order Was Just The Beginning (214_12) as the starting Effect. Neither was named anywhere; the live `ObjectiveAnalyzer` is text-driven and the flip text names no card, and the Starkiller SYSTEM has NO battleground icon so a generic battleground heuristic misses it. (NOTE: the dead `ObjectiveHandler.OBJECTIVE_REQUIREMENTS` map had a 208_57 entry but is never consulted anywhere in `src/`, verified codebase-wide; editing it is a no-op.)
- Scope/boundary: all three blocks gated to the objective title containing "i want that map", so no other objective is touched. The +400 is additionally gated to temp IDs, so later real-id deploys do NOT get steered onto the system over the battleground sites. The ObjectiveAnalyzer fragment applies +150 to both the system and its sites on later real-id paths (on-strategy: build the battlegrounds).
- Verified: compiles clean (`mvn -pl gemp-swccg-server -am compile`, BUILD SUCCESS in container). Two adversarial workflows. The first 4-lens pass CAUGHT that the original analyzer-fragment-only approach was inert (the temp-id `parseInt` throw) and that a +150 would have lost the system-vs-site tie by +30. The corrected evaluator approach was re-verified by a 3-lens pass (does-it-fire end-to-end, magnitude, regression), all PASS, with confirmed score math (system 450 vs 50, effect 1100 vs 100), confirmed context population (`setBlueprints` at RandoCalAi ~943, index-parallel; `setTestingTexts` is never called so the blueprint-id match carries the fix), and confirmed no cross-evaluator merge onto `ARBITRARY_CARDS` candidates. STILL pending: the live in-game run. The running jar is old code until rebuilt.
- Revert: remove the three `V186`-tagged blocks (the `evaluateDeployLocation` temp-safe Starkiller block, the `evaluateUnknown` First Order +1000 branch, and the `ObjectiveAnalyzer.parseFlipCondition` named block).

## 2026-06-25 — Cancel-loop deploy fix: turn-scope the decision-block maps (Rando stopped deploying after turn 2)
- MOD `server/.../ai/models/rando/DecisionTracker.java` — two writes to the PERMANENT `blockedResponses` map redirected to the TURN-SCOPED `turnBlockedActions`: (1) `blockLastActionOnCancel` (~line 443) — comment out the `blockedResponses` write, KEEP the existing `turnBlockedActions` write at ~446; (2) the sequence-repeat detector (~line 302) — comment out the `blockedResponses` write, ADD a `turnBlockedActions` write with the same key/response.
- MOD `server/.../ai/models/chosenone/DecisionTracker.java` — identical mirror (both write sites). chosenone's `:302` had NO turn-scoped sibling, so it was the most exposed copy.
- Why: Steve played a DARK Battle-Droid swarm deck; Rando deployed turns 1-2 then STOPPED, drew into a 13-20 card hand with 27 force, and never built ground presence. Root cause: the cancel-loop guard. A lone Battle Droid (ability 0) fails the V136 §A ability gate (-1500 at every site), so the "Choose where to deploy" pick scores all-negative, Rando clicks Done, and after 3 strikes `blockLastActionOnCancel` wrote the blocked deploy slot into the PERMANENT `blockedResponses` map. That map clears only at game start (mid-game `onPhaseChange` resets the WRONG DecisionTracker instance — RandoCalAi overrides `decide()` and reads its own tracker, which `onPhaseChange` never touches). The block keys on a POSITIONAL action ID the engine REUSES for a different card each decision, so a slot blocked by an un-placeable droid on turn 4 vetoed the legitimate Lord Sidious (A:7) and Dofine+Tey How (A:5) group deploys the planner actually wanted on turn 5. By turn 5 the accumulated set `{3,4,5,7,8,9,10,11}` `-9999`'d every Deploy action; even `V37.4 HAND BLOAT: DEPLOY SOMETHING!` was hard-vetoed.
- Fix mechanism: `getBlockedResponses()` returns the UNION of both maps, so writing to `turnBlockedActions`-only preserves within-turn loop protection (the 3-strike detector still re-trips fresh each turn); `turnBlockedActions.clear()` runs on every turn change (`updateState`, verified to run on RandoCalAi's OWN tracker before the deploy gate reads), so the block no longer survives into later turns. Both permanent writes (cancel-loop 443 + sequence-repeat 302) closed, so the whole permanent-write class is fixed, not half.
- Boundary/scope: every consumer of the block map (`DeployEvaluator`, `MoveEvaluator`, `ActionTextEvaluator`, `DrawEvaluator`, `HeuristicAiBase`) uses it for WITHIN-turn loop-breaking; none relies on cross-turn persistence (`BattleEvaluator`/`CardSelectionEvaluator` don't read it). The cross-turn severity ladder (`getLoopSeverity`/`shouldForceDifferentChoice`/`shouldConsiderConcede`) keys off `sequenceRepeatCount`, NOT this map, so cross-turn loop protection is untouched.
- Verified: adversarial read-only review (GO-WITH-CHANGES verdict — `Handoffs/K2_HANDOFF_2026-06-25_fix-review-verdict.md`) + local council (engineer + voice_of_reason, both confirmed Fix sound + the 302 catch). Empirically 302 never poisoned a deploy in the logged game (14 fires, all non-deploy phases; the turn-5 lockout was 100% the 443 path, 35 `CANCEL LOOP BROKEN` events) — fixed anyway as a same-class landmine. STILL PENDING: live in-game droid-deck run (grep `nohup.out`: turn-5+ deploy is NOT a wall of `-9999`, a droid lands). The running jar is old code until rebuilt.
- Does NOT close: within a SINGLE turn the positional-ID collision still survives (a deploy phase can accumulate slot-IDs that wall off a good card the same turn). Real cure = key the block on the cancelled card's blueprint/title instead of the reused positional slot — follow-up V-tag, not shipped here.
- Pairs with (NOW APPLIED 2026-06-25, see the §A entry below): the §A droid/fodder fix (`CharacterDeploySiteEvaluator.java` ~360-365, gate the -1500 on `oppPower > 0`), which stops droids entering the loop at the source.
- Revert: un-comment the four `blockedResponses.computeIfAbsent(...)` lines (rando + chosenone, at ~302 and ~443) and remove the added `turnBlockedActions` write at ~302 in both files.

## 2026-06-25 — Fix #2: V136 §A ability penalty gated to contested sites only (fodder seeds uncontested + objective sites)
- MOD `server/.../ai/models/common/strategy/CharacterDeploySiteEvaluator.java` (~360-365) — the §A `-1500` "almost never deploy a sub-ability-4 group" gate now fires only when `oppPower > 0` (contested). At an UNCONTESTED site (`oppPower == 0`) a low-ability fodder body (Battle Droids ability 0, weak Neimoidians) falls through to V156 (turn≤2 solo guard) then the `+500` reward, so it deploys to establish presence/drain and to SEED objective flip-sites. Shared `common/` file → fixes both bots.
- Why: a droid swarm has zero-ability bodies, so §A `-1500`'d EVERY ground deploy (contested or not) — Rando couldn't build presence, and could never seed the Invasion objective's Theed Palace Throne Room (V22 boosted the site `+150` but §A `-1500` dominated → net `-1200`, room stayed empty, objective never flipped). Steve: droids are fodder; low ability is fine at an empty site you just want to drain.
- Boundary math (additive-domination discipline): uncontested ability<4 goes `-1500` → `+500` (or V156 `-300` on turn≤2); contested ability<4 unchanged (`-1500`, a weak solo can't win the battle); ability≥4 unchanged; the V151/V181 contested-commit logic is gated `oppPower>0 && abilityPass`, never reachable by a sub-4 droid anyway. Invasion-flip read-only pass confirmed V156/V151/V181 reachable only by ability≥4 bodies → contested-fight discipline not regressed.
- Verified: compiles + boundary math above. STILL PENDING: live droid-deck game (a droid lands at an uncontested site; the Throne Room seeds). Running jar is old code until rebuilt. Does NOT by itself flip Invasion — that also needs a NEIMOIDIAN at the Throne Room via a `with a <type> there` parser/steering enhancement (`Handoffs/K2_HANDOFF_2026-06-25_invasion-flip-fix.md`).
- Revert: restore the gate to `if (!abilityPass)` (drop the `oppPower > 0f &&`).

## 2026-06-28 — V67h dead-search: junk-target pass-through (stop vetoing valid location pulls from multi-clause OR interrupts)
- MOD `server/.../ai/models/rando/strategy/DeckOracle.java` + `chosenone/strategy/DeckOracle.java` (`validatePullFromSourceCard`, just before the WILL_FAIL return ~1217 / ~1110): before returning WILL_FAIL, if ANY parsed target is JUNK (`length > 25` OR contains a digit, the SAME criterion V177 uses), return UNKNOWN instead. The V67h call sites (`DeployEvaluator:789`, `ActionTextEvaluator:4318`, both bots) apply `-9999` ONLY on WILL_FAIL, so UNKNOWN lets the existing weights handle the action.
- Why: forensics over the 2026-06-23..27 logs (workflow `wz72d8p1g`) found Rando PASSING a valid location pull. Turn 1, dark: offered `Deploy a Blockade Flagship site from Reserve Deck` (from the interrupt We Must Accelerate Our Plans). Positive rules scored it +1600 (V100 +1500, V116 +100), but V67h's game-text parser collapsed WMAOP's three OR-clauses into ONE junk target ("3 force to take one effect of any kind, one interrupt with the word 'podracer s'...") and, not finding that junk in Reserve, returned WILL_FAIL → `-9999` → net `-8399` → Pass (2.0) won. Cross-checked: Blockade Flagship: Bridge WAS in Rando's Reserve and the card's filter accepts it, so the pull would have SUCCEEDED. V177 already flagged this exact text as PARSE-JUNK pass-through, but V67h applied its own `-9999` independently; the two detectors disagreed and the meaner one won.
- Boundary (additive-domination + V177 parity): genuine dead-searches use short, digit-free proper-noun targets (My Sister Has It → [chief chirpa's hut, guest quarters], Fall Of The Legend → weather vane, Welcome Home Lord Tyranus → [petranaki arena, works]) — none is junk, so they still WILL_FAIL and correctly block the reveal-the-deck waste. Only a junk parse flips to UNKNOWN. The two dead-search detectors (V177 + V67h) now agree.
- This ADJUSTS V67h in place (no new V-tag), per the update-old-rule rule. Compiles clean (`mvn -pl gemp-swccg-server -am compile`, EXIT=0). STILL PENDING: reload-ai + a real game running We Must Accelerate Our Plans (grep `nohup.out` for `V67h PARSE-JUNK pass-through` and confirm the Blockade Flagship site pull is no longer `-9999`'d). Running jar is old code until rebuilt.
- Revert: delete the `v67hJunkT` junk-gate loop just before the WILL_FAIL return in both `DeckOracle.java` files.

## 2026-06-28 — V22 starting effects: add Silence Is Golden to the preferred list
- MOD `server/.../rando/evaluators/CardSelectionEvaluator.java` (~7937) + `chosenone/.../CardSelectionEvaluator.java` (~7896), the V22 PREFERRED STARTING EFFECT block: added `silence is golden` to the +200 preferred list (was endor shield / alert my star destroyer). When a starting interrupt deploys Effects from Reserve Deck on turn 0, Rando now prefers Silence Is Golden (a dark Effect; Card3_110 original / Card210_045 V). Note: Card4_054 is Levitation, NOT Silence Is Golden (its text just mentions the phrase).
- Adjusts V22 in place (no new V-tag, per the update-old-rule rule). Compiles clean (`mvn -pl gemp-swccg-server -am compile`, EXIT=0). Source-only until reload-ai.
- Revert: drop the `|| titleCheck.contains("silence is golden")` clause in both files.

## 2026-06-28 — V188: Set Your Course For Alderaan — don't deploy ability characters to Death Star sites (drains canceled there)
- MOD `server/.../ai/models/common/strategy/CharacterDeploySiteEvaluator.java` `evaluateSite` (top, right after `opponentId`): early gate returns `-900` when the deploying card has ability ≥ 1, the candidate site is a `Filters.Death_Star_site`, AND the deploying player has `Set Your Course For Alderaan` in play (front). Shared `common/` file → both bots, both deploy paths (`DeployEvaluator` from-hand + `CardSelectionEvaluator` choose-target) via the one `evaluateSite`.
- Why: Steve — "When Rando is playing 'Set Your Course For Alderaan' he should not deploy his characters with ability to the Death Star because he can't Force drain there. Wasted guys deployed there last game." The objective's FRONT text: "At Death Star sites, your Force drains and battle damage against you are canceled." An ability character at a Death Star site can't drain (its whole purpose), so it's wasted; steer it to a drainable battleground.
- Front-only detection (no flip false-positive): `PhysicalCardImpl.getBlueprint()` (`:497`) returns `_backBlueprint` once flipped, so `getTitles()` / `Filters.title("Set Your Course For Alderaan")` stops matching the instant the card flips to "The Ultimate Power In The Universe" — at which point the Death Star becomes the win condition and you WANT presence there, so the gate correctly lifts on its own.
- Boundary (additive-domination): NARROW early gate, fires ONLY for (ability ≥ 1 + Death Star site + this objective front). Ability-0 fodder untouched (may still hold a Death Star site for the flip/superlaser). Every other objective, every non-Death-Star site, and the flipped back side are untouched (§A/§B/§C/§D run normally). `-900` sits clearly below a drainable battleground's net (~+600–800 = §A win +500 plus §B), so ability drivers route to drainable sites; not an absolute block (if a Death Star site is the only legal target, Rando can still go). No clean engine "drains canceled here" query exists, so we key on the objective + site.
- NEW V-tag (V187 = other K-2's concurrent work; this is the next free tag). Compiles clean (`mvn -pl gemp-swccg-server -am compile`, EXIT=0). Source-only — NOT deployed (Steve is holding reload-ai to test a different deck first).
- Revert: delete the `v188Ability` early-gate block at the top of `evaluateSite` (tagged `V188`).

## 2026-06-29 — V120 fix: weapon-pull block no longer mis-fires on a CHARACTER pull (Vader)
- MOD `server/.../rando/evaluators/ActionTextEvaluator.java` + `chosenone/.../ActionTextEvaluator.java` (V120 weapon-title match, ~1877): the bidirectional `contains()` match (added by V125 for abbreviated/prefixed weapon titles) also matched a CHARACTER pull whose name is a substring of a weapon title. "Deploy Vader from Reserve Deck" parsed `vader`; the deck's `Darth Vader's Lightsaber` title contains `vader`, so V120 classified the Vader CHARACTER pull as a weapon pull with no holder and HARD-BLOCKED it at -9999 every turn. Vader is the Hunt Down deck's flip engine, so the objective never flipped and Rando lost. Confirmed in the 2026-06-29 replay (`V120 WEAPON-PULL BLOCK: 'Deploy Vader from Reserve Deck' (weapon 'Vader', criteria 'vader') ... -9999`).
- Fix: for the loose "weapon-title contains parsed-name" direction, require the parsed name to cover the weapon title's LAST significant word (the weapon noun, e.g. `lightsaber`), not just the owner portion. `vader` → title core noun `lightsaber`; `vader`.contains(`lightsaber`) = false → no longer a weapon pull. Exact-match and parsed-name-contains-full-title directions (the V125 cases, e.g. `Vader's Lightsaber` → `Darth Vader's Lightsaber (V)`) unchanged: `vader's lightsaber`.contains(`lightsaber`) = true. Strips `(V)`/parenthetical suffixes; ≥4-char noun threshold mirrors the V185/DeckOracle idiom.
- Boundary: V120 still fires on a real weapon pull with no holder (`Deploy Vader's Lightsaber from Reserve Deck` with no Leia/Ben/Rey on table → -9999). Only the character-name false positive is removed. Adjusts V120/V125 in place — no new V-tag (update-old-rule rule).
- Verified: compiles clean (`mvn -pl gemp-swccg-server -am compile`, EXIT=0). Source-only until reload-ai.
- Revert: restore the single `if (pTitleLower.contains(v120WeaponLower) || v120WeaponLower.contains(pTitleLower)) { ... }` in both files.

## 2026-06-30 — V61c: always keep 3 cards in the Reserve Deck for battle/weapon destiny
- GOAL (Steve): "He should probably always keep 3 cards in reserve for any battle that turn." Rando kept emptying its Reserve Deck during activation, leaving nothing to draw battle/weapon destiny from, then losing battles it should have won.
- ROOT CAUSE was a conflict between two of Steve's own rules: V168 (ActionTextEvaluator) forced Rando to ALWAYS activate Force (+5000, never pass), and activating moves cards Reserve → Force Pile. So "always activate" literally drained the reserve. V61c's amount-cap alone couldn't hold the line: the engine forces ≥1 per activation (V43), so once the reserve was at 3 it eroded 3→2→1→0 over turns (`activating 1 of 3` → `1 of 2` → `1 of 1` in the log).
- FIX, two halves:
  1. `server/.../rando/evaluators/ForceActivationEvaluator.java` `calculateActivationAmount` (~186): cap the activation amount so the Reserve Deck never drops below 3 — `amount = max(1, min(maxAvailable, reserveDeck - 3))`. Handles reserve > 3 (activate down to leave 3). Logs `V61c DESTINY BUFFER (keep 3 in reserve)`. (Earlier force-pile-gated version removed — it almost never fired because Rando draws its Force to hand.)
  2. `server/.../rando/evaluators/ActionTextEvaluator.java` — NEW this session, the piece that actually makes it hold. When `reserveDeck <= 3`, carve an exception into BOTH activation guards so Rando PASSES activation instead of draining the last 3:
     - V168 always-activate block (~166): if reserve ≤ 3, score "Activate Force" at −6000 (below Pass ~5–8) instead of +5000, so the action-choice lands on Pass.
     - V38.3 "you have not activated Force — pass?" confirm (~1342): if reserve ≤ 3, honor "Yes" (pass, +9999) instead of the usual bounce-back to "No", or the pass would be undone.
- BEHAVIOR: reserve > 3 → activate, leaving exactly 3. reserve ≤ 3 → pass activation entirely this turn (Rando lives off its Force Pile) to protect the 3-card destiny buffer. Self-regulates: destiny/weapon draws deplete the buffer, the Used Pile reshuffles back in when the reserve empties, and Rando activates down to 3 again next time it has a surplus.
- TRADEOFF (Steve accepted, "for now"): when the reserve is ≤ 3 Rando gets no new Force from activation that turn, so it's slightly more passive when its reserve is low. FUTURE refinement (planned, not built): pass activation ONLY when Rando intends to battle that turn; a deploy-and-end-turn-without-battling turn can safely activate everything. That needs reading battle/deploy intent BEFORE the activate phase.
- Boundary (additive-domination): −6000 dominates the +5000 it replaces, and Pass (~5–8) sits well above it and above the −100 BAD_ACTION_THRESHOLD, so Pass wins cleanly without tripping the "all actions bad" fallback. reserve ≥ 4 leaves V168/V38.3 fully intact (always-activate unchanged). Both edits are RANDO-side only (ActionTextEvaluator + ForceActivationEvaluator in `rando/`); chosenone not touched.
- Verified: compiles clean (`mvn -pl gemp-swccg-async -am package`, EXIT=0); both new strings present in the running `web.jar` (`pass activation, keep 3 for destiny` = 1, `confirm pass, keep 3 for destiny` = 1); restarted + operational. STILL PENDING: live game confirming the reserve holds at 3 and battles get destiny (grep the log for `V61c DESTINY BUFFER: reserve=` pass lines).
- Revert: in ActionTextEvaluator restore the unconditional V168 (+5000 on "activate force") and the unconditional V38.3 (No +9999 / Yes −9999); in ForceActivationEvaluator restore `amount = maxAvailable` (V57 full).

## 2026-06-29 — V179 fix (#4): don't rank a download of a location you already HOLD above deploying it (A Good Friend / A Cunning Warrior)
- MOD `server/.../rando/strategy/DeployPhaseScript.java` + `chosenone/.../strategy/DeployPhaseScript.java` (`resolveSteps` section D + new `namedLocationInHand` helper): when the DPS bucketer classifies a [download]/reserve-pull action, a target naming a specific LOCATION classifies into the LOCATIONS bucket ONLY if that location is not already in the bot's hand (`s == Step.LOCATIONS && namedLocationInHand(gameState, playerId, target)` → skip).
- Why: replay 2026-06-29 (Rando LIGHT, Rey/Luke Saga). A Good Friend + A Cunning Warrior each [download] one of {a location, an epic event, a weapon}. Their once-per-turn download was classified as a high-priority LOCATIONS pull (DPS LOCATIONS bucket; V67ai +1800) because the target text contains "jedi village" — outranking deploying the actual Ahch-To: Jedi Village from hand (+1900, V162). So Rando FIRED THE DOWNLOAD FIRST while Ahch-To was still in hand; nothing was legal (Be With Me needs an Ahch-To location on table; the location was in hand not reserve; the weapon had no holder), the engine correctly reshuffled, and the once-per-turn download was burned. Rando deployed Ahch-To seconds later, too late; Be With Me (the epic event) never came out all game. NOT an engine/card bug (verified: `DeployCardFromPileEffect` re-checks `Filters.deployable` at resolve; Be With Me's `canSpot(AhchTo_location)` was legitimately false) — a Rando action-ordering bug.
- Fix mechanism: dropping the LOCATIONS tag moves the download out of the LOCATIONS bucket (it still classifies by its OTHER targets — the weapon → WEAPONS), so the DPS walk deploys the real Ahch-To from hand in the LOCATIONS step FIRST; the download fires in a later step once the location is on table and can then pull Be With Me.
- Boundary: `namedLocationInHand` only matches a specific named location (≥4 chars, not a bare category word — "location"/"site"/"system"/"battleground"/"docking bay"/"mobile site") against a LOCATION card actually in the bot's hand. So generic/objective location pulls ("Deploy a location from Reserve Deck") never gate (no card is titled "location"), and a legitimate location pull whose target sits in RESERVE (not hand) is untouched. Adjusts the V179 DPS location classification in place — no new V-tag (update-old-rule rule).
- Verified: compiles clean (`mvn -pl gemp-swccg-server -am compile`, EXIT=0); live in the running jar (`namedLocationInHand` in both bots' `DeployPhaseScript.class`, HTTP 200); traced against the replay case (targets [be with me, jedi village, leia's lightsaber] → jedi village gated out → steps={WEAPONS} → Ahch-To hand-deploy wins LOCATIONS). PENDING: a live Saga game (Ahch-To deploys before the download; Be With Me pulls).
- Revert: in both files restore the loop body to `Step s = stepForPullTargetText(t); if (s != null) steps.add(s);` and delete the `namedLocationInHand` helper.

## 2026-06-25 — V156 UPDATED: smart solo-deploy hold (ability/weapon/battleground aware) — entry backfilled 2026-07-01
- MOD `server/.../ai/models/common/strategy/CharacterDeploySiteEvaluator.java` (~462, logging at ~517/~522) — the old V156 turn≤2 solo guard (-300 flat) rewritten per Steve's spec. The hold now fires ONLY when ALL of: candidate is a battleground SITE (not a system, not a non-battleground), deploying card is a CHARACTER (ships/vehicles exempt — ability-2 ships are often fine solo), turn ≤ 2, and the site is NOT objective-flip-relevant (seeding flip sites must stay legal) — with a `v156FlipNotReady` carve-back: a flip-relevant site does NOT exempt when the flip is mechanically not ready yet (Verge of Greatness: Death Star not orbiting Scarif → a turn-1 solo seed is still a free kill, hold it). Solo is allowed when ability ≥ 6, or ability ≥ 5 with a matching weapon in hand (the weapon's own `getMatchingCharacterFilter()` accepts the deployer — no hardcoded names); otherwise `V156 SOLO HOLD` -600, and `V156 SOLO OK` +250 when a buddy is available to join. Added `isObjectiveRelevantSite` param to `computeTeamViability` + call site. Shared `common/` file → both bots.
- Why: Steve (2026-06-25, after Ozzel died solo turn 1): solo characters at battlegrounds are free kills unless they can defend themselves. "Ability 6 solo is fine. Ability 5 with a weapon is probably ok. Under that, wait for a buddy. Sites only, battlegrounds only, turns 1-2."
- Boundary (additive-domination): -600 holds a lone sub-threshold character below the §A +500 uncontested reward (net -100 < Pass) without hard-blocking (an objective-forced deploy can still exceed it); +250 buddy nudge cannot flip a contested -1500 site positive. Non-battleground deploys (senators to the Senate etc.), systems, turn 3+, and ability-0 fodder at flip sites are all untouched.
- Committed: rode into `d72ced949` (the V188 commit swept this file). Verified live 2026-06-28+ (`V156 SOLO HOLD` fires in the log). PENDING: nothing.
- Revert: restore the pre-2026-06-25 V156 block (flat -300 turn≤2 solo guard) — the old logic is commented out in place above the new block.

## 2026-06-28 — V61b: battle anyway when OVERPOWERING, even with an empty Reserve Deck — entry backfilled 2026-07-01
- MOD `server/.../ai/models/rando/evaluators/BattleEvaluator.java` (~627) — before the V61 empty-reserve penalty chain (-800/-400/-200 for battling with no destiny draw), re-scan the friendly-vs-opponent power margins across battle-eligible sites; if the best margin ≥ 8 (`v61Overpowering`), SKIP the V61 penalties and battle on raw power. Destiny is a luxury when you outpower a site 18-to-1; the overflow damage wins regardless.
- Why: Steve's 2026-06-28 game — Rando sat on a massively overpowered Hoth site (26 power vs 4 per the code comment) and refused to battle all game because its Reserve Deck was empty (V61 penalties dominated). "He should have battled anyway, he overpowered by a lot."
- Boundary (additive-domination): margin < 8 leaves the V61 chain fully intact (close battles still correctly avoided with no destiny); margin ≥ 8 only removes the V61 penalty, all other battle scoring runs normally. Rando-side only.
- Verified: in the running jar; fired live (Starkiller site, 18 vs 1 → battled, logged `V61b`). UNCOMMITTED — source sits in the working tree.
- Revert: delete the `v61Overpowering` re-scan + the `if (v61Overpowering)` skip wrapping the V61 chain (tagged `V61b`).

## 2026-06-28 — V79b: Death Star parsec choice — steer Verge of Greatness to Scarif (parsec 7) — entry backfilled 2026-07-01
- MOD `server/.../ai/models/rando/RandoCalAi.java` (~692) — NEW MULTIPLE_CHOICE handler for the engine's "Choose parsec to move to" prompt: when Verge of Greatness is on table, pick the result option closest to parsec 7 (or an explicit orbit/Scarif option) and return its index. The Death Star must walk 4→6→7 by hyperspeed and orbit Scarif for the objective; the old generic chooser picked effectively at random, so the Death Star loitered at parsec 4.
- ALSO MOD `server/.../ai/models/rando/evaluators/MoveEvaluator.java` (~291) — the V79 parsec regex now takes the LAST parsec mentioned in the action text (`while (find)` instead of first-match). Kept although INERT for this decision (the move action's text carries no parsec — the real decision is the MULTIPLE_CHOICE above; this was the first, mis-targeted attempt, left as a harmless robustness fix).
- Why: Steve, two games running: "verge of greatness the death star needs to move to the right parsec number. we never got that right in the previous fix." V79 (2026-05-15) steered the MOVE action but the parsec NUMBER is a separate MULTIPLE_CHOICE decision V79 never saw.
- Boundary: handler gated on Verge of Greatness in play AND the decision text containing the parsec-choice prompt; every other MULTIPLE_CHOICE falls through to the normal evaluators. Extends the V79 family in place (V79b = the missing decision surface, not a behavior change to V79).
- Verified: in the running jar; PENDING live Verge game (grep the log for `V79b` and confirm 4→6 turn 1, 6→7 turn 2). UNCOMMITTED — source sits in the working tree.
- Revert: delete the `V79b` MULTIPLE_CHOICE block in `RandoCalAi.java` and restore `if (v79m.find())` in `MoveEvaluator.java`.

## 2026-06-28 — V187: -300 to duplicate starting effects (DeckOracle-scored starting-interrupt choice) — entry backfilled 2026-07-01
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` (~7937, inside the turn≤0 V22 starting-effect block) — when a starting interrupt lets Rando choose Effects to deploy from Reserve Deck, any candidate whose title appears MORE THAN ONCE in the decklist gets `V187 DUPLICATE` -300. MOD `server/.../ai/models/rando/strategy/DeckOracle.java` (~389) — NEW `countCopiesByTitle(title)` (case-insensitive title count over the deck catalog) to power it.
- Why: Steve (2026-06-28): "I want a -300 points to any effect that Rando has duplicates in the deck of. So use deck oracle to help score which starting effects should be chosen." Effects are unique-in-play; deploying the single copy of a one-of keeps the deck clean, while deploying one of a pair leaves a dead duplicate in the deck. Prefer the one-ofs at setup.
- Boundary (additive-domination): -300 re-orders only WITHIN the starting-effect candidate pool (V22 preferred-list +200 and V80/V186 objective boosts at +1000 still dominate it by design — a strategy-named effect wins even if duplicated). No effect is blocked, only demoted. Rando-side only.
- Committed: rode into `8fd884375` (swept with the V67h/V22 commit). Verified: in the jar; PENDING a live game with a duplicated effect in the deck (grep `V187 DUPLICATE`).
- Revert: remove the `V187` block in `CardSelectionEvaluator.java` and `countCopiesByTitle` in `DeckOracle.java`.

## 2026-06-28 — Logging: Rando decision log survives restarts (mainlog appender) — entry backfilled 2026-07-01
- MOD `src/gemp-swccg-async/src/main/resources/prod-log4j.xml` (~31-36) — the `com.gempukku` logger (`additivity=false`, previously stdout-only) gains `<AppenderRef ref="mainlog" level="info"/>`. All Rando V-tag decision lines (logger.warn/info) now ALSO land in the `mainlog` RollingFile → `logs/gemp-swccg.log` (10MB rotation into `logs/YYYY-MM/app-*.log.gz`).
- Why: Steve: "Last replay is not readable?" The decision log lived on stdout only, which moved between `nohup.out` and the container TTY across restarts — every restart orphaned the previous game's decisions. A stable file makes "check the last game" work every time.
- Boundary: log volume roughly doubles the file appender's intake (stdout keeps its copy); rotation caps disk. No gameplay behavior change.
- Verified: live — `V61c`/`V61b` lines observed in `logs/gemp-swccg.log` post-restart. UNCOMMITTED — sits in the working tree.
- Revert: remove the `mainlog` AppenderRef from the `com.gempukku` logger.

## 2026-07-01 — V177 dead-search: CATEGORY RESCUE (stop false-blocking type-phrase downloads like I'm Sorry's interior Cloud City site) — adjusts V177 in place
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` (~261, inside the V177 block branch) + chosenone mirror (~242) — before applying the -2000 DEAD SEARCH block, consult `DeckOracle.validatePullFromSourceCard(RESERVE_DECK, sourceGameText)` — the SAME validated path V67h and ActionAudit already trust (V82.1 category fallback + V82.2 blueprint-predicate fallback). WILL_SUCCEED → no block, log `V177 CATEGORY RESCUE`, the action re-enters normal scoring. Any other outcome → the -2000 block stands unchanged.
- Why: TDIGWATT game 2026-07-02 02:09 UTC (replay `7co2xviwqo5q3zac`), Rando lost by depletion in 4 turns. I'm Sorry (V) may [download] an interior Cloud City site; Dining Room (V) + Security Tower (V) sat in Reserve ALL GAME. The raw `hasTargetInZone` title matcher cannot match the type-phrase "interior cloud city site" against titles like "Cloud City: Dining Room" (last-word "site" is in no title; the ≥6-char word-rescue only tries "interior"), so V177 declared DEAD and blocked the download 19 times while V67h said WILL_SUCCEED in the SAME 19 evaluations — an in-log self-contradiction. The block froze Rando at 1 CC site (flip needs 3 controlled Bespin locations), froze Force income at 8 vs 12, and the two downloadable sites were eventually burned as Force loss. Same false negative blocked Piett's "[any commander]" search 27x on T4. This is the detection-path-mismatch class from Rando_AI_Rule_Audit.xlsx.
- Boundary (additive-domination): NO magnitude changes — pure gate alignment. Genuine dead searches (proper-noun targets: Force Projection, Weather Vane, Petranaki Arena) still fail the validator and still block at -2000. The rescue fires ONLY on WILL_SUCCEED — exactly the verdict V67h acts on — so the two dead-search detectors can no longer disagree. Rescued actions score naturally (V183's deck-title scan is unaffected: type-phrases are not deck titles; V97/V116 positives apply as designed).
- Accepted false positive: V82.1 maps the type word only ("site" → ANY location in Reserve, qualifiers ignored). If every interior CC site is out of Reserve but any other location remains, the rescued search fires and no-ops: one wasted once-per-turn action + reshuffle, no Force cost. Bounded per-turn waste vs. the unbounded strategic damage of a permanent false block; consistent with feedback_reserve_deck_pulls (pulls fire every turn). Do NOT "improve" this by tightening `validatePullFromSourceCard` with qualifier matching — V67h and ActionAudit share it; that would change three rules at once. NOTE: `recordFailedPull` (the 2-strikes backstop) has ZERO callers anywhere in src — dead wiring — so the no-op repeats each turn; separate fix if it ever matters.
- Mirrored to chosenone (identical insert, chosenone DeckOracle types).
- Verified: compiles clean in-container; jar byte-search PRESENT in both bots' classes; work-verifier PASS 5/5. LIVE-VERIFIED 2026-07-02 (2 headless self-play games, k2_player.py holding the LIGHT seat): `V177 CATEGORY RESCUE` fired 3x on the I'm Sorry action, zero false DEAD SEARCH blocks, and I'm Sorry deployed an interior CC site from Reserve on turn 1 in BOTH games (Carbonite Chamber / Security Tower, replays 94d9142hqg0zs0fr + x5cbzmegdjsx24zv).
- Revert: delete the `V177 CATEGORY RESCUE` if/else wrapper in both ActionTextEvaluators — the original block body is the untouched else branch.

## 2026-07-04 — V189: net-value drain gate (never pay more to initiate a drain than it's worth) + V140 repaired in place
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` (`evaluateForceDrain`, right after the V24.15 zero-drain check) + chosenone mirror — NEW V189: query `getInitiateForceDrainCost` (the exact query the engine charges via `PayInitiateForceDrainCostEffect` — sums every INITIATE_FORCE_DRAIN_COST modifier: Battle Order, Battle Plan, anything future, no card-name hardcoding) and hard-block (-2000, early return) any drain whose initiation cost exceeds the drain amount.
- ALSO MOD same files — V140 UPDATED in place: its hand-rolled "Battle Plan on table = universal waiver" scan was factually wrong (Battle Plan, Card8_035, waives only Battle Order's, Card8_118, modifier and imposes its OWN identical 3-Force tax; only occupying a BG site + BG system waives it). Detection replaced with the same engine cost query; old scan commented out in place. Reasoning/log strings updated to the engine-truth wording.
- Why: Steve, 2026-07-04: "He should not have paid to drain for 1 with battle plan or battle order on the board." Game `20jqtseod148of4y`: with asdf's Battle Plan on table, Rando paid 3 Force to drain 1 at Audience Chamber TWICE. Offender A: the untagged "no deployables — drain is our only pressure" +70 boost returns before any net math. Offender B: V140's false Battle-Plan waiver (+60). V189 fronts both.
- Boundary (additive-domination): cost-0 games completely unaffected (0 is never > drain; drainAmount > 0 guaranteed past V24.15). Net 0 ALLOWED (pay 3 drain 3 = 3 permanent Life Force damage for 3 recycling Force). Pay-3-drain-2 now blocked — deliberately SUPERSEDES V52's old "drains >= 2, net -1, marginal but worth it" stance (AI_VERSION_HISTORY ~line 2551); Steve's rule wins. Blocked drain scores exactly -2000 (nothing scores a drain action earlier), losing to Pass (+5) with ~2005 margin; the early return skips the +70 boost / V140 / V52 / V29.9 — the offenders. V104 (drain ≤ 1 under BO) becomes a redundant backstop fronted by V189 (kept, noted in code). Known remaining gap (pre-existing, not fixed here): the untagged forceAvailable<3 branch still applies -50 to genuinely FREE drains before V140 runs — under-draining, fail-safe, separate item.
- UPDATED 2026-07-06 (in place, Steve 2026-07-04: "We should still allow drain 2 for 3 force if there is enough force to deploy and move everything that rando wants to do that turn"): two tiers now. Net <= -2 (pay 3 drain 1 — the actual 07-04 offender) stays FLAT-BLOCKED. Net -1 (pay 3 drain 2) is BUDGET-GATED: allowed iff `forcePile - initCost >= sum(live deployable hand costs, persona-dead excluded via AiCardHelper.isDeadCard) + 2 move allowance`. Drains are CONTROL phase (before Deploy/Move), so the budget is a forecast; it is recomputed from live gameState at EVERY drain decision, which satisfies "recheck whenever new force is used" by construction (DeckOracle.refresh freshness pattern). Steve's "move the deploy cost evaluator right after activation" was NOT built literally: DeployPhasePlanner caches per turn (createPlan ~116-120), so priming it at Control would serve a stale over-budgeted plan into the V38.4/waitingForPlannedCards hold-back machinery (log proof: 07-04 game, drain paid at 17:16:50, plan created 17:16:57 with force=9 not 12); per-decision recompute is semantically identical with none of that risk. Follow-up only if a second consumer needs the planner primed early. REWORD of the old boundary line: pay-3-drain-2 is budget-gated, not flat-blocked — V52's old "net -1 marginal but worth it" stance is RESTORED only while the turn's deploy+move intent stays funded. Boundary rows: drain-1-cost-3 → net -2 → blocked (both versions); drain-2-cost-3 at 12F/no plan → allowed (+70, beats Pass); 12F/5-cost plan → 9 >= 7 → allowed (V52 +50 + multi-drain +200, ceiling ~+340 with V29.9's +40+30); 6F/5-cost plan → 3 < 7 → blocked -2000; turns 1-2 under BO an allowed net -1 with deployables scores V48 -50 + multi-drain +200 = +150 and fires (pre-V189 behavior, faithful to the budget rule, NOT a regression). Known over-allow gaps (all err toward one marginal drain): forecast omits Effects/weapons/devices deploy costs, Reserve-pull costs, and opponent-turn reacts; an exception inside the budget loop fails open (net -1 proceeds ungated at logger.debug) — same fail-open class as v1. V104-fronting note stays true (under BO an allowed net -1 is drain 2; V104 only fires on drain <= 1). Grep: `V189 NET -1 DRAIN ALLOWED` / `V189 DRAIN NET-1 BUDGET BLOCK`. Revert of the update alone: restore the single `cost > drain → -2000` block.
- Mirrored to chosenone. Verified: compiles clean; jar + live-game verification pending (grep `V189 DRAIN NET-VALUE BLOCK`; a Battle Plan game should show the pay-3-drain-1 blocked while a 3+ drain still fires).
- Revert: delete the V189 block in both ActionTextEvaluators; un-comment V140's old scan and restore its old reasoning/log strings.

## 2026-07-04 — V190: starships deploy to systems, not docking bays (pull gate + destination widening)
- MOD `server/.../ai/models/rando/strategy/DeckOracle.java` + chosenone mirror — NEW `reservePullFetchesOnlyStarships(gameText)`: resolves each parsed pull-target against the RESERVE catalog (docking-bay targets match docking-bay TITLES only — the V82.3 'docking bay'→LOCATION category fallback let the SYSTEM Nal Hutta satisfy a docking-bay check, not repeated; type-words by category; exact-phrase title match; deliberately NO last-word fallback so parser junk resolves to nothing). NEW static `spaceLocationOnTable(gameState)`: any SYSTEM or SECTOR on table (fail-open on null state).
- MOD `server/.../ai/models/rando/evaluators/DeployEvaluator.java` (V67h WILL_SUCCEED branch, after the V185 gate) + chosenone mirror — V190 gate: pull whose remaining fetchable targets are ALL starships + no space location on table → block (-12000 + continue). Blocked action lands ~-6400 after the skipped downstream bonuses, far below the -100 viability floor; the observed offending stack was +9380 (V67ai +2000 x2, V82 +2500, V100 +1500, V67ak +800...), all dominated.
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` + chosenone mirror — destination half WIDENED in place (old untagged -1500 docking-bay rule, 2026-06-03): now `isDockingBay || isGroundSite` = ALL sites, message "STARSHIP TO SITE = 0 POWER!". Behavior change: non-docking-bay sites go from -10 ("STARSHIP TO GROUND - unusual!", branch commented out in place) to -1500.
- Why: Steve, 2026-07-04: "He should not have deployed starships to a docking bay. Only deploy starships to systems." Game `20jqtseod148of4y`: Court Of The Vile Gangster's pull fetched Elis In Hinthra (and later Dengar In Punishing One) and parked them at Executor: Docking Bay at 0 power, three segments running; the 4 Force also starved the V29 PAIRED buddy deploy (Wooof never joined Greedo).
- Boundary: turns 1-2 of that same deck are UNAFFECTED (docking bays still in Reserve → the pull fetches a LOCATION → helper false → pull fires, correctly deployed both docking bays). Ship + system/sector on table → gate stands down, destination eval steers to space (system positive vs site -1450). Vehicles untouched (non-STARSHIP fetch → helper false; vehicle destination logic separate). Mixed starship+other fetches → helper false (conservative).
- KNOWN LIMITS, documented deliberately: (1) a space location where the ship cannot legally deploy still stands the gate down (icons/ownership not checked, v2 item); (2) the gate blocks the bad pull but does NOT itself make Rando deploy the system first — the "Deploy Nal Hutta from Reserve Deck" action is unclassified by the DPS walk and unreachable in the LOCATIONS bucket, a separate follow-up; (3) SECTORS count as valid space parking and are NOT penalized as destinations — Steve said "systems", sectors are legal space locations, awaiting his ruling; (4) a deck with no system at all keeps its ships in Reserve permanently — consistent with Steve's rule.
- Mirrored to chosenone (note: chosenone has no V185 block — pre-existing mirror debt, NOT ported here; V190 inserts standalone).
- Verified: compiles clean; jar + live-game verification pending (grep `V190 STARSHIP-NO-SYSTEM blocked` and `STARSHIP TO SITE = 0 POWER`; success = no ship ever parks at a docking bay).
- Revert: delete the V190 gate blocks (both DeployEvaluators), the two V190 helpers (both DeckOracles), and restore `if (isDockingBay)` + the commented isGroundSite branch (both CardSelectionEvaluators).

## 2026-07-06 — V51/V105/V112/V117 (in place): Battle Order 4th-slot deadlock + occupation-predicate unification
- Why: Steve, 2026-07-06, the "Verge of Greatness" game (replay `unli50oa1ur8bdux`): Rando controlled Scarif (battleground SYSTEM) + Scarif battleground sites and drained there 70+ times, qualifying for his own Battle Order (dark Effect 13_54, a K&D defensive shield that taxes the OPPONENT's drains +3 while Rando occupies both theaters) the entire game — and never deployed it. Two faults: (1) `ShieldStrategy.prefers4thSlot` returned "Battle Order" all game (occupation genuinely true), and the V105/V107 4th-slot code hard-blocked every OTHER offered shield at -5000 (2760 fires) to force a Battle Order that was never in the candidate list, so the 4th slot deployed nothing (a prefer-an-unavailable-card deadlock); (2) latent — the three Battle Order occupation checks (V51 deploy path, V51 shield path, V112) hand-rolled `hasBGSite/hasBGSystem` via `getCardsAtLocation` + owner-match, which disagreed with V105's power-based scan (V51 logged hasBGSystem=false while a ship sat at Scarif system). Same detection-mismatch class as the V140 fix (2026-07-04).
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` + chosenone mirror. TWO new private helpers per bot:
  - `occupiesBothTheaters(game, playerId)` = `Filters.canSpot(game, null, and(occupies(pid), battleground_site)) && ...battleground_system` — the engine's own `OccupiesCondition` semantics, exactly what Card13_054 checks, so gate and card can never disagree. Fails closed (no false deploy). Replaces the hand-rolled loops in V51 (both deploy + shield paths) and V112. Old loops commented out in place (feedback_comment_out_old_rules).
  - `preferredShieldInCandidates(context, preferredTitle)` — scans the decision's full candidate list (ARBITRARY_CARDS blueprint path + standard card-id path) for the preferred title. V105 and V117 now only pursue/hard-block for the preferred 4th-slot card when it is ACTUALLY on the menu; otherwise they HOLD the slot closed (Steve's closed-by-default 4th slot) instead of spamming a -5000 block for a card that can't be picked. Kills the 2760-fire deadlock. (Steve's call 2026-07-06: "just stop the spam" — keep the 4th slot closed when Battle Order isn't offered, do not fall to a random shield.)
- Boundary (additive-domination): NO magnitude changes — the +2000 boost / -5000 hold / -9999 gate keep their values; only the boolean predicates feeding them change. When Rando occupies both theaters AND Battle Order is offered → V51 passes (+50 / no block) and V105 boosts it +2000 → it deploys. When Rando does not occupy both → -9999 (still blocked, no self-tax, no regression). When the preferred card is absent → HOLD (slot closed), no spam. Occupy-vs-control: the card triggers on OCCUPY (presence), so the gate uses occupy, matching Card13_054. Known unmodeled edge (not fixed, over-deploy not self-tax): the helper omits Battle Order's `battlePlanOnTable` waiver branch — if a Battle Plan is already on table both taxes are waived and Rando's Battle Order is redundant; deploying it then wastes a slot but does not self-tax. Noted for a follow-up.
- Verified: compiles clean in-container (both bots). Jar byte-search + work-verifier + SELF-PLAY pending: a Scarif dark deck game where Rando controls both theaters should now show `V51 ... Requirements met`, `V105/V107 4TH SLOT: BOOST 'Battle Order'`, and Battle Order reaching the table; and NO `HARD-BLOCK ... prefer 'Battle Order'` spam when it isn't offered. NOTE: whether Battle Order is actually OFFERED (K&D shield-play is 4x/game capped) is a separate limiter self-play must confirm.
- Revert: restore the four hand-rolled occupation loops (commented out in place) and remove the `preferredShieldInCandidates` guard from V105/V117 (revert the `preferredOnMenu` / `!preferredShieldInCandidates` conditions to `preferred != null` / `v117Preferred == null`); delete both helpers.

## 2026-07-06 — V51 EARLY-DEPLOY (in place): Battle Order/Plan deploys turns 1-2 when Rando occupies both theaters
- Why: Steve, 2026-07-06 (follow-up to the same-day V51/V105/V112/V117 Verge fix): "Battle Order can be deployed during turn 1 or 2 as well if a battleground site and system is occupied by Rando" + "Just have to occupy. does not matter if opponent also occupies." Battle Order (dark shield 13_54) taxes the OPPONENT's drains +3 while Rando occupies a battleground site + system, and the tax COMPOUNDS every turn — so deploying it early is high value. GAP: with occupation true but before the turn-3 4th slot (SHIELD_PACING allows 2 shields turn 1, 3 turn 2; the V105 +2000 4th-slot boost only opens at shieldsRemaining()<=1), Battle Order scored only the base SITUATIONAL_HIGH-untriggered 80 (its conditionsMet is never set — no opponentObjective/opponentCardsSeen/auto-play trigger), losing to auto-play shields (200). So it never deployed turns 1-2 even when qualifying.
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` + chosenone mirror — in BOTH V51 Battle Order branches (the evaluateShieldSelection shield path AND the Reserve/hand deploy path), when `occupiesBothTheaters(game, playerId)` is true, add `+200` (base 80 → 280) so Battle Order/Plan beats the auto-play shields and deploys within the turn's pacing budget. Extends the existing V51 gate in place (no new V-tag, per feedback_update_old_rule_not_new_version). Also extended both V51 title tests to match "battle plan" (the light twin), consistent with the V112 gate.
- Boundary (additive-domination, verified): +200 is GUARDED on `shieldScore > -50f` so it can NOT resurrect a V43 redundant-shield block (-100, opponent already has an equivalent Battle Order/Plan), a pacing-cap -50, or a not-played -100 — those all still win. Not-qualifying stays -9999 (the boost lives in the occupy-both else-branch only). 4th slot unchanged: the V105 +2000 still dominates (the +200 rides on top, e.g. 40+2000+200=2240, Battle Order was already the V105 pick). Occupy-ONLY gate per Steve — no opponent-occupation clause (rare over-deploy when opponent also occupies both = tax waived both ways, a slightly wasted shield, never a self-tax). Magnitude 280 beats auto-play (200) and sits under the top dark grabbers; exact turn-1 ordering is deck-dependent but it deploys by turn 1-2 within pacing.
- KNOWN GAP (documented, not fixed): the boost lives in the shield-selection + deploy routes. A MIXED K&D stacked pile (<50% shields) routes the play through evaluateUnknown/V112 (a pure -9999 block, no positive branch), where the boost does not fire. The primary K&D shield route (evaluateShieldSelection, the one the Verge game used) IS covered. Adding a V112 positive branch is a follow-up if a mixed-pile deck shows the gap live.
- Verified: compiles clean in-container (both bots). Jar byte-search + work-verifier pending. BEHAVIORAL self-play still owed (covers this + the base V51/V105 Verge fix): a Scarif dark game where Rando occupies both theaters early should log `V51 BATTLE ORDER EARLY-DEPLOY ... +200` and Battle Order reaching the table on turn 1-2 (K&D's 4x/game shield-play cap is a separate limiter to watch).
- Revert: remove the two `V51 BATTLE ORDER EARLY-DEPLOY ... +200` blocks (both bots, both paths) and the `|| ...contains("battle plan")` title extensions.

## 2026-07-06 — V29 BESPIN-FIRST updated in place: release the -500 gate when the objective itself forbids Executor or no capital-ship path exists (TDIGWATT bug B)
- MOD `server/.../ai/models/rando/evaluators/DeployEvaluator.java` (~1234, inside the V29 non-exempt branch) + `chosenone/evaluators/DeployEvaluator.java` (~1185): before applying the V29 -500 "deploy Executor first" penalty, run two release checks: (a) `ObjectiveAnalyzer.objectiveForbidsDeployingExecutor()`; (b) DeckOracle scan for any STARSHIP/CAPITAL DeckCard in HAND/RESERVE_DECK/FORCE_PILE/USED_PILE. Either releases the gate (LOG.info "V29 BESPIN-FIRST RELEASED", no penalty); otherwise the original -500 + LOG.warn fire verbatim (kept unchanged in the else branch).
- MOD `server/.../ai/models/rando/strategy/ObjectiveAnalyzer.java` + `chosenone/strategy/ObjectiveAnalyzer.java`: new field `objectiveGameText` (stored in analyze() ~126, cleared in reset() ~412) + new method `objectiveForbidsDeployingExecutor()` (rando ~1052 / chosenone ~1032): sentence-scan of the objective blueprint's game text for "executor" plus a deploy-forbid phrase ("may not deploy" / "cannot deploy" / "may not be deployed") in the same sentence. Universal — no card ids or name lists.
- Why: game 2026-07-02 02:09, Rando DARK on TDIGWATT (V) (Card226_012, deploys with I'm Sorry). Its game text — verified from `src/gemp-swccg-cards/.../set226/dark/Card226_012.java:45` — reads "For remainder of game, you may not deploy Admiral's Orders or [Death Star II] Executor." The deck can never occupy Bespin space, so the V29 gate never released and EVERY bare character deploy ate -500 all game; Rando flooded weak solos, held 1 CC site vs the 3 needed, never flipped.
- Boundary: variant character deploys return to natural V38/V136 magnitudes (~+50..+400 > Pass 5-8); locations still exit earlier at the +200 LOCATION bucket; exempt actions (ship/AMSD/Executor/Bespin) untouched — the release code sits inside the non-exempt branch. Classic TDIGWATT (Card109_012) keeps the gate whole: no "may not deploy" clause on either side (back-side "may not cancel"/"may not be modified" don't match) and Executor is an accessible capital in Reserve. Release adds NO positive score, so nothing new can dominate.
- Verified: statically only — V29 branch confirmed live (no `if (false`) in both bots; card text read from source; forbid-scan checked against classic front+back and variant front+back texts; single diff hunk per DeployEvaluator, 4 per ObjectiveAnalyzer. Compile + reload-ai deferred to orchestrator. PENDING live check: a TDIGWATT (V) game should show zero "V29 BESPIN-FIRST: BLOCKING" lines (INFO "RELEASED" lines if level permits) with characters deploying to CC sites; a classic TDIGWATT game should still show BLOCKING lines pre-Executor.
- Revert: in both DeployEvaluators delete the `bfGateReleased` block and un-nest the original -500 addReasoning/LOG.warn (kept verbatim in the else); in both ObjectiveAnalyzers delete the `objectiveGameText` field/store/reset lines and `objectiveForbidsDeployingExecutor()`.

## 2026-07-06 — V61c UPDATE (in place): battle-intent bypass — keep-3 destiny buffer only on turns a battle is plausible
- MOD `server/.../ai/models/rando/evaluators/DecisionContext.java` (~line 309) — ADD `isBattlePlausibleThisTurn()`: the ONE shared battle-intent predicate. Conservative v1 per Steve: battle is "plausible" if ANY location is CONTESTED (both sides' total power > 0 there), the same scan V61b uses in BattleEvaluator (~630). Null game/gameState/playerId/opponent or any exception → true (keep 3). Zero contested locations → false (deploy-and-end turn).
- MOD `server/.../ai/models/rando/evaluators/ForceActivationEvaluator.java` `calculateActivationAmount` (~line 193) — the V61c keep-3 cap (`min(maxAvailable, reserveDeck - 3)`) now applies ONLY when the predicate is true; otherwise activate `maxAvailable` in full. Old always-on line kept commented above. V67at end-game clamp still runs after the branch, unchanged, and still wins when more conservative.
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` V168 carve-out (~line 185) + V38.3 confirm carve-out (~line 1404) — both `reserve <= 3` conditions become `reserve <= 3 && isBattlePlausibleThisTurn()`. No-contested turns fall through to normal V168 ALWAYS ACTIVATE (+5000) and normal V38.3 bounce-back (No +9999). Old conditions kept commented. The second V38.3 arm (`evaluateActivateForce` ~5060, flat +500, no carve-out) untouched — dominated either way.
- Why: Steve (2026-07-01): "If Rando intends to battle that turn, he needs to save 3. If he intends to deploy and end turn without battling he can activate all force." The 2026-06-29 V61c protected the buffer EVERY turn, throttling activation on turns with no possible battle. Activate phase precedes Deploy/Battle, so intent is PREDICTED: contested location exists → battle plausible → keep 3 (old behavior); zero contested → activate all. Bias toward keeping 3 when unsure — a false "no battle" re-opens the no-destiny bug; a false "battle" only costs a little activation.
- Boundary: no new score magnitudes — the change only selects between the two pre-existing regimes (V61c buffer vs V168/V38.3/V57 full-activate). All three sites share the SAME predicate (the original V61c bug was these sites disagreeing), and board presence can't change during the Activate phase, so they cannot split within a turn. V61b (overpower exception) and V67at (end-game reserve-2) untouched. Known blind spot (accepted, v1): Rando can CREATE a contested site by deploying onto opponent presence after activating full; the BattleEvaluator V61 reserve guard (-800/-400/-200 at reserve 0/1/2) still discourages destiny-dependent battles on such turns.
- Verified: marker line `V61c BATTLE-INTENT: no contested location — activating full` logs at each site when the bypass actually changes the outcome (FAE: only when the cap would have clamped; V38.3: once, on the "No" option). Grep-confirmed zero `if (false` in the edited branches (live code) and zero V61c/DESTINY BUFFER logic in chosenone/ (nothing to mirror — chosenone never got V61c). Compile + live-jar byte-check pending with the orchestrator's build.
- Revert: in all three files, delete the `V61c UPDATED 2026-07-06` blocks and un-comment the `V61c pre-2026-07-06 (always-on buffer)` lines directly above each replacement; remove `isBattlePlausibleThisTurn()` from DecisionContext.

## 2026-07-06 — V169 + V47 (both in place): blocked-retreat double-penalty unstacked + Lando stay-lock gated on objective and survivability
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` (fields ~41-48, V169 branch ~128-186) + chosenone mirror — V169 UPDATED: ATE is now the SINGLE owner of the endangered-mover soft block, resized -400 → -250, with a per-turn retry budget (3 soft excusals per action, keyed by actionText, reset on turn change like blockedResponses) after which the V163 hard veto (-100000) resumes.
- MOD `server/.../ai/models/rando/evaluators/MoveEvaluator.java` (~164-213) + chosenone mirror — V169 UPDATED: the duplicate soft block (EvaluatedAction ctor -400 PLUS addReasoning -400 = -800, both add per EvaluatedAction.java:30/:44) commented out in place; the `continue` removed so endangered movers fall through to rankMoveFromLocation and their retreat bonuses actually attach; the -9999 hard block for non-endangered movers unchanged, wrapped in else.
- MOD same MoveEvaluators (rando ~378-445, chosenone ~367-434) — V47 UPDATED: the Lando -9999 stay-lock now requires (a) an active Bespin/Cloud City occupation objective (ObjectiveAnalyzer isAnalyzed + needsBespinSystemPresence; pre-flip isObjectiveRelevantLocation, post-flip isFlipBackProtectionLocation — the site must still serve the objective) AND (b) survivability (skip when powerDiff < RandoConfig.BATTLE_DANGER_THRESHOLD = -6, the same RETREAT boundary calculateThreatLevel uses; getTotalPowerAtLocation both sides). The generic 'platform' site fragment commented out in place (Title.java proof: Endor: Landing Platform (Docking Bay), Coruscant: Private Platform, Kashyyyk: Skyhook Platform all matched; every CC site title starts "Cloud City: " so the 'cloud city' fragment alone keeps full CC coverage incl. East Platform).
- Why: audit rows cross-brain-1 + move-8 (Rando_Overlap_Audit_2026-07-04.xlsx, both CONFIRMED-high, re-verified untouched at HEAD 2026-07-06). cross-brain-1: V169 was implemented twice for the same actionId; the merged score on the Asajj-class boundary was ~-1050 vs Pass +5, so the retreat retry the tag was written to enable (replay lk6xgsokjcwrwxuu, 6v27, beaten next turn) was mathematically impossible — both code comments claiming "-400 lets the retreat retry" were false. move-8: any *lando* at any site containing 'platform' froze at -9999 on any deck with no danger exit, overriding RETREAT +150 / V22.5 +160 / V53 +500; false-positive sites are real cards.
- Boundary (additive-domination, done before code): Asajj case AFTER = ATE(-250 + V35.4 +150) + MoveEval RETREAT +150 = +50 > Pass 8 → retreat retries (spy variant +150). No-safe-destination case: 3 excusals then hard veto → Pass wins, turn terminates (Keder guard preserved; worst case 3 extra cancel cycles). DANGEROUS-tier endangered (-80) and RISKY-tier (-600) still lose to Pass. Non-endangered blocked: -100000 + -19998 unchanged. V47: TDIGWATT Lando at a live CC site with diff >= -6 locks exactly as before; diff < -6 or non-CC deck or irrelevant site → lock stands down and the normal leave doctrines decide. Only penalties were reduced; nothing new dominates.
- Known limits: the -9999/-19998 ctor+addReasoning double-add on the HARD block paths (MoveEvaluator + audit-noted DeployEvaluator) is NOT fixed here — hard vetoes dominate at either magnitude; separate consolidation item. V47 now requires OUR objective to be a CC objective: a Lando sitting at CC sites in a non-objective deck no longer gets the lock (deliberate narrowing per the audit).
- Mirrored to chosenone (both rules exist there; line-for-line identical, chosenone package types).
- Verified: all touched branches live (no `if (false` tape in any of the 4 files); typed compile of all 4 files clean (0 errors) via in-container javac 21 against web.jar + sourcepath; full mvn build left to the orchestrator. Live-game grep handles: `V169: soft-block` / `V169: retry budget exhausted` / `V169 MoveEvaluator: endangered mover ... blocked-but-excused` / `V47 LANDO STAY skipped`.
- Revert: in both ATEs delete the retry-budget fields and if/else, un-comment the old -400 addReasoning line; in both MoveEvaluators un-comment the old softBlockedMove block (restoring its `continue`), unwrap the hard block from the else, and restore the V47 one-liner by un-commenting `|| locLower.contains("platform")` and deleting the two gate blocks plus the if/else around the -9999 addReasoning.

## 2026-07-06 — V67bc UPDATE + V191: DPS non-bucket epilogue (into-hand pulls pickable) + top-N candidate logging

- MOD src/.../ai/models/rando/evaluators/CombinedEvaluator.java — V67bc updated in place: EPILOGUE before the DPS-walk PASS return (+ NON_BUCKET_EPILOGUE_FLOOR=+50 constant); V191 top-5 candidate log at the decision site.
- MOD src/.../ai/models/chosenone/evaluators/CombinedEvaluator.java — exact mirror of both.
- MOD src/.../ai/models/rando/RandoCalAi.java — V191 breadcrumb on the fallback-heuristic branch (path + final pick; top-5 lives in HeuristicAiBase's private pick loop, not instrumented).
- MOD src/.../ai/models/chosenone/TheChosenOneAi.java — mirror of the breadcrumb.

Why: (audit deploy-sequencing-4) DeployPhaseScript deliberately excludes "take X into hand / into pile" Reserve pulls from every bucket (they don't put a card on table), but the V67bc walk could only ever return FROM a bucket — when all buckets were exhausted it returned PASS, so a well-scored into-hand pull (V116 +100 baseline and up) was unpickable forever. The epilogue picks the best NON-bucket, non-PASS-typed action when it clears +50; otherwise the original PASS runs. V191 logs one line per CombinedEvaluator decision ('V191 TOPN: <decisionType> phase=<phase> :: actionId=score x5', sorted on a COPY so live tie-breaking never moves) — the dominance-regression detector for future rule additions.

Boundary: epilogue is only reachable after every bucket's best is < -100, so it can only replace PASS, never a bucket winner (a +900 non-bucket pull still loses to a +420 STEP 3 deploy). Junk below +50 (e.g. keyword-only ~+45) still passes; -60 non-bucket still passes. PassEvaluator's Cancel-id pass (V27/V27.1 bonuses can stack past +50) is excluded by ActionType. V191 is instrumentation only, zero scoring changes.

Verified: no 'if (false' guard in either CombinedEvaluator (walk is live); rando/chosenone mirrors diff-identical except package/import; brace/paren balance clean; compile deferred to orchestrator.

Revert: delete the V67bc EPILOGUE block + NON_BUCKET_EPILOGUE_FLOOR constant and the V191 blocks in the four files (all-additive change, zero lines removed — plain deletion of the tagged blocks restores prior behavior exactly).

## 2026-07-06 — Rando audit fixes: V82/V60 pull-guard bypass, V25 static-battleground drain leak, V35.4 inverted spy detection (both bots)
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` + `chosenone/evaluators/ActionTextEvaluator.java`, three in-place V-tag updates (no new tags):
  (1) V82/V60 UPDATED (rando ~4325/~4372/~4611; chosenone ~4273/~4321/~4560): V82 SITE PULL +2500 moved INSIDE the `if (!hardBlocked)` region (old pre-guard placement commented out above the guards); V60 Guard 1 (reserve <= 2) magnitude -400 → -9999, matching the DeployEvaluator copy of the same guard.
  (2) V25 UPDATED (rando ~5318; chosenone ~5243): Simple Tricks battleground test switched from static printed icons (`hasIcon DARK_FORCE && LIGHT_FORCE`) to the engine's `modifiersQuerying.isBattleground(gameState, drainLoc, null)` — the same call pattern V140 used before its 2026-07-04 rework; static icons kept only as fallback when the game object is unavailable.
  (3) V35.4 UPDATED (rando ~3569; chosenone ~3517): opponent-spy detection fixed to owner == OPPONENT && isUndercover (old inverted test flagged OUR OWN spy); the +250/+150 move bonus is now scoped to actions whose MOVER (the action's cardId) is at the blocked location; an undercover mover never collects it; unresolvable mover falls back to the old any-location scan.
- Why: Rando_Overlap_Audit_2026-07-04 rows deploy-sequencing-1, control-drain-5, move-7 (all CONFIRMED, high). (1) A reserve=2 live-target pull scored +2200..+3700 against Guard 1's -400, so Rando fired the search and revealed his last 2 reserve cards — exactly what the guard exists to prevent. (2) A dual-icon site dynamically made non-battleground still passed the static check, so Rando drained into a guaranteed Simple Tricks cancel — reopening the incident V25 was written to fix. (3) Our V170 drain-block spy paid +250 to every move on the table, including its own move-away: V53 -300 + 250 + RETREAT +150 = +100 > Pass → spy abandoned its post.
- Boundary (additive-domination math done pre-code): (1) reserve=2 pull now V116 +100 [+V100 +1500] -9999 = -9899/-8399 < Pass ~5-8 (was +2200/+3700 > Pass); reserve >= 3 unblocked pulls score IDENTICALLY to before (V82 unchanged inside the open path); max ungated positive stack (+1600) is dominated by -9999. (2) Dynamic non-BG + Simple Tricks: -9999 + return (was drain fires and gets cancelled); dynamic-BG site no longer falsely blocked; no Simple Tricks on table → no change. (3) Spy move-away: -300 + RETREAT 150 = -150 < Pass (was +100 > Pass); opp-spy-at-our-site true positive upgraded +150 → +250 and scoped to movers at that site.
- Mirrored to chosenone 1:1 (regions were byte-identical apart from the DeckOracle package). NOT yet compiled or live-verified — orchestrator compiles after the batch. Verify in game logs: `V60 RESERVE RISK ... too risky (-9999)`, `V25 SIMPLE TRICKS: BLOCKING`, `V35.4: UNDERCOVER SPY at ...` firing only on genuine opponent spies.
- Revert: (1) restore -400 in Guard 1 (old line commented in place) and un-comment the old V82 block above the guards / delete the moved copy inside `!hardBlocked`; (2) delete the `context.getGame()` branch — the old static check survives as the else-if fallback; (3) restore the commented-out inverted ownership branch and delete the `v354Mover` resolution/scoping block. All three old versions are commented out at the edit sites in both files.

## 2026-07-06 — T2 helper COMMIT-1: V58 phantom-reserve in-play gate + engine maintain-cost basis (new MaintenanceFacts) across the force-reserve family (both bots)
- ADD `server/.../ai/models/common/strategy/MaintenanceFacts.java` — shared static `maintainCost(blueprint)`: 0 without Icon.MAINTENANCE; else parses game text with case-insensitive regex `end of your turn:.*?use (\d+)` (verified against ALL 8 maintenance cards in the codebase: Lando Scoundrel 1, Han/Chewie/Falcon 3, Chewie Enraged 2, Boba Fett BH 2, Stormtrooper Garrison 1, Thok And Thug 2, Blizzard 4 1, Ap'lek "Use 1 or [Skull]" 1); fallback on no-match = deployCost (orchestrator ruling H3, conservative); memoized by blueprint. Ground truth: the engine charges the card's OWN maintain cost (AbstractNonLocationPlaysToTable :1833/:1894 per-card overrides), NOT deploy cost.
- MOD `rando/evaluators/DrawEvaluator.java` `calculateForceToReserve` (~:483) + chosenone mirror — V58/V78/V79 UPDATED in place: `Zone.isInPlay()` gate at loop top covering ALL FIVE detections (DTF / First Strike / IAO / maintenance / Verge) — `getAllPermanentCards` returns every card ever created including RESERVE_DECK (GameState.java:2800-2802), so an opponent DTF still in his deck phantom-taxed our draws from turn 1 (audit force-economy-5). Same gate on the nested V79 Death Star scan (an undeployed Death Star can't be moved). V67w UPDATED in place: maintenance reserve `+1/card` → `MaintenanceFacts.maintainCost` (+1 was UNDER for the Falcon's 3F upkeep — the original V67w complaint, right number this time). Lying javadoc fixed.
- MOD `rando/evaluators/PassEvaluator.java` V27 (~:242) + chosenone mirror — basis deployCost → maintainCost; weights 25/50 untouched. (V27.1 DTF block already had the in-play gate; untouched.)
- MOD `rando/strategy/DeployPhasePlanner.java` V22.3 (~:225) + chosenone mirror — basis deployCost → maintainCost; the audit-refuted "Maintenance cost in SWCCG = card's deploy cost" comment corrected.
- MOD `rando/evaluators/DeployEvaluator.java` + chosenone mirror, FOUR basis sites: V59 self-maint (~:2101, `maintenanceCost = cost` → maintainCost), V24.5 existing-maint sum (~:2188), V29.13 self-awareness compare (~:2312, `< cost` → `< maintainCost`, log value follows), V38 solo-deploy maintObligation (both the on-table sum ~:2417 and the this-card `+= cost` arm). All weights (-2000/-1500/-400/-50/-500/-30) untouched — only the obligation NUMBER changes.
- MOD `rando/evaluators/CardSelectionEvaluator.java` `v173WaveProjection` (~:5703) + chosenone mirror (~:5659) — V173/V174 UPDATED in place per ruling H2: (a) tableMaint scan gets the same `Zone.isInPlay()` gate (reserve-deck maintenance cards phantom-taxed the wave budget), (b) tableMaint + the deploying card's own upkeep switch to maintainCost, (c) maintenance-buddy DOUBLE-SPEND fixed: spend was `2x deploy cost`, now `deploy cost + maintainCost`.
- Why: two audit rows. force-economy-5: V58 reserved for threats that were still IN THE DECK — e.g. never-played DTF made Rando hold-reserve (-15) instead of drawing surplus (+80..+400) every turn. force-economy-1: the deploy-cost basis over-reserved 2-5x — Lando Scoundrel (deploy 5, maintain 1) was effectively undeployable below pile ~10 (V59 HARD -2000 on a phantom 5F obligation) and V22.3 shaved 5F/turn off the deploy budget for a 1F upkeep.
- Boundary (spec rows 1-8 re-verified against the edited code): row 1 phantom DTF → reserve 3→2, draw-vs-pass flips to Draw; row 2 reserve-deck Lando → surplus +320→+400; row 3 V27 Pass bonus +50→0 (pile 4 vs maintTotal 5→1); row 4 V59 HARD -2000 → V64 TIGHT -400 (+1600 swing, Lando deployable at pile 8); row 5 V29.13 -50→0; row 6 Falcon draw pressure +320→+160 (3F upkeep finally protected); row 7 planner effectiveForce 3→7; row 8 v173 budget 3→4 (wave affords one more ~cost-1 buddy). Every flip is the intended bug fix; all rule WEIGHTS unchanged.
- SCOPE CARVE-OUT: the spec's 8th basis site, MoveEvaluator V27 (~:1577-1579), is deliberately NOT swapped — orchestrator ruling: another crew owns MoveEvaluator/ActionTextEvaluator this wave. Until their pass lands, MoveEvaluator's maintenance copy still over-reserves on deploy-cost basis (soft -60/-80 penalties only). Follow-up owner: that crew or the COMMIT-2 cache consolidation.
- Verified: all edited blocks live (no `if (false` tape); rando/chosenone mirrors symmetric (call-count parity checked per file); regex hand-verified against all 8 card game texts incl. Ap'lek; boundary rows above recomputed from the new code; compile deferred to the orchestrator per wave protocol.
- Revert: delete `MaintenanceFacts.java`; in the 10 touched files un-comment the `superseded T2 COMMIT-1 2026-07-06` lines directly above each replacement and delete the replacement lines + the two `Zone.isInPlay()` gate blocks per DrawEvaluator (loop top + nested Death Star scan) and one per CardSelectionEvaluator (tableMaint loop).

## 2026-07-06 — T2 helper COMMIT-2 (move #1, score-neutral cache): ONE shared per-decision force-reserve computation (new ForceReserveService) for the five maintenance/DTF consumers (both bots)
- ADD `server/.../ai/models/common/strategy/ForceReserveService.java` — immutable `Facts {dtfActive, firstStrikeActive, iaoActive, grabberUnused, maintenanceObligation, maintenanceCardCount, undercoverSpyCount, vergeNeedsDeathStarMove}` + static `compute(game, gs, playerId)`: ONE `Zone.isInPlay()`-gated pass over getAllPermanentCards, title/icon detection copied from the five consumer sites (post-COMMIT-1 text, MaintenanceFacts basis). Shared by both bots (MaintenanceFacts/ShieldFacts precedent).
- MOD rando + chosenone `evaluators/DecisionContext.java` — lazy `getForceReserveFacts()`: one compute per decide() call. SOAK INSTRUMENT: every 20th decision (static counter), every cache READ re-runs compute() and logs `MAINT CACHE MISMATCH` at WARN on any field divergence (cached vs freshly-computed = what an inline per-caller scan would have produced at that moment). Remove the soak branch after 2 clean full games.
- MOD the five consumers, both bots — each inline scan commented out in place (`T2 MOVE #1 COMMIT-2 (2026-07-06)` breadcrumbs, feedback_comment_out_old_rules) and replaced by a Facts read; ALL weights untouched:
  - DrawEvaluator `calculateForceToReserve` — V58/V67w/V78/V79 UPDATED in place: the five-detection scan (+ nested Death Star scan) → Facts (dtf / firstStrike / iao / maintenanceObligation / vergeNeedsDeathStarMove); the V67z transit reserve and contested-count loop stay local (not permanent-card scans).
  - PassEvaluator — V27.1 DTF scan → `facts.dtfActive` (weights 20/40/60); V27 maintenance scan → `facts.maintenanceObligation` (25/50).
  - MoveEvaluator — V29 DTF+grabber scan → Facts (weights -100/-150/-60); V27 maintenance scan → `facts.maintenanceObligation` (-80). NOTE: this was COMMIT-1's declared carve-out still on the deploy-cost basis — the cache closes it (V27 basis UPDATED in place, audit force-economy-1; the ONLY value change in this commit).
  - DeployEvaluator — V24.5 existing-maint sum → Facts (-50/-50); V29.13 DTF+grabber scans → Facts (-30); V38 on-table maintObligation + DTF interruptReserve + V53 spy count → Facts. This-card maintain adds and V59's plan-aware pendingDeployCost stay LOCAL (per-candidate, not shared facts). Weights -2000/-1500/-400/-50/-500/-30 untouched.
  - DeployPhasePlanner — V22.3 maintenanceReserve → static `ForceReserveService.compute(...).maintenanceObligation` at plan creation (no DecisionContext there; already once-per-turn). The old loop's `if (allCards != null)` wrong-variable guard retires with the block.
- Documented unifications (ForceReserveService header; the only places the old sites disagreed with EACH OTHER): (1) DTF/FS/IAO ownership = exact-opponent match (DrawEvaluator alone treated any non-friendly owner as opponent — identical in 2-player play); (2) grabberUnused = ANY unused grabber, the MoveEvaluator V29 semantic (DeployEvaluator V29.13 broke on the FIRST grabber card regardless of state — differs only with 2+ mixed-state grabbers, which no current deck fields); (3) spy count Zone-gated (isUndercover is only ever true in play — no-op). NOT moved: DeployEvaluator's V48/V79 combined Vader+Verge scan (~:226-283) — Vader-position is not a shared fact; left local per the designer spec's edit list.
- Why: T2 plan move #1 (Rando_Overlap_Audit force-economy family) — five copies of the same scan drifted independently (the MoveEvaluator basis miss was the live example); one cached computation per decision ends the drift class and cuts the per-action rescans.
- Boundary: score-neutral by construction — every consumer reads values identical to its post-bug-fix inline scan. The only behavior deltas: MoveEvaluator V27's completed basis swap (soft -60/-80 penalties now fire on the engine maintain number, COMMIT-1 boundary rows 3/6 semantics) and the three documented unifications (all no-op on real boards). The soak instrument verifies neutrality live.
- Verified: all edited blocks live pre-edit (no `if (false` tape); replaced locals orphan-checked (no live refs to commented-out variables — MoveEvaluator's later `opponentId` uses are separate scopes, pre-existing or the original wouldn't have compiled); rando/chosenone source blocks diff-verified IDENTICAL pre-edit and edited symmetrically; brace balance clean. Compile deferred to the orchestrator per wave protocol.
- Revert: delete `ForceReserveService.java` + the `getForceReserveFacts()` block in both DecisionContexts; in the 10 consumer files un-comment each `T2 MOVE #1 COMMIT-2` commented block and delete the Facts-read lines (+ their breadcrumb comments) directly above it.

## 2026-07-06 — T2 helper MOVE-3 remainder: V105/V106/V107 occupation predicates unified into new ShieldFacts (both bots) + fourthSlotPick helper
- ADD `server/.../ai/models/common/strategy/ShieldFacts.java` — shared statics for the SHIELDS family: `occupiesBothTheaters(game, pid)` (VERBATIM body of the private helper added to rando CardSelectionEvaluator in ee0a1b435 — engine `canSpot(occupies + battleground_site/system)`, fails closed), `occupiesAnyBattleground(game, pid)` (engine `canSpot(occupies + battleground)`, presence-based, fails closed), `shieldsOnTable(gs, pid)` (verbatim V117 on-table count scan: owner + DEFENSIVE_SHIELD + `Zone.isInPlay()`). Homed in common/strategy per the T2 spec placement decision (MovePredicates/MaintenanceFacts precedent) so both bots share one copy.
- MOD `rando/strategy/ShieldStrategy.java` + chosenone mirror — V105 UPDATED in place: `prefers4thSlot` trigger A's own `getTotalPowerAtLocation` power>0 theater scan (the audit shields-response-2 Status@HEAD residue) replaced by `ShieldFacts.occupiesBothTheaters` (old scan commented out in place per feedback_comment_out_old_rules). V106 UPDATED in place: the `weOccupyAnyBg` trigger-B input switches from the power-scan derivation to `ShieldFacts.occupiesAnyBattleground`. `oppBgCount`/`myBgCount` (trigger B/C) deliberately STAY power-based with a divergence comment (T4 candidate). HOLD log placeholders `sysBg/siteBg` → `bothTheaters/anyBg` (variables no longer exist). NEW `fourthSlotPick(gs, game, playerId, Predicate<String> preferredOnMenu)` returning `{preferred, pursue}` — consolidates the pick+menu dance V117/V105/V124 each hand-roll (`pursue = preferred != null && (predicate == null || predicate.test(preferred))`; null predicate = V124's current `preferred != null` semantics).
- Why: audit shields-response-2 — after ee0a1b435 unified V51/V112 (and the V51 gate itself) onto the engine occupies-predicate, `prefers4thSlot` trigger A still used power>0. The two disagree on zero-power occupation: an unpiloted ship at a battleground system occupies it (Power.java:49-51 gives it power 0) so Battle Order's OWN condition (Card13_054 OccupiesCondition) is LIVE while trigger A said no — 4th slot dead (V117 -9999 / V105 -5000 / V124 -3000) though the card would fire. Boundary rows 13/15 in resources/T4_Boundary_Tables_2026-07-06.md.
- Boundary: the trigger-A/B predicate swap is the ONE intended behavior change (boundary rows 13 + 15: unpiloted-ship occupation now opens the slot when the card itself is live; when the preferred card is not on the menu, HOLD lines fire exactly as before — row 14, zero swing on shield scores). ALL weights stay per-caller and untouched (-9999/+2000 V117, -5000/+2000 V105, -3000 V124). `fourthSlotPick` is score-neutral by construction and currently UNCALLED — CSE (V117/V105) and ATE (V124) adopt it in a later wave (both files untouched this wave per orchestrator instruction; CSE's private `occupiesBothTheaters` copy also delegates/retires then).
- Mirrored to chosenone 1:1 (twins were byte-identical apart from package/RandoLogger import; regenerated wholesale and diff-verified identical modulo those 2 lines).
- Verified: statically only — both prefers4thSlot blocks confirmed live (no `if (false` tape) in both bots pre-edit; no live references to the commented-out scan variables remain; brace/paren balance clean; ShieldFacts bodies diff-checked verbatim against CSE :8746-8762 and the V117 scan. Compile deferred to the orchestrator per wave protocol. PENDING live check: board with a BG system held only by an unpiloted ship + BG site with characters should log `V105 4TH SLOT (A): Battle Order — we occupy system+site battlegrounds` where the pre-fix log showed `sysBg=false`.
- Revert: in both ShieldStrategy files un-comment the SUPERSEDED 2026-07-06 power-scan block + the old `weOccupyAnyBg` line, delete the two ShieldFacts call lines + the `occupyBothTheaters` local + the `fourthSlotPick`/`FourthSlotPick` additions, restore the old HOLD log line; delete `ShieldFacts.java`.

## 2026-07-06 — V192 (T4.2 pull-engine merge): ONE pull scorer — vetoes first, single tier table, activate base +5500 (both bots)
- NEW HUB TAG V192 (precedent V136/V153/V158/V159, orchestrator ruling P3): every reserve-deck pull is now scored by ONE scorer in `ActionTextEvaluator`'s PULL-ENGINE branch. Absorbed tags (old code commented out in place, `//` per line, tag+date breadcrumbs — revert path): V60-pull baseline (+150/+250), V82 site-pull grant (+2500), V95 dead-interrupt (standalone additive → hardBlock in the veto chain), V97 pull-before-activate (+1500), V100 location-pull-before-chars (+1500), V116 reserve floor (+100), V67l/V67ai location tiers (2000/1800/1600/1500 → 1500/1400/1300/1200), V67m/V67am weapon +600 / device +400 grants (values kept, single-emit), generic V29.7 PULL FIRST +250. V60 keeps ONLY its Hidden Path transit arm (+20000 R4, T4.1 band); the V60-tagged reveal-risk guards keep their historical log tags inside the veto chain for replay-grep continuity.
- MOD `rando/evaluators/ActionTextEvaluator.java` + chosenone mirror — PULL-ENGINE branch: (a) trigger widened to the union of the absorbed V97/V95/V116 triggers (`[upload]` + generic take-into-hand; Take-into-hand dispatch above now EXCLUDES reserve-deck takes so they fall through — single owner); (b) ALL vetoes run first and short-circuit via `hardBlocked` (Guard1 reserve<=2 -9999, Guard2 fail-stop, Guard3 named-miss, V66/V123, V67h, V67ac, V95 folded as hardBlock, V131 not-in-deck, V67ar/V67ao/V149 weapon-holder gates); (c) ONE positive emit: BASE (+150 deploy-grade; +5500 PULL_BASE_ACTIVATE under the old V97 scope — Phase==ACTIVATE, static source EFFECT/EPIC_EVENT/INTERRUPT/OBJECTIVE, K&D + Anger, Fear, Aggression excluded — chained ABOVE V168's +5000) + TYPE TIER (exactly one: LOCATION 1500/1400/1300/1200 by source category via the shared isLocationPull predicate = V67l 34-keyword list ∪ V82 source-text regex ∪ V100's "planet"/"sector"; WEAPON 600; DEVICE 400) + CONTEXT (+50 [download]; +25 chars-or-vehicles-in-hand during DEPLOY on location pulls) — clamped 1750 deploy / 7100 activate; (d) V131 Tier-2 downgrade RE-WIRED from additive -2000 to STRUCTURAL: suppresses ALL positives (incl. V67ak, now gated on !hardBlocked && !downgrade) and emits -200; (e) P1 STAND-DOWN (orchestrator ruling): at reserve<=3 AND `DecisionContext.isBattlePlausibleThisTurn()` (the SAME shared V61c predicate, no second copy) the activate base stands down to deploy grade so pulls stop eroding the destiny buffer V61c is protecting. Log handle: `V192 PULL SCORER (ACTIVATE|DEPLOY-GRADE): base X + tier Y [...] + ctx Z = N [absorbs ...]`.
- MOD `rando/evaluators/DeployEvaluator.java` + chosenone mirror — double-count side killed (each commented out in place): V60 +100 baseline (~:873), V67ai Tier 1-3 DE copy (ds-2: the ATE copy where V131 gates it is the single owner; V67i detection KEPT LIVE as predicate for the weapon-gate routing), V67am +600 weapon grant (~:3668). The V162/V67ai-Tier4-HAND hand-location block (+1900 total) is UNTOUCHED — it is the anchor the pull scorer must stay below (V179 lesson). ALL DE vetoes stay (duplicate -9999s harmless).
- CHOSENONE MIRROR NOTE: same merge, one structural absence — chosenone has no V61c destiny buffer and no `isBattlePlausibleThisTurn()`, so the P1 stand-down is ABSENT there by design (noted in-file; add if V61c is ever mirrored).
- Why: audit rows deploy-sequencing-1 / ds-2 / ds-3 / ds-7 — the pull pile double- and triple-counted across two evaluators (+6250 known, ~+8000 worst case), so (1) activate-window pulls LOST to V168 and fired after activation with a shrunken pool (feedback_pull_before_activate violated), (2) downloads outranked held locations by 6050 (the A Good Friend replay class), (3) V131's downgrade and V95's dead-interrupt save were drowned by the pile they were supposed to kill.
- Boundary (T4_Boundary_Tables_2026-07-06.md §T4.2, re-verified against final code): row 1a pull 5500 > V168 5000 (pull first, was 2000 vs 5000 — FLIP as intended); row 1b objective download 7050 > 5000 (preserved); row 2 hand-location 1950 > pull 1625-1775 > chars ~550 (FLIP: order restored, was pull +8000 first); row 3 downgraded pull -150 < Pass +6 in the activate window (FLIP; deploy-window residual: DE's uniform V38.4 urgency (+50..+500 on every deploy action) can lift a downgraded pull back above Pass when hand >= 9 and NOTHING else is deployable — bounded corner, flagged for follow-up); row 4 reserve<=2 -9999 flat, zero resurrection surface; row 5 V95 dead interrupt -2000 flat (was net +100 and FIRED); row 6 blaster pull single-counted +800 (was +1600).
- Corridor audits (orchestrator ruling P2): ACTIVATE window — live positives >= 2000 are exactly V168 +5000 (activate action), V192 pull 5500-7050, V60 transit +20000 (R4 mandatory, correctly dominates); V79/V29.15/V61c-confirm hits are MULTIPLE_CHOICE/CONFIRM surfaces outside the ACTION_CHOICE corridor. DEPLOY window — generic char/vehicle stacks ~250-550+urgency stay below the pull tier; playbook corners (V52 TDIGWATT T1 +850..+1500, V52b Hidden Path +800, V30 combos +1000, V67ak +800) can stack to ~1650-2350 and edge out UNKNOWN-source (+1200-tier) pulls; V67ak +800 survives as a separate line on pulls (max pull 2525 deploy / 7850 activate in the rare loc+key-char multi-clause corner). Pre-existing relationships, not introduced by the merge; flagged for the playtest queue.
- Verified: all edited blocks live (no `if (false` tape); brace/paren balance clean on all 4 files; grep-zero LIVE emitters of the absorbed lines (V116 floor, V97/V100 +1500, V82 +2500, V67ai DE tiers, V67am DE +600, generic V29.7 +250) in both bots — the TDIGWATT admiral/general V29.7 +250/+300 playbook nudge deliberately untouched; exactly ONE positive emit path per pull action. Compile deferred to the orchestrator per wave protocol.
- Revert: in both ATE files un-comment the V116/V95/V97/V100/V82/V67ai/V67am/V29.7 blocks tagged `2026-07-06` and delete the V192 SINGLE EMIT + P1 stand-down + trigger widening (restore the commented old trigger/dispatch); in both DE files un-comment the V60 baseline / V67ai tier switch / V67am grant. All old code is commented in place at each edit site.

## 2026-07-06 — T4.1: MOVE clobber ladder — rank bands R1-R4 + veto matrix in MoveEvaluator (~20 tags re-ranked IN PLACE, no new V-tag; spec + rulings L1-L4 in resources/T4_Boundary_Tables_2026-07-06.md §T4.1)
- MOD `server/.../ai/models/rando/evaluators/MoveEvaluator.java` + chosenone mirror — NEW ladder machinery: per-action rank (R4 MANDATORY TRANSIT +20000 / R3 SURVIVAL +12000 / R2 DOCTRINE +6000 / R1 0, max-of-claims) + typed veto flags, applied by `ladderFinalize()` immediately before `actions.add`. Fines clamped to ±2800 ("LADDER CLAMP"); a NEGATIVE clamp hit demotes one band R2→R1 / R3→R2, R4 exempt ("LADDER DEMOTE", ruling L1). R2 claims need strength: own fine >= +200 OR drain-delta >= 2 ("LADDER: R2 claim ... REJECTED — weak" otherwise, ruling L2). First-use band assertion recomputes R2-floor (6000-2800-550=+2650) vs R1-ceiling (1670+250=+1920) from live constants and `logger.error`s on inversion, no crash (ruling L4; "LADDER BANDS OK" line). All 8 early returns in `rankMoveFromLocation` removed (every block always runs). IN-PLACE conversions, old lines commented `// OLD:` at each site: V37.1 CRUSH/FAVORABLE -9999+return → -1500 R1 weight; V85 -2000+return → -800 R1 weight + drain via shared `MovePredicates.drainAt`; V53b Safehouse→Corridor `setScore(9999)` → R4 claim + 800 fine (+20800, order-independent); V53b Mapuzo-exit +800 → R4 claim; V60 Corridor-landspeed `setScore(-9999)` → hard veto; V135 -2000 → hard veto (strengthened); V47 / V49 -9999 → hard veto (gates unchanged); V160 cancel-loop -9999 → -100000 flat (beats everything incl. R4, ruling L3); V38.3 wrong-direction -9999 → DEFERRED veto suppressed ONLY by the V53b transit claim identities ("V38.3 wrong-direction suppressed (R4 mandatory transit)", NOT rank==R4, ruling L3 — move-3f unstall), Castle arm → hard veto; V137 winnability → shared `MovePredicates.canWinAt` with mover-side forfeit sum, VETO applied ONLY to battle-seeking R2 claims (hunt/contest/attack — never R4/R3/non-battle-R2, ruling L3), old -800/-1500 RETAINED as live R1 weights so non-battle paths keep the deterrent; V29.13 drain both endpoints via `drainAt`. Rank claims: R3 = threat RETREAT, V59 DOOMED, V91; R2 battle-seeking = V35/V29.12 hunt, V34/V36 contest, ATTACK + V29.7 (NEWLY gated on `isAdjacentSites` to the best target — "no R2 claim (target not adjacent)" keeps the Rey class at R1); R2 non-battle = V29.13 toward-group + GOOD DRAIN (delta>=2), V31, V73 shuttle, V111, V53 spy follow/reposition, SPREAD, V22.2/V22.5 (attempt kept, fails L2 at today's +160 max). Default -50 moved into the finalizer (rank==R1 + rankMoveFromLocation ran).
- MOD (NEW FILE) `server/.../ai/models/common/strategy/MovePredicates.java` — THE shared graded winnability predicate + drain metric, homed in common/ so CDSE and BOTH bots reach it (orchestrator re-home of the spec's RandoMovePredicates): `canWinAt` = clean win (power >= oppPower && ability >= 4, CDSE:253-254 parity) OR V181 tolerance (0 < gap <= 3 && opp drain at site >= 2 && ability >= 4 && forfeit parity ourForfeit <= theirForfeit*1.25); `drainAt`/`drainDelta` on engine `getForceDrainAmount` (move-6 one-metric consolidation). Fail-open (never veto blind). T2 HANDSHAKE (record in the reorg baton §11): T2's reserved WinnabilityCalculator graded overload must DELEGATE here — do not fork the predicate (header comment in the class).
- MOD `server/.../ai/models/common/strategy/CharacterDeploySiteEvaluator.java` — V181 block now calls the shared `canWinAt` (core gs/mq overload; behavior-identical port: at that point projectedPower < oppPower always, so the clean arm cannot fire; tolerance arm byte-equivalent incl. the one-sided 1.25x forfeit cap; deploy bonus +min(300, drain*100) kept exactly; drain-0 fail-open guard falls through like the old chain; old inline chain commented for revert). Kills the move-4a deploy/move same-tag drift permanently (V136/V137 parity pair).
- MOD `server/.../ai/models/rando/evaluators/ActionTextEvaluator.java` + chosenone mirror — ONLY two alignment edits (nothing else in ATE touched): V60 Hidden Path transit +9999 → +20000 (shares the R4 band with ME's transit arms, beats any ME R3 stack <= ~15350 by construction, move-3e; V60 keeps ONLY this transit arm per ruling P3); V134 Odin Nesloor -9999 → -100000 (its "transport" text co-sums with ME-scored actions, must stay veto-class across the new bands).
- Why: the MOVE audit's clobber class. V85's -2000 + bare return killed the V73 Cantina↔Mos Eisley shuttle (move-1) and Hunt-Down Vader's hunt (move-2); V37.1's -9999 + return stalled Hidden Path forever at a FAVORABLE Mining Village (move-3a); the setScore(±9999) hacks were one-appended-rule away from silent re-decides (move-3c/3d); V38.3 stalled the Mapuzo exit to an empty site (move-3f); V137 refused the exact contested move V181 committed as a deploy (move-4a contradiction).
- Boundary: committed BEFORE implementation in resources/T4_Boundary_Tables_2026-07-06.md §T4.1 (move-1 +5560, move-2 ~+6160, move-3a +19300, 3b +20000, 3c +20800, 3d/-135/-47 vetoes -100000, 3e +20000, 3f suppressed→+20800, 4a +6650, 4b veto; V85-protect ~-720, V37.1-protect ~-1550; band margin +730). Per-row re-verification against the FINAL code: all rows PASS (see session verification; move-2/3c/V85-protect totals differ by the retained V29.7 fine (+~265) / pre-existing +10 "Land (ground deployment)" quirk that setScore used to wipe — outcomes identical).
- Verified: static this session (no local JDK — orchestrator compiles + jar byte-check per BUILD_AND_DEPLOY): zero live setScore/-9999 in either MoveEvaluator; scanner brace-balance on all 6 files; mirror wiring parity rando vs chosenone (3×R4, 4×R3, 16×R2, 5×hard-veto, 1× each wrongDir/canWin/finalize); no `if (false` around any edited block (liveness rule).
- Revert: whole ladder = revert the 6 files (MovePredicates.java is new — delete). Per-tag revert: every conversion keeps its OLD lines commented in place, tagged `UPDATED 2026-07-06 T4.1`; grep the tag to walk all sites.

## 2026-07-06 — T2 helper COMMIT-1: V58 phantom-reserve in-play gate + engine maintain-cost basis (new MaintenanceFacts) across the force-reserve family (both bots)
- ADD `server/.../ai/models/common/strategy/MaintenanceFacts.java` — shared static `maintainCost(blueprint)`: 0 without Icon.MAINTENANCE; else parses game text with case-insensitive regex `end of your turn:.*?use (\d+)` (verified against ALL 8 maintenance cards in the codebase: Lando Scoundrel 1, Han/Chewie/Falcon 3, Chewie Enraged 2, Boba Fett BH 2, Stormtrooper Garrison 1, Thok And Thug 2, Blizzard 4 1, Ap'lek "Use 1 or [Skull]" 1); fallback on no-match = deployCost (orchestrator ruling H3, conservative); memoized by blueprint. Ground truth: the engine charges the card's OWN maintain cost (AbstractNonLocationPlaysToTable :1833/:1894 per-card overrides), NOT deploy cost.
- MOD `rando/evaluators/DrawEvaluator.java` `calculateForceToReserve` (~:483) + chosenone mirror — V58/V78/V79 UPDATED in place: `Zone.isInPlay()` gate at loop top covering ALL FIVE detections (DTF / First Strike / IAO / maintenance / Verge) — `getAllPermanentCards` returns every card ever created including RESERVE_DECK (GameState.java:2800-2802), so an opponent DTF still in his deck phantom-taxed our draws from turn 1 (audit force-economy-5). Same gate on the nested V79 Death Star scan (an undeployed Death Star can't be moved). V67w UPDATED in place: maintenance reserve `+1/card` → `MaintenanceFacts.maintainCost` (+1 was UNDER for the Falcon's 3F upkeep). Lying javadoc fixed.
- MOD `rando/evaluators/PassEvaluator.java` V27 (~:242) + chosenone mirror — basis deployCost → maintainCost; weights 25/50 untouched.
- MOD `rando/strategy/DeployPhasePlanner.java` V22.3 (~:225) + chosenone mirror — basis swap; the audit-refuted "Maintenance cost in SWCCG = card's deploy cost" comment corrected.
- MOD `rando/evaluators/DeployEvaluator.java` + chosenone mirror, FOUR basis sites: V59 self-maint (~:2101), V24.5 sum (~:2188), V29.13 self-compare (~:2312, `< cost` → `< maintainCost`), V38 maintObligation both arms (~:2417). All weights (-2000/-1500/-400/-50/-500/-30) untouched — only the obligation NUMBER changes.
- MOD `rando/evaluators/CardSelectionEvaluator.java` `v173WaveProjection` (~:5703) + chosenone mirror (~:5659) — V173/V174 UPDATED in place per ruling H2: (a) tableMaint scan gets the same in-play gate, (b) tableMaint + the deploying card's own upkeep switch to maintainCost, (c) maintenance-buddy DOUBLE-SPEND fixed: spend was `2x deploy cost`, now `deploy cost + maintainCost`.
- Why: audit force-economy-5 (V58 reserved for threats still IN THE DECK — never-played DTF flipped draw-vs-pass to Pass every turn) + force-economy-1 (deploy-cost basis over-reserved 2-5x — Lando Scoundrel effectively undeployable below pile ~10 on a phantom 5F obligation).
- Boundary (spec rows 1-8 re-verified against the edited code): row 1 reserve 3→2, draw-vs-pass flips to Draw; row 2 surplus +320→+400; row 3 V27 +50→0; row 4 V59 HARD -2000 → V64 TIGHT -400; row 5 V29.13 -50→0; row 6 Falcon +320→+160; row 7 planner effectiveForce 3→7; row 8 v173 budget 3→4. Every flip is the intended bug fix; all rule WEIGHTS unchanged.
- SCOPE CARVE-OUT: MoveEvaluator V27 basis site (~:1577) deliberately NOT swapped — another crew owns MoveEvaluator/ActionTextEvaluator this wave; its deploy-cost basis (soft -60/-80) stays until their pass or COMMIT-2.
- Verified: all edited blocks live (no `if (false` tape); mirrors symmetric; regex hand-verified against all 8 card texts; typed javac compile (web.jar classpath + sourcepath) of all 11 files: 0 errors; full mvn left to orchestrator.
- Revert: delete `MaintenanceFacts.java`; in the 10 touched files un-comment the `superseded T2 COMMIT-1 2026-07-06` lines above each replacement and delete the replacement lines + the two `Zone.isInPlay()` gate blocks per DrawEvaluator and one per CardSelectionEvaluator.

## 2026-07-06 — V192 (T4.2 pull-engine merge): ONE pull scorer — vetoes first, single tier table, activate base +5500 (both bots)
- NEW HUB TAG V192 (precedent V136/V153/V158/V159, orchestrator ruling P3): every reserve-deck pull is now scored by ONE scorer in ActionTextEvaluator's PULL-ENGINE branch. Absorbed tags (old code commented out in place, // per line, tag+date breadcrumbs — revert path): V60-pull baseline (+150/+250), V82 site-pull grant (+2500), V95 dead-interrupt (standalone additive → hardBlock in the veto chain), V97 pull-before-activate (+1500), V100 location-pull-before-chars (+1500), V116 reserve floor (+100), V67l/V67ai location tiers (2000/1800/1600/1500 → 1500/1400/1300/1200), V67m/V67am weapon +600 / device +400 grants (values kept, single-emit), generic V29.7 PULL FIRST +250. V60 keeps ONLY its Hidden Path transit arm (+20000 R4, T4.1 band); the V60-tagged reveal-risk guards keep their historical log tags inside the veto chain for replay-grep continuity.
- MOD rando/evaluators/ActionTextEvaluator.java + chosenone mirror — PULL-ENGINE branch: (a) trigger widened to the union of the absorbed V97/V95/V116 triggers ([upload] + generic take-into-hand; Take-into-hand dispatch above now EXCLUDES reserve-deck takes so they fall through — single owner); (b) ALL vetoes run first and short-circuit via hardBlocked (Guard1 reserve<=2 -9999, Guard2 fail-stop, Guard3 named-miss, V66/V123, V67h, V67ac, V95 folded as hardBlock, V131 not-in-deck, V67ar/V67ao/V149 weapon-holder gates); (c) ONE positive emit: BASE (+150 deploy-grade; +5500 PULL_BASE_ACTIVATE under the old V97 scope — Phase==ACTIVATE, static source EFFECT/EPIC_EVENT/INTERRUPT/OBJECTIVE, K&D + Anger, Fear, Aggression excluded — chained ABOVE V168's +5000) + TYPE TIER (exactly one: LOCATION 1500/1400/1300/1200 by source category via the shared isLocationPull predicate = V67l 34-keyword list ∪ V82 source-text regex ∪ V100's "planet"/"sector"; WEAPON 600; DEVICE 400) + CONTEXT (+50 [download]; +25 chars-or-vehicles-in-hand during DEPLOY on location pulls) — clamped 1750 deploy / 7100 activate; (d) V131 Tier-2 downgrade RE-WIRED from additive -2000 to STRUCTURAL: suppresses ALL positives (incl. V67ak, now gated on !hardBlocked && !downgrade) and emits -200; (e) P1 STAND-DOWN (orchestrator ruling): at reserve<=3 AND DecisionContext.isBattlePlausibleThisTurn() (the SAME shared V61c predicate, no second copy) the activate base stands down to deploy grade so pulls stop eroding the destiny buffer V61c is protecting. Log handle: V192 PULL SCORER (ACTIVATE|DEPLOY-GRADE): base X + tier Y [...] + ctx Z = N [absorbs ...].
- MOD rando/evaluators/DeployEvaluator.java + chosenone mirror — double-count side killed (each commented out in place): V60 +100 baseline (~:873), V67ai Tier 1-3 DE copy (ds-2: the ATE copy where V131 gates it is the single owner; V67i detection KEPT LIVE as predicate for the weapon-gate routing), V67am +600 weapon grant (~:3668). The V162/V67ai-Tier4-HAND hand-location block (+1900 total) is UNTOUCHED — it is the anchor the pull scorer must stay below (V179 lesson). ALL DE vetoes stay (duplicate -9999s harmless).
- CHOSENONE MIRROR NOTE: same merge, one structural absence — chosenone has no V61c destiny buffer and no isBattlePlausibleThisTurn(), so the P1 stand-down is ABSENT there by design (noted in-file; add if V61c is ever mirrored).
- Why: audit rows deploy-sequencing-1 / ds-2 / ds-3 / ds-7 — the pull pile double- and triple-counted across two evaluators (+6250 known, ~+8000 worst case), so (1) activate-window pulls LOST to V168 and fired after activation with a shrunken pool (feedback_pull_before_activate violated), (2) downloads outranked held locations by 6050 (the A Good Friend replay class), (3) V131's downgrade and V95's dead-interrupt save were drowned by the pile they were supposed to kill.
- Boundary (T4_Boundary_Tables_2026-07-06.md §T4.2, re-verified against final code): row 1a pull 5500 > V168 5000 (pull first, was 2000 vs 5000 — FLIP as intended); row 1b objective download 7050 > 5000 (preserved); row 2 hand-location 1950 > pull 1625-1775 > chars ~550 (FLIP: order restored, was pull +8000 first); row 3 downgraded pull -150 < Pass +6 in the activate window (FLIP; deploy-window residual: DE's uniform V38.4 urgency (+50..+500 on every deploy action) can lift a downgraded pull back above Pass when hand >= 9 and NOTHING else is deployable — bounded corner, flagged for follow-up); row 4 reserve<=2 -9999 flat, zero resurrection surface; row 5 V95 dead interrupt -2000 flat (was net +100 and FIRED); row 6 blaster pull single-counted +800 (was +1600).
- Corridor audits (orchestrator ruling P2): ACTIVATE window — live positives >= 2000 are exactly V168 +5000 (activate action), V192 pull 5500-7050, V60 transit +20000 (R4 mandatory, correctly dominates); V79/V29.15/V61c-confirm hits are MULTIPLE_CHOICE/CONFIRM surfaces outside the ACTION_CHOICE corridor. DEPLOY window — generic char/vehicle stacks ~250-550+urgency stay below the pull tier; playbook corners (V52 TDIGWATT T1 +850..+1500, V52b Hidden Path +800, V30 combos +1000, V67ak +800) can stack to ~1650-2350 and edge out UNKNOWN-source (+1200-tier) pulls; V67ak +800 survives as a separate line on pulls (max pull 2525 deploy / 7850 activate in the rare loc+key-char multi-clause corner). Pre-existing relationships, not introduced by the merge; flagged for the playtest queue.
- Verified: all edited blocks live (no `if (false` tape); brace/paren balance clean on all 4 files; grep-zero LIVE emitters of the absorbed lines in both bots — the TDIGWATT admiral/general V29.7 +250/+300 playbook nudge deliberately untouched; exactly ONE positive emit path per pull action. Compile deferred to the orchestrator per wave protocol.
- Revert: in both ATE files un-comment the V116/V95/V97/V100/V82/V67ai/V67am/V29.7 blocks tagged 2026-07-06 and delete the V192 SINGLE EMIT + P1 stand-down + trigger widening (restore the commented old trigger/dispatch); in both DE files un-comment the V60 baseline / V67ai tier switch / V67am grant. All old code is commented in place at each edit site.
(Matching V192 block also inserted in resources/k2-resources/originals/02-rando-history/AI_VERSION_HISTORY.md before the V189 block.)

## 2026-07-06 — T2 helper MOVE-3 remainder: V105/V106/V107 occupation predicates unified into new ShieldFacts (both bots) + fourthSlotPick helper
- MOD: ADD server/.../ai/models/common/strategy/ShieldFacts.java — shared statics: occupiesBothTheaters(game, pid) (VERBATIM body of the private helper ee0a1b435 added to rando CardSelectionEvaluator :8746-8762 — engine canSpot(occupies + battleground_site/system), fails closed), occupiesAnyBattleground(game, pid) (canSpot(occupies + battleground), presence-based, fails closed), shieldsOnTable(gs, pid) (verbatim V117 count scan: owner + DEFENSIVE_SHIELD + Zone.isInPlay()). MOD rando/strategy/ShieldStrategy.java + chosenone mirror — V105 UPDATED in place: prefers4thSlot trigger A's own getTotalPowerAtLocation power>0 theater scan replaced by ShieldFacts.occupiesBothTheaters (old scan commented out in place); V106 UPDATED in place: weOccupyAnyBg trigger-B input → ShieldFacts.occupiesAnyBattleground; oppBgCount/myBgCount stay power-based with a divergence comment (T4 candidate); HOLD log sysBg/siteBg → bothTheaters/anyBg; NEW fourthSlotPick(gs, game, playerId, Predicate<String>) → {preferred, pursue} consolidating the pick+menu dance (null predicate = V124's current preferred!=null semantics; adopters wire in a later wave).
- Why: audit shields-response-2 — after ee0a1b435 unified V51/V112 onto the engine occupies-predicate, prefers4thSlot trigger A still used power>0. The two disagree on zero-power occupation: an unpiloted ship at a battleground system OCCUPIES it but has power 0 (Power.java:49-51), so Battle Order's OWN condition (Card13_054 OccupiesCondition) could be LIVE while trigger A said no — 4th slot dead (V117 -9999 / V105 -5000 / V124 -3000) though the card would fire. Boundary rows 13/15 in resources/T4_Boundary_Tables_2026-07-06.md.
- Boundary: the trigger-A/B predicate swap is the ONE intended behavior change (the audit's deliberate low-severity unification); row 14: preferred card not on the menu → HOLD lines fire exactly as before, zero swing. ALL weights stay per-caller, untouched: -9999/+2000 V117, -5000/+2000 V105, -3000 V124. fourthSlotPick is score-neutral by construction and currently UNCALLED (CSE/ATE off-limits this wave).
- Verified: statically — both prefers4thSlot blocks live pre-edit (no if(false) tape) in both bots; no live references to the commented-out scan variables remain; brace/paren balance clean in all three files; ShieldFacts bodies diff-checked verbatim against CSE :8746-8762 and the V117 scan; chosenone mirror diff-verified identical modulo package/import. Compile deferred to orchestrator. Live handle: unpiloted-ship-only BG system + BG site with characters → 'V105 4TH SLOT (A): Battle Order — we occupy system+site battlegrounds' where the pre-fix log showed sysBg=false.
- Revert: in both ShieldStrategy files un-comment the 'SUPERSEDED 2026-07-06 (T2 MOVE #3)' power-scan block + the old weOccupyAnyBg line, delete the two ShieldFacts call lines + the occupyBothTheaters local + the fourthSlotPick/FourthSlotPick additions, restore the old HOLD log line; delete ShieldFacts.java.

## 2026-07-06 — T2 helper COMMIT-2 (move #1, score-neutral cache): ONE shared per-decision force-reserve computation (new ForceReserveService) for the five maintenance/DTF consumers (both bots)
- MOD/ADD: ADD common/strategy/ForceReserveService.java — immutable Facts {dtfActive, firstStrikeActive, iaoActive, grabberUnused, maintenanceObligation, maintenanceCardCount, undercoverSpyCount, vergeNeedsDeathStarMove} + static compute(game, gs, playerId): ONE Zone.isInPlay()-gated pass over getAllPermanentCards, detection copied verbatim from the five consumer sites (post-COMMIT-1 text, MaintenanceFacts basis). MOD rando + chosenone DecisionContext — lazy getForceReserveFacts() (one compute per decide) with the soak instrument: every 20th decision, every cache READ recomputes fresh and logs `MAINT CACHE MISMATCH` at WARN on divergence. MOD the five consumers in both bots, inline scans commented out in place, weights untouched: DrawEvaluator V58/V67w/V78/V79 → Facts; PassEvaluator V27.1 (20/40/60) + V27 (25/50) → Facts; MoveEvaluator V29 (-100/-150/-60) + V27 (-80) → Facts (closes COMMIT-1's MoveEvaluator deploy-cost-basis carve-out — the only value change); DeployEvaluator V24.5 (-50/-50) + V29.13 (-30) + V38/V53 → Facts (V59 pendingDeployCost + this-card maintain adds stay local); DeployPhasePlanner V22.3 → static compute at plan creation (retires the `if (allCards != null)` wrong-variable guard with the block).
- Why: T2 plan move #1 — five independent copies of the same scan drifted (the MoveEvaluator basis miss was the live example); one cached computation per decision ends the drift class and cuts per-action rescans. All V-tags updated in place, no new tag.
- Boundary: score-neutral by construction — every consumer reads values identical to its post-bug-fix inline scan. Only deltas: MoveEvaluator V27's completed basis swap (soft -60/-80 now fire on the engine maintain number) and 3 documented unifications where the old copies disagreed with each other (exact-opponent ownership; ANY-unused-grabber = MoveEvaluator semantic; Zone-gated spy count) — all no-op on real boards. NOT moved: DeployEvaluator's V48/V79 Vader+Verge combined scan (Vader-position isn't a shared fact).
- Verified: all edited blocks live pre-edit (no `if (false` tape); rando/chosenone blocks diff-verified IDENTICAL pre-edit and edited symmetrically; no orphaned refs to commented-out locals; full javac compile of all 13 files with symbol resolution against the built engine jars in the app container (syntax AND symbols clean — mvn package still deferred to orchestrator). Live handle: grep `MAINT CACHE MISMATCH` across 2 full games = zero hits ⇒ cache certified neutral, then comment out the soak branch.
- Revert: delete ForceReserveService.java + the getForceReserveFacts() blocks in both DecisionContexts; in the 10 consumer files un-comment each `T2 MOVE #1 COMMIT-2` commented block and delete the Facts-read lines (+ breadcrumb comments) directly above it.

## 2026-07-07 — V43 UPDATED in place: starting-interrupt pick reads what the interrupt DOES (effect-deployers beat duds)
- TAKE 2 (same day): the first scan required "from reserve deck" — a phrase NEITHER card contains (fabricated phrasing, not read from source). Corrected to scan the STARTING clause for deploy+effect, verified against Card9_139 ("Deploy up to three Effects..." → +300, total 350) and Card200_125 ("draw up to 12 cards" → 0, total 50). Offline scoring check: 350 vs 50.
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` (~7048, inside the V43 STARTING INTERRUPT block) + chosenone mirror (~7003): the generic-tie branch now scans the interrupt's game text — deploys Effects/defensive shields from Reserve = +300/+250/+200 (scaled by "up to three/two"), deploys anything from Reserve = +100, else the old flat 0. Epic Event +1500 top tier unchanged.
- Why: game 2a999777 (2026-07-07, Rando DARK Verge vs Steve). V43 scored Prepared Defenses (deploy up to 3 Effects) and Surface Defense (V) both at flat 50; the engine's shuffle order put Surface Defense first, Rando picked it, zero starting Effects deployed. The morning game (same deck) had the reverse order and looked fine — the pick was a coin flip all along. NOT a regression: pre/post logs show identical 50/50 scoring; the flaw predates today's work.
- Boundary: 50+300=350 vs 50 — Prepared Defenses class wins deterministically. Epic Event 1550 still dominates everything. No other rule scores this ARBITRARY_CARDS surface (turn-0, CSE-only), so no additive-domination risk.
- Verified: compiles clean both bots (real mvn exit checked). PENDING deploy + a setup where the deck carries both interrupt classes: expect `V43 STARTING INTERRUPT: ... deploys Effects from Reserve — PREFER (+300)`.
- Revert: restore the single flat-0 else branch in both files (old lines commented in place).

## 2026-07-07 — V79 family UPDATED in place + V79b FLIP-BACK GUARD: Verge post-flip — the Death Star STAYS in Scarif orbit (both bots)
- ROOT CAUSE (Game9f3c46b00681, Rando DS Verge conceded T5, 39-9): `getAtLocation()` is ALWAYS null for the Death Star mobile-system LOCATION card (every DS move logged 'Card not at a location'), so every "is the DS at Scarif" check in the V79 family was permanently false. Post-flip (T2 05:49, Krennic at Scarif: Command Center + 'Orbit Scarif') the V79 +500 default-move bonus kept 'Move using hyperspeed' the top move action forever, and V79b's closest-to-7 parsec answer FROM ORBIT is the deep-space EXIT — the engine excludes the currently-orbited system from re-orbit at the chosen parsec (MoveMobileSystemUsingHyperspeedAction:82, `Filters.not(Filters.isOrbitedBy(card))`). Net: the DS toggled out of/into Scarif orbit turns 3-5 (out on odd turns via the deep-space branch :83-88, back in on even turns via auto-orbit :91-96), plus V35.4 +150 misfires and a perpetual 1-Force reserve that suppressed a real Mara Jade With Lightsaber deploy at 06:02. (Card-text NOTE: this objective's printed flip-back is leader-based — 'Flip this card if you do not have a leader at a Scarif battleground site', Card216_011_BACK — so the T3/T5 exits did NOT unflip it; the guard is V22.2 protection-class regardless: the flipped side's total-battle-destiny +1/+2 umbrella covers 'Death Star, system it orbits, and sites related to either', and any orbit-based flip-back objective would lose outright.)
- MOD `rando/evaluators/MoveEvaluator.java` + chosenone mirror (V79 block ~:496): at-Scarif detection → `getSystemOrbited()` (the engine's own orbit primitive; the flip condition itself is `Filters.isOrbiting(Title.Scarif)`, Card216_011:122 — getSystemOrbited holds the orbited system's TITLE, set from `newOrbit.getTitle()` in MoveMobileSystemUsingHyperspeedEffect:104); old getAtLocation lines commented in place. NEW `V79b FLIP-BACK GUARD` in the same block: objective FLIPPED (`context.getObjectiveAnalyzer().isFlipped()`) + DS orbiting Scarif → T4.1 ladder HARD VETO (`ladderVetoHard`, -100000 at the finalizer) on the hyperspeed move — post-flip the DS never leaves orbit. Pre-flip steering (4->6->7->orbit) untouched; a post-flip DS knocked into deep space still enters the unchanged steering branch and re-orbits. Section-header NOTE updated (the V79 arm is no longer fully inert).
- MOD `rando/RandoCalAi.java` V79b (~:742): belt-and-suspenders — when flipped + orbiting Scarif (state should be unreachable given the MoveEvaluator veto), prefer an orbit/Scarif option, else answer the DS's CURRENT parsec (least-bad: stays at Scarif's parsec; from deep space at that parsec the lone-orbitable auto-re-orbit fires, :91-96); logs `V79b FLIP-BACK GUARD`. Pre-flip and post-flip deep-space recovery keep the original closest-to-7 steering. CHOSENONE NOTE: TheChosenOneAi has NO V79b MULTIPLE_CHOICE handler (its parsec surface is the ATE arm below) — nothing to mirror there.
- MOD `rando/evaluators/ActionTextEvaluator.java` + chosenone mirror: V79/V103 parsec arm (~:1317) same getSystemOrbited fix — with atScarif now TRUE while parked the steering branch is skipped and the V103 PARSEC FALLBACK's closest-to-7 pick IS the stay pick (Scarif = parsec 7), so no extra !flipped gate is needed and post-flip deep-space recovery steering stays live. V35.4 (~:3688) now SKIPS mobile-system LOCATION movers — 'move away to drain elsewhere' is character/starship doctrine; a location neither drains nor unblocks a drain, and the +150 was cementing the toggle (alone it beats Pass: +40 base +150 vs ~+28).
- MOD `rando/evaluators/DeployEvaluator.java` + chosenone mirror (~:251), `common/strategy/ForceReserveService.compute` (~:216, the `vergeNeedsDeathStarMove` fact feeding BOTH DrawEvaluators' V79 draw reserve), and `common/strategy/CharacterDeploySiteEvaluator.isV156FlipNotReady` (~:852, the shared V156 flip-ready predicate): same getSystemOrbited swap. Kills the perpetual 'V79 VERGE MOVE RESERVE' (deploy + draw phases) once the DS is parked — post-veto that reserve would have been PERMANENTLY wasted — and lets V156 see a parked/flipped Verge as flip-ready. Six copies of the predicate found (grep-swept ai/ for Death-Star getAtLocation checks), all six now on the one engine primitive.
- Boundary math: post-flip+orbiting move = LADDER VETO -100000 vs Pass +28 — nothing outscores the veto class (max positive stack R4 20000 + fine clamp 2800 + ATE cross +250 = 23050). Pre-flip+orbiting move = -10 'Card not at a location' [-100 V29 reserve when armed] + 0 V79 (now gated) + 0 V35.4 (now excluded) ≈ -110 vs Pass +28 → Pass wins, DS holds orbit awaiting Krennic/Tarkin (old behavior: +540 → exited orbit and UN-SATISFIED the flip condition pre-flip). Pre-flip not-at-Scarif and post-flip deep-space: +500/steering unchanged → 4->6->7->orbit / re-orbit. No new rank claims, so zero R2/R3/R4 ladder interactions; V79's +1500 orbit-Scarif fine stays R1 (R1_FINE_CEILING note still accurate). V137/V160/V38.3 veto matrix untouched (the guard rides the existing hard-veto class).
- Verified: brace balance vs HEAD identical on all 7 touched files; all edited blocks live (no `if (false` tape); the six dsAtScarif copies grep-confirmed as the ONLY Death-Star-position checks under ai/. Compile + live Verge game PENDING — expect post-flip: `V79b FLIP-BACK GUARD ... VETOED` each move phase, NO `V79 VERGE MOVE RESERVE` lines while orbiting, and NO parsec prompt at all.
- Revert: in the 7 files un-comment the getAtLocation blocks tagged `V79 UPDATED 2026-07-07` and delete the adjacent getSystemOrbited lines; delete the two `V79b FLIP-BACK GUARD` blocks (MoveEvaluator, both bots) and the stay-parked block in RandoCalAi V79b; restore `if (!v354MoverIsUndercover)` in both ATEs (V35.4 lines commented in place).

## 2026-07-07 — V156 UPDATED in place (both arms): weak-solo deploy hold no longer expires after turn 2 + NEW JOIN-GROUP move arm (both bots)
- Incident (game 2026-07-07 05:45, Rando DARK Verge vs asdf; diagnosis vs logs/gemp-swccg.log lines 17261-30606): turn 2 V156 correctly held General Tagge / Death Trooper (-600 at every empty battleground) and both joined Vader at Citadel Tower. Turn 3 the SAME deploy class sailed through — the `currentTurn <= 2` gate was the single condition that let it: Baron Soontir Fel (ability 3, power 2) deployed SOLO to Scarif: Beach at +1065 (V136 §A handed the uncontested BG +500; only counterweights left were V38 -150 and V113 -300, and V113 fired uniformly at every candidate so it cancelled out). Move phase: the ladder DID claim the fix (V31 R2 DOCTRINE +6000, V32 agreeing) but the destination surface -9999'd the only join target — V41 WRONG DIRECTION's "empty" counts only OPPONENTS, so consolidating onto Vader's own 3-stack was "wrong direction" (dest -10151.5) — cancel loop, V160 veto, Fel rotted at Beach and was battled + forfeited 3 minutes later. Audit rows deploy-siting-1/-2 (Rando_Overlap_Audit_2026-07-04.xlsx, both CONFIRMED high) predicted exactly this: deploy-side V156 expires after turn 2 while the move side refuses to fix the solo state it creates.
- MOD `server/.../ai/models/common/strategy/CharacterDeploySiteEvaluator.java` (V156 gate ~:563, shared file → both bots) — DEPLOY arm: gate now `(currentTurn <= 2 || (ability < 4 && v156AnyBuddyAvailable))` — the ability<4 band holds (-600) on ALL turns at uncontested battleground sites when a buddy plan exists (another character on table or affordable in hand, the EXISTING v156AnyBuddyAvailable predicate); with no buddy anywhere the lone body still deploys (it is all we have). The ability>=4 class keeps the original turn<=2 gate and ALL exemptions (ability>=6 solo, armed ability-5, objective flip-site carve-back incl. v156FlipNotReady) byte-identical. Old gate commented in place. ALSO: the inline Verge flip-not-ready scan extracted VERBATIM to the new public static `isV156FlipNotReady(gs, playerId)` (old inline commented in place) so the move arm reuses the SAME predicate — no fork.
- MOD `server/.../ai/models/rando/evaluators/MoveEvaluator.java` + chosenone mirror (inside the V32 SOLO ESCAPE branch) — MOVE arm, action surface: NEW R2 DOCTRINE ladder claim `V156 JOIN-GROUP` (fine +250, passes the L2 strength gate; battleSeeking=false so the V137 canWinAt veto can never kill it per ruling L3) when the mover is a weak (ability<4) solo CHARACTER at a site with opponent power 0 and an ADJACENT site holds friendly characters (largest adjacent stack logged; same isAdjacentSites reachability gate the ATTACK claim uses). Exempt: undercover spies (V170 parked spies sit), opponent presence at the site (retreat/battle problem, not a join), solo doing READY objective work at a flip-relevant site (shared isV156FlipNotReady carve — same as deploy side).
- MOD `server/.../ai/models/rando/evaluators/CardSelectionEvaluator.java` + chosenone mirror (`evaluateMoveDestination`) — MOVE arm, destination surface (the second blocker; without this the R2 claim moves and the destination cancels — Fel's exact loop): NEW `V156 JOIN-GROUP MODE` computed once per decision from the decision-text blueprint hint (V169 retreat-mode pattern; same weak-solo predicate as the action surface). In join mode: (a) destinations with friendly characters get `V156 JOIN-GROUP DEST` +250 base +50 per extra body, cap +400 (largest-stack preference); (b) V41 WRONG DIRECTION is skipped for friendly-character destinations (new exemption branch beside the existing V169-retreat and V67z exemptions — a consolidate onto OUR OWN stack is by definition not wrong direction). V41 still fires in join mode for genuinely EMPTY destinations.
- Boundary math (additive-domination discipline, replay numbers): DEPLOY — Fel→Beach §A +500 → -600 flips V136 CS 800 → -300, site total 1065 → -35, loses to Citadel Tower 615 → he joins Vader's stack, byte-identical to the turn-2 Tagge decision (905 → Tower); turn<=2 behavior unchanged (gate short-circuits on the first arm); ability>=4 on turn 3+ unchanged; no-buddy-anywhere unchanged (+500); READY flip-sites unchanged (carve-back kept). MOVE action — fines V85 -800 + V32 +50 + V156 +250 + V22.2 -120 ≈ -620, R2 base +6000 → ≈ +5380 > Pass 6; R2 6000 < R3 12000 < R4 20000 so survival/transit still outrank; canWinAt veto N/A (non-battle claim). MOVE destination — Tower was -10151.5 (V41 -9999 + V67g -250 + others +97.5); new: +97.5 - 250 + 350 (3-stack bonus) = +197.5, above the -100 BAD_ACTION floor → the join EXECUTES instead of cancelling; V67g's leave-the-better-drain fine stays live and still wins against a +250 single-buddy join at a drain-0 destination when the mover drains 2 (250-250-97.5-margin cases sit near the floor — deliberate: drain-2 solos with only a 1-man buddy stack adjacent are a judgment call, big stacks pull harder). V169 retreat mode is mutually exclusive by construction (retreat needs opponent EXCESS at the mover's site, join needs opponent power 0 there).
- Why not fold V38/V113 too: diagnosis DESIGN scoped this to V156's two arms; V38/V113 magnitudes untouched (audit deploy-siting-1 unification of all four detectors stays queued).
- Verified: typed javac compile of all 5 files in the app container (web.jar + server classes classpath, sourcepath resolution): 0 errors — NO mvn run per house rules; brace/paren balance clean; ladder claim sits between ladderResetForAction (:456) and ladderFinalize (:2149) like V31's; chosenone mirrors diff-equivalent (package-qualified ObjectiveAnalyzer only difference). PENDING: rebuild + live game — expect `V156 SOLO HOLD ... turn 3+` on weak-solo deploys and `V156 JOIN-GROUP` / `V156 JOIN-GROUP EXEMPT` when a stranded weak solo consolidates.
- Revert: CharacterDeploySiteEvaluator — restore the commented old gate line + old inline flip scan (delete isV156FlipNotReady + the v156WeakBandAllTurns line); MoveEvaluator (both bots) — delete the `V156 JOIN-GROUP` block inside the V32 branch; CardSelectionEvaluator (both bots) — delete the JOIN-GROUP MODE block, the v156DestFriendlyChars count, the JOIN-GROUP DEST bonus, and the `else if (v156JoinMode ...)` V41 exemption branch. All new code is tagged `V156 JOIN-GROUP (2026-07-07)` / `V156 UPDATED 2026-07-07` — grep the tag to walk all sites.

## 2026-07-07 — V156 STACK-MATH refit: JOIN-GROUP targets ability-total, not headcount (solo doctrine)
- NEW shared predicate in `common/strategy/MovePredicates.java`: `siteAbilityTotal()`, `isDefensibleStack()` (site total friendly ability >= 4, the rulebook battle-destiny threshold), and `bestJoinDestination()` (nearest adjacent site that BECOMES destiny-capable when the mover joins; falls back to strongest adjacent group). ONE predicate, three consumers — no fork.
- MOD `rando/evaluators/MoveEvaluator.java` + chosenone mirror (V156 JOIN-GROUP R2 claim): destination now chosen by `bestJoinDestination` (ability-total) instead of the largest friendly COUNT. A weak solo walks to the site that reaches ability 4, not merely the most bodies.
- MOD `rando/evaluators/CardSelectionEvaluator.java` + chosenone mirror (V156 JOIN-GROUP DEST): destination bonus now ability-total-based — base +250, ability-total lean up to +100, +150 kicker when the join makes the stack destiny-capable (dest total + mover ability >= 4), cap +450. Stores the mover's ability for the kicker.
- Why (Steve): the interrupted draft fixed the Fel-at-Beach loss but keyed JOIN on friendly COUNT; Steve's doctrine is site TOTAL ability >= 4 (3 ability-1 droids are NOT defensible, 1 ability-6 already is). This delivers the doctrine. Deploy-side hold + V41 wrong-direction override + V79b Verge flip-back guard + ForceReserveService Verge fix from the draft (commit c1d5ced8c) are unchanged.
- Boundary: Fel case preserved — Citadel Tower (Vader 6 + Tagge 3 + Trooper 2 = ability 11, already defensible) scores the +450 cap, well above the ~-152 non-join PASS. R2 ladder base (+6000) unchanged; the join still can't outrank R3 survival / R4 transit.
- Verified: compiles clean both bots (MVN_EXIT=0). PENDING: deploy + a live game with a weak solo (watch `V156 JOIN-GROUP: ... join X (stack reaches ability N)`).
- Revert: restore the count-based loops in both MoveEvaluators + CardSelectionEvaluators; delete the 3 new MovePredicates methods (old code was replaced, see git c1d5ced8c for the count version).

## 2026-07-07 — V177/V82.1 parser fix: objective pull-targets anchored on the pull VERB (Endor Operations + recurring class)
- MOD `rando/strategy/DeckOracle.java` + chosenone mirror, `parseSourceCardPullTargets` (~1163): the "from Reserve Deck" capture regex changed from `([^.;]+?)\s+from\s+reserve\s+deck` (grabs the WHOLE clause back to the previous period) to `[^.;]*\b(take|deploy|download|upload|reveal|retrieve|use|search for|add|put|choose)\b\s+([^.;]*?)\s+from\s+reserve\s+deck` — capture only the object AFTER the last pull-verb before "from Reserve Deck". Also added a quantifier strip (`^(one|two|three|four|up to N)`) to both normalization loops.
- Why (Steve, live game): Rando on Endor Operations (Card8_167) never fired its "may take one Ominous Rumors or Establish Secret Base into hand from Reserve Deck" pull. The old regex captured "While this side up, once during each of your control phases, may take one Ominous Rumors or Establish Secret Base into hand" and comma-split it into garbage targets [while this side up, phases, one ominous rumors, establish secret base]; "while this side up" passed the stopword filter, V177 couldn't find it in Reserve, declared DEAD and hard-blocked (-2000) EVERY turn. This is the recurring "Rando won't pull with his objective" class — any objective whose pull clause has leading timing text (While this side up / once during your X phase) was vulnerable.
- Boundary (regression-tested BEFORE deploy, python harness on the exact regex): Endor Operations OLD [while this side up, phases, ominous rumors, establish secret base] → NEW [ominous rumors, establish secret base]. Preserved unchanged: Invasion "deploy a Naboo site from Reserve Deck" → [naboo site] (the greedy [^.;]* anchors on the LAST "deploy", fixing the original V82.1 concern), "take one capital starship or Effect" → [capital starship, effect], "deploy a battleground site" → [battleground site].
- Verified: compiles clean both bots (MVN_EXIT=0). PENDING: a live Endor Operations game — expect NO "V177 DEAD SEARCH blocked ... source 'Endor Operations'", and Rando taking Ominous Rumors / Establish Secret Base into hand during its control phase.
- Revert: restore the `([^.;]+?)\s+from\s+reserve\s+deck` pattern and remove the quantifier strip in both bots' DeckOracle.

## 2026-07-07 — V193: Endor Operations flip gate — steer ONE body to Endor: Bunker so Establish Secret Base (V) can deploy (both bots)
- ROOT CAUSE (replay qgdridfo166f27r3, Rando DARK Endor Operations vs asdf; Steve mistakes 2/5/6): Endor: Bunker sat EMPTY all game (us:0 them:0) while Rando piled every body onto Endor: Landing Platform (docking bay), so the cost-0 Establish Secret Base (V) (Card207_025) in hand — "Deploy on Bunker if you control that site" — was NEVER a legal deploy and the objective never flipped. Two ObjectiveAnalyzer parse bugs blinded Rando: (1) parseLocationReferences captured only garbage fragments ("deploy endor", "place out of play if an endor") so isObjectiveRelevantLocation() was FALSE for EVERY Endor site → no Endor site ever got the CharacterDeploySiteEvaluator +100 BG / +200 objective bonus; (2) the on-table regex grabbed the whole conjunction "ominous rumors and establish secret base are both" as ONE required card, so neither flip-card was recognized. There was no playbook entry telling Rando that CONTROLLING Endor: Bunker is the flip gate. (Sibling to the same-day V177/V82.1 pull-parser fix above — that one let the objective PULL the flip cards into hand; this one gets a body onto Bunker so the pulled Establish Secret Base can actually deploy. Card version verified against the real blueprint src/.../set207/dark/Card207_025.java — the gate is "control Bunker", NOT biker-scout / AT-ST logic, which belongs to a different, non-V card this deck does not run.)
- MOD `rando/strategy/ObjectiveAnalyzer.java` + chosenone mirror, `parseFlipCondition` (title-keyed block beside V160/V186): gated on objectiveTitle contains "endor operations" — (a) `addLocationFragment("endor")` so all Endor sites become objective-relevant; (b) V21 required-cards parser fix in place (per feedback_update_old_rule_not_new_version): clear the junk "...are both" entry and add the two real flip-cards "ominous rumors" + "establish secret base"; (c) NEW field/getter `getFlipCriticalControlSite()` returns "endor: bunker" (reset + parseGameText clears added). SCOPED to this objective; the global parseLocationReferences/ON_TABLE regex is untouched (too risky).
- MOD `rando/evaluators/DeployEvaluator.java` + chosenone mirror (V193 BUNKER CONTROL, nested in the V136 character-deploy siting block ~1845): +400 on the candidate site when ALL of — objective analyzer isAnalyzed() && getFlipCriticalControlSite() equalsIgnoreCase this site's title && Rando does NOT already control it (`GameConditions.controls`) && Establish Secret Base is in hand OR reserve (`DeckOracle.isCardInHand/isCardInReserve`). Self-limiting one-shot: once one body lands on the empty site Rando controls it → the not-controlled guard closes → the bonus never stacks per-body, and the rest of the pile reverts to normal siting.
- Boundary math (from the diagnosed game): Landing Platform first body ≈ 660 (empty-bay +80, V136 CS 350→600, drain +30, high-ability +50, reinforce-solo +150, buddy +40, battleground +80, non-obj -40). Endor: Bunker first body WITH Part-1 objective relevance but WITHOUT the bonus ≈ 430-470 (BG bonuses + obj +200, no drain/empty-bay/reinforce). +400 → Bunker ≈ 830-870 > 660 so the FIRST body picks Bunker (beats the pile by ~170-240); after control the guard closes and Bunker drops back below Landing Platform, so every LATER body sites normally (drain economy preserved). Additive-domination: outside Endor Operations flipCriticalControlSite is null → +400 never fires (zero cross-deck effect); within it, the intended first-body flip is the fix, not a silent regression, and it self-terminates. Part 1's +200 Endor relevance uses the SAME existing objective-bonus magnitude every other objective gets (V160/V186/ISB) — no new magnitude there.
- Verified: brace/paren balance vs HEAD unchanged on all 4 files; V193 mirrored rando↔chosenone (only the DeckOracle package differs); no `if (false` tape on the edited V136 / parseFlipCondition blocks; new symbols resolve (GameConditions.controls line 3946, DeckOracle.isCardInHand/isCardInReserve both bots). NO mvn run per house rules (orchestrator compiles). PENDING live Endor Operations game — expect log `V193: Endor Operations detected`, isObjectiveRelevantLocation true for Endor: Bunker, `V193 BUNKER CONTROL ... +400` once, a body on Bunker while empty, Establish Secret Base (V) deploying onto Bunker, and the objective flipping.
- Revert: delete the title-keyed Endor block in both ObjectiveAnalyzers (+ the flipCriticalControlSite field/getter/reset/parseGameText lines) and the `V193 BUNKER CONTROL` block in both DeployEvaluators. All new code tagged `V193` — grep the tag to walk every site.

## 2026-07-07 — Six Endor-game fixes (replay qgdridfo166f27r3); all UPDATE/CONSOLIDATE existing rules, no contradictory new versions
### V58/V67w MAINTENANCE FLOOR hardened (mistake 1) — DrawEvaluator both bots
- Rando drew its Force Pile below its maintenance upkeep in the Draw phase → Ap'lek sacrificed at end of turn → Life Force loss. The maintenance reserve was computed (V58/V67w) but only softly enforced (-15), which draw-hunger bonuses dominated. MOD: hard floor near the top of the draw body, BEFORE V42 + all bonuses: if maintenanceObligation>0 && forcePile<=maintenanceObligation && handSize>1 → VERY_BAD_DELTA + early return (Pass wins). Keys ONLY off maintenanceObligation so only "spend-it-and-lose-a-card" Force is hard-protected; empty-hand (handSize<=1) still falls to V42. Hardens V58/V67w IN PLACE, no new tag.
- Boundary: forcePile 1 <= maint 1 & hand 4 → suppress draw, Ap'lek lives. forcePile 2 > 1 → inert. maint 0 → never runs (byte-identical). Revert: remove the maintenance-floor block in both DrawEvaluators.
### V153 THIN RESERVE (mistake 3) — CardSelectionEvaluator both bots, UPDATES V153 in place
- When Rando's RESERVE DECK <= 10 he kept losing Force from reserve, burning destiny draws. MOD: in the V153 zone-order PROTECT-CHARS tier (regular + battle mirror), if isFromReserve && v153ProtectChars && reserveDeckSize<=10 → -335 so the reserve zone lands below hand-characters (100) but above the force pile (50). Survival tier (lifeForce<4) untouched.
- Boundary: reserve 400-335=65, between hand-char 100 and force-pile 50 — lose hand before reserve, keep activation fuel. Only helps VOLUNTARY loss (V118/V153 choices); opponent-FORCED reserve loss is unaffected. Revert: remove the THIN RESERVE guard in both blocks/bots.
### V24.15 EFFECTIVE DRAIN (mistake 4) — CardSelectionEvaluator both bots, CONSOLIDATED (was a separate V189b, folded into V24.15 per feedback_update_old_rule_not_new_version)
- Under opponent Battle Plan (+3 drain-initiation tax) Rando piled bodies onto Endor: Landing Platform (raw drain 1, net 1-3=-2, a drain there is a net loss V189 blocks every time) because deploy-siting only read RAW drain. MOD: extended the existing V24.15 (previously "avoid 0-drain sites") with a second arm — when initCost>0 && rawDrain-initCost <= -2 (the exact V189 predicate), penalize -(300+power*30) cap -700, EXEMPTING objective-relevant sites and V166 contests. Zero-drain arm is byte-identical to the old V24.15.
- Boundary: cost 0 (no tax = most games) → new arm never fires. Battle Plan live: Landing Platform net -2 fires; Cloud City drain-2 net -1 does not; Endor Bunker exempt (objective). Revert: restore the single-arm V24.15 zero-drain block in both bots.
### V193 ENDOR OPERATIONS Bunker plan (mistakes 2/5/6) — ObjectiveAnalyzer + DeployEvaluator both bots
- REFINED same day (Steve): the flip-gate CARD ('establish secret base') was hardcoded in DeployEvaluator — moved into the ObjectiveAnalyzer Endor block as getFlipCriticalControlCard(). DeployEvaluator's V193 is now a fully GENERAL flip-gate steer (reads both the control site AND the required card from the objective logic; log renamed BUNKER CONTROL -> FLIP-GATE CONTROL). All Endor-specific data now lives in the objective logic; the deploy scorer has zero card names. Any future occupation objective that sets a flip-critical site+card gets the steer for free.
- Endor: Bunker sat empty all game, so the cost-0 Establish Secret Base (V, Card207_025 = "Deploy on Bunker if you control that site") sat un-deployable in hand and the objective never flipped. ObjectiveAnalyzer also mis-parsed the objective text (garbage location fragments; whole "...are both" conjunction as one required card). MOD Part 1 (ObjectiveAnalyzer, title-keyed block for "endor operations", scoped — global regex untouched): add "endor" location fragment (Endor sites now objective-relevant), set requiredCardsOnTable = ominous rumors + establish secret base, new getFlipCriticalControlSite() = "endor: bunker". MOD Part 2 (DeployEvaluator V193, NEW tag = new behavior): +400 when analyzer flags the flip-critical control site, Rando doesn't yet control it, and Establish Secret Base is in hand/reserve — steers ONE body into the Bunker (self-limiting: bonus stops once controlled).
- Boundary: V193 is null for every other objective (0 contribution). Bunker 430+400=830 > Landing Platform ~660 for the first body; stops once Bunker controlled. Revert: remove the endor-operations block in ObjectiveAnalyzer + the V193 block in DeployEvaluator, both bots.
