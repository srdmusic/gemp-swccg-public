# 501_19 More Systems Will Rally: DEFERRED (2026-07-27)

Queue position: last P4 singleton. Deferred by the lead integrator with reasons, not dropped.

1. NO_PROFILE: unlike every objective batched tonight, 501_19 has no profile in the runtime
   playbooks and no entry in either seed facts file. The batch would author a complete
   profile from scratch (scaffolding + rules), a different change class from the
   flipLocationRules extensions of Batches Nine through Sixteen.
2. Conquest-driven law: the flip driver is flipping 501_23 Utapau / 501_26 Christophsis to
   their Separatist backs via Force drains or battle wins at related sites. The contract
   test needs real drain/battle sequences, not raw placement — roughly 3x tonight's
   per-batch harness cost.
3. Non-canonical: playtest set 501, setTestingText, hidden from the deck builder. Lowest
   live-table value in the queue.

The extraction packet is COMPLETE and preserved (agent output 2026-07-27): full front/back
law with citations, the onTable Separatist_system composite-key encoding plan (Hunt Down
onTable precedent), fixtures, traps (221_62 conversion, EP1-only deploy ban, four-effect
setup order), and a recorded engine defect (Card501_019_BACK.java L163-L168 leftover
System.out.println dereferences oldLocation before the null check — NPE risk; engine file,
out of AI scope, flagged for the devs).

Next session picks this up with: (a) profile authorship modeled on the leanest existing
entry, (b) the encoding plan above verbatim, (c) a conquest-sequence test helper.
