# 13_73 Let Them Make The First Move — behavior dossier

Family C. DARK mirror of 13_46. Codex coverage c7fe91860..ee64e6f3b: NONE. No identity flag. Profile inert (loaderEnabled absent).

## SOURCE LAW (re-verified vs Card13_073.java / _BACK @HEAD; record cites 145-162, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped && canSpot(INCLUDE_EXCLUDED_FROM_BATTLE, opponents AND Jedi AND presentAt(interior_Naboo_site AND battleground_site))` (Card13_073.java:147-150).
- flip-back-to-front: same NEGATED — no opponent Jedi present at any interior Naboo bg site; flip adds `RetrieveForceEffect(1)` (Card13_073_BACK.java:180-183, 190-193).
- terminal/hard-lose: none. Back is the mirror WEAPON: once/move your Dark Jedi duels an opponent Jedi present (2 destiny each, loser -2 unreducible, diff>=5 → Reserve loss + lost). Back drawback: no drain/battle where you have a Dark Jedi (Card13_073_BACK.java:64-173).

## WHO DRIVES THE FLIP
OPPONENT — fires only when the Light player parks a Jedi at an interior Naboo bg site; the Dark owner cannot force it, only stage a Dark Jedi to duel.

## CLASSIFICATION: EXISTING_ENGINE_FIT (positioning) + optional shared extension
- Gate location `interior_Naboo_battleground_site` ALREADY resolves (OA:8189-8192). Recipe: `loaderEnabled:true` + `locationFragments` = interior Naboo battleground → +150/+200 deploy bias positions YOUR Dark Jedi there for the duel. That is the achievable value; no YOUR-actor rule drives the flip.
- OPTIONAL SMALL_SHARED_EXTENSION (rank 2, shared with 13_46/222_27): opponent-actor-presence readiness fact (spot `opponents AND Jedi` at the gate class). Natural consumer for the UNREAD `denyOpponentActor` scoreRole. Cannot change the trigger — posture only.

## RULE OWNERS
- locationFragments consumers (getLocationObjectiveBonus, DPP/DE/CSE/ME sites) — live once loaderEnabled. Setup pulls already engine-forced (Theed Palace Generator Core/Generator, Deep Hatred, Card13_073.java:55-79).
- No actorToSite owner engages; no Naboo/duel arm in models/**.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario)
- `opponentJediPresentAtInteriorNabooBattlegroundFlipsToBack()` — native flip fires (opponent Jedi at Theed-interior bg → real FlipCardEffect).
- `deployingYourDarkJediTowardInteriorNabooBattlegroundIsBiasedForDuelReadiness()` — decision: bot prefers interior-Naboo-bg deploy over equal off-Naboo site.
- `opponentJediPresentButNotAtInteriorNabooBattlegroundDoesNotFlip()` — near-miss: Jedi present elsewhere → no flip.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: park a Dark Jedi at an interior Naboo battleground site (locationFragments bias) for immediate back-side duel use.
- READINESS (opponent-driven): don't chase the flip; hold a dueler and expect the toggle when the opponent commits a Jedi; back reverts (+1 Force) when they leave.
- POST-FLIP DRAWBACK: back bans your drain/battle where your Dark Jedi is — prize the duel kill, not board pressure.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas) + runtime gap confirmed (inert profile, no flag, gate resolvable but unwired). Codex must: enable loaderEnabled + wire interior_Naboo_battleground_site fragments; share the opponent-actor-presence fact decision with 13_46/222_27; 3-rung suite; parity. Not started.
