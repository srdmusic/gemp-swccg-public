# 7_296 Carbon Chamber Testing — behavior dossier

Family: captive / escort-move gate (DARK). Codex coverage in c7fe91860..ee64e6f3b: NONE. Flip route B is the escort-move shape closest to Codex's `actorLocationRules`, but it is a MOVE EVENT + a terminal "keep a captive on table" law.

## SOURCE LAW (re-verified vs Card7_296.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back route A (one-shot, post-deploy): `!canSpot(INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE, Filters.imprisonedIn(Security_Tower))` → `SetWhileInPlayData` flag + `FlipCardEffect` (Card7_296.java:120). If NO captive imprisoned at setup, flips immediately (and stamps the flag the back later reads).
- flip-to-back route B (MOVE event): `movedToLocationBy(playerId, Filters.escorting(Filters.frozenCaptive), Filters.Audience_Chamber) && canBeFlipped` (Card7_296.java:171-172). Escort carrying a frozen captive must COMPLETE a move to Audience Chamber; a frozen captive merely sitting there does NOT flip.
- flip-back: NEVER. Back has no FlipCardEffect.
- terminal/HARD-LOSE: back (My Favorite Decoration) placed OUT OF PLAY when `!canSpot(INCLUDE_CAPTIVE, Filters.frozenCaptive)` on table, UNLESS `cardHasWhileInPlayDataSet(self)` (route-A flag) (Card7_296_BACK.java:100-112). Post-flip the AI must keep ≥1 frozen captive on table permanently (unless it flipped via route A).

## KEY IDENTITIES
- Front 7_296 / back 7_296_BACK. `Filters.Security_Tower` (setup imprison zone), `Filters.Carbonite_Chamber` / `_Console`, `Filters.frozenCaptive`, `Filters.escorting(Filters.frozenCaptive)`, `Filters.Audience_Chamber` (flip gate site, route B), `Filters.Jabbas_Prize` (own imprisonable captive).
- Setup deploys (front:59-79): Carbonite Chamber, Console, Security Tower. Then: if Jabba's Prize in own Reserve Deck → deploy it imprisoned in Security Tower; else OPPONENT may imprison a Rebel there (Han/Leia/Luke excluded under YCEPBT / RTP / TIGIH) (front:82-113).
- Front pull once/deploy: Audience Chamber / Docking Bay 94 / East Platform (front:136-155). Back pull once/deploy: Scum And Villainy while a frozen captive at Audience Chamber (BACK:77-93). Ban: may not deploy Dark Deal (:162).

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → matched but fully INERT (consumer-map §2). All ACTIVE-eligible fields empty/null → ZERO consumers. `objectiveNamedLocations` (Cloud City package, Audience Chamber, ...) present but DEAD (not Gson-bound).
- No identity flag ("carbon chamber testing" not in OA:315-331) → generic evaluators + V21/V25/V186 parsers only.
- No captive/escort/imprison/frozen-captive/`movedToLocationBy` strategy in `models/**` (grep clean). The escort-move flip has no fact; the terminal "keep a frozen captive" law has no `requiredCardsOnTable` entry → V21 never-pitch (CSE:4178+) does NOT shield the captive.
- Expected failures by phase: PULL — no bias to pull Audience Chamber early (route-B prerequisite); `locationFragments` empty → no +150/+200/+200-V136 toward it. MOVE — nothing recognizes "move the escort+frozen-captive to Audience Chamber" as the flip event (MoveEvaluator has no captive/escort branch). DEPLOY — no drive to imprison Jabba's Prize / secure a captive. FORCE-LOSS/BATTLE — post-flip, Rando may let the last frozen captive be released/destroyed → back placed OUT OF PLAY (terminal loss) with nothing guarding it.

## CROSS-PHASE OBLIGATIONS
- INITIATE: secure a frozen captive at setup (Jabba's Prize) — else route A auto-flips (acceptable). Pull Audience Chamber, then MOVE an escort carrying the frozen captive there (route B) to flip.
- PRESERVE (terminal): after a route-B flip, NEVER let the last frozen captive leave the table — that is a hard out-of-play loss. (Route-A flips carry the WhileInPlayData immunity flag and are exempt.)
- ABSTAIN: do not deploy Dark Deal (banned).

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `CarbonChamberTestingObjectiveEngineContractTest`: `movingEscortWithFrozenCaptiveToAudienceChamberFlipsToBack()`, `frozenCaptiveSittingAtAudienceChamberDoesNotFlip()` (near-miss: standing state ≠ move event), `noImprisonedCaptiveAtSetupAutoFlipsRouteAAndStampsFlag()`, `backGoesOutOfPlayWhenLastFrozenCaptiveLeavesUnlessFlagSet()`.
2. `CarbonChamberTestingObjectiveBehaviorTest` (both bots): `escortCarryingFrozenCaptiveIsClassifiedAsFlipMoveActor()`, `audienceChamberIsClassifiedAsFlipGateForBothBots()`, `lastFrozenCaptiveRegistersHardLossExposure()`.
3. `CarbonChamberTestingRetentionBehaviorTest`: `frozenCaptiveRetentionIsUrgentPostFlipWhenFlagUnset()`, `forceLossNeverReleasesTheLastFrozenCaptive()`.
4. `CarbonChamberTestingCombinedEvaluatorDecisionTest` (both bots): `pullingAudienceChamberBeatsUnrelatedSitePull()`, `escortMoveToAudienceChamberBeatsIdleMove()`.
5. `CarbonChamberTesting*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No existing captive/escort rule owner (registry search clean; `ForceLossPolicy` V74 tangential). `actorLocationRules`/`actorToSite` models actor→control-site by OCCUPANCY, not a MOVE-completion event, and has no frozen-captive concept — NEW fact + consumer (step-3b hook, OA:8064).
- FACT: escort-move-flip fact (mover = `escorting(frozenCaptive)`, gate = Audience Chamber) + a captive-on-table survival fact. BOUNDARY: enable `loaderEnabled`; wire the frozen captive (Jabba's Prize / imprisoned card) into `requiredCardsOnTable` so V21 + `RequiredCardRetentionRisk` shield it — and wire Audience Chamber into `locationFragments` for the +150/+200 pull/deploy bias.
- CONSUMER: a MoveEvaluator branch rewarding "escort+frozen-captive → Audience Chamber" (the flip move) + a terminal guard (reuse `ObjectiveHardLossPolicy`) blocking any release of the last frozen captive post-flip. Boundary-math the move bonus vs existing MoveEvaluator drain/consolidation scores.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD, 0 deltas). Codex must: (1) engine-contract test proving the route-B escort-move flip AND route-A auto-flip + the out-of-play terminal, (2) decide fact-vs-consumer for escort-move + captive survival (nothing exists), (3) at minimum wire the frozen captive into requiredCardsOnTable + Audience Chamber into locationFragments across both bots. Not started.
