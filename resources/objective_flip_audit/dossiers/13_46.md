# 13_46 We'll Handle This — behavior dossier

Family C (presence/spot gates). Codex coverage c7fe91860..ee64e6f3b: NONE. No identity flag ("we'll handle this" not in OA:315-331; flag set = isInvasion/isMyLord/isEndor/isTdigwatt/isWantThatMap/isISBOperations/isHuntDownV/isShieldWillBeDown only). Profile inert (loaderEnabled absent).

## SOURCE LAW (re-verified vs Card13_046.java / _BACK @HEAD; spot-checked 143-161, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped && canSpot(INCLUDE_EXCLUDED_FROM_BATTLE, opponents AND Dark_Jedi AND presentAt(interior_Naboo_site AND battleground_site))` (Card13_046.java:146-149). VERIFIED exact.
- flip-back-to-front: same, NEGATED — no opponent Dark Jedi present at any interior Naboo bg site; flip additionally `RetrieveForceEffect(1)` (Card13_046_BACK.java:181-183, 189-193).
- terminal/hard-lose: none. Back is a WEAPON: once/move phase your Jedi may duel an opponent Dark Jedi present (2 destiny each, loser loses 2 unreducible; diff>=5 → Reserve loss + lost). Back drawback: you may NOT drain/battle where you have a Jedi (Card13_046_BACK.java:60-173).

## WHO DRIVES THE FLIP
OPPONENT — the flip fires only when the Dark player parks a Dark Jedi at an interior Naboo bg site; the Light owner cannot force it, only be READY to duel.

## CLASSIFICATION: EXISTING_ENGINE_FIT (positioning) + optional shared extension
- Gate location `interior_Naboo_battleground_site` ALREADY resolves in resolveLocationFilter (OA:8189-8192). Recipe: `loaderEnabled:true` + `locationFragments`/keySite = interior Naboo battleground → the +150/+200 deploy bias (getLocationObjectiveBonus) + V22 ordering positions YOUR Jedi there for back-side duel readiness. That is the whole forceable value; no actorToSite drives the flip (the actor is the opponent's).
- OPTIONAL SMALL_SHARED_EXTENSION (Family-C rank 2): an opponent-actor-presence readiness fact (spot `opponents AND Dark_Jedi` at the gate class) so the AI knows the flip is imminent/live and keeps a dueling Jedi parked. Schema already carries `scoreRole=denyOpponentActor` UNREAD (consumer-map §3a) — this is its natural first consumer. Low priority: cannot change the flip trigger, only posture.

## RULE OWNERS (grep rules.json + ai tree)
- locationFragments consumers: getLocationObjectiveBonus / isObjectiveRelevantLocation (OA), DPP:642-656/1184-1191/1613-1617, DE:1227, CSE +150/+200 sites, ME:642/1043 — all live TODAY once loaderEnabled.
- No actorToSite owner engages (findFlipGateActorRule requires a YOUR-actor rule; none applies). No Naboo/Jedi-duel arm anywhere in models/**.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario, house vocab)
- `opponentDarkJediPresentAtInteriorNabooBattlegroundFlipsToBack()` — native flip fires (put an opponent Dark Jedi at Theed-interior bg site; assert real FlipCardEffect).
- `deployingYourJediTowardInteriorNabooBattlegroundIsBiasedForDuelReadiness()` — decision test: with loaderEnabled+fragments the bot prefers an interior-Naboo-bg deploy over an equal off-Naboo site.
- `opponentDarkJediAtNonNabooSiteDoesNotFlip()` — near-miss: Dark Jedi present but NOT at an interior Naboo bg → no flip, no false positive.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: park a Jedi at an interior Naboo battleground site (locationFragments bias) so the back's duel is usable the instant it flips.
- READINESS (opponent-driven): do not chase the flip; keep a dueler positioned and expect the toggle when the opponent commits a Dark Jedi.
- POST-FLIP DRAWBACK: back forbids your drain/battle where your Jedi is — value the duel kill, not board pressure, while flipped.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas) + runtime-gap confirmed (inert profile, no flag, gate location resolvable but unwired). Codex must: enable loaderEnabled + wire interior_Naboo_battleground_site fragments; decide whether to build the shared opponent-actor-presence readiness fact (shared with 13_73/222_27); write the 3-rung suite; prove rando↔chosenone parity. Not started.
