# 112_1 Agents In The Court — behavior dossier

Family D. Codex coverage c7fe91860..ee64e6f3b: NONE. DARK twin = 112_15 (My Kind Of Scum) — identical flip law, build once. Distinctive: the flip's key actor is a non-unique alien of the CHOSEN REP's species, where the Rep is chosen-state picked at setup and its species is read from its blueprint at flip-check time.

## SOURCE LAW (record re-verified vs Card112_001.java / _BACK.java @HEAD; guard+branches spot-checked 133-167, exact, 0 deltas)
- flip-to-back (auto, isTableChanged, singleton): GUARD `rep = self.getWhileInPlayData().getPhysicalCard(); if rep==null return null` (134-137) — no Rep, no flip trigger ever. Then anyOf: [`canSpotLocation(non_Tatooine_location)` → `occupies(playerId,3,INCLUDE_EXCLUDED,battleground_site)` AND `occupiesWith(self,playerId,battleground_site, non_unique+alien+species(rep.species))`] OR [NOT non_Tatooine_location → `occupies(playerId,2,battleground_site)`] (144-150). OCCUPY, not control.
- flip-to-front (auto): `canBeFlipped` AND NOT `occupies(playerId,2,battleground_site)` (112_001_BACK.java:155-156). Asymmetric — back never re-checks the 3-site/species branch; 2 occupied battleground sites always hold it.
- terminal: none either side.

## KEY IDENTITIES
- Front 112_1 / back 112_1_BACK, LIGHT. Rep = `Filters.and(unique, alien, hasSpecies)` revealed from Reserve Deck at setup; stored `WhileInPlayData(rep)` + `gameState.setRep`. Dynamic actor = `Filters.and(non_unique, alien, Filters.species(rep.getBlueprint().getSpecies()))`.
- Gate class = `Filters.battleground_site`; branch selector = `Filters.non_Tatooine_location` (either player's location on table). Setup deploys: Hutt Trade Route, a Jabba's Palace site, (opt) Yarna d'al' Gargan.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. flipGateSite/keyCharacterFilter null, requiredCardsOnTable/pullableCards/locationFragments empty. No identity flag.
- No fact for the chosen Rep or its species; the dynamic occupyWith leg cannot be expressed by any static `resolveFilter` key (species unknown until Rep chosen at runtime). The 2-vs-3 branch selector and occupy-vs-control distinction are unrepresented.

## CLASSIFICATION: NEW_PRIMITIVE (chosen-state species fact); count/branch legs = SMALL_SHARED_EXTENSION
- Primitive — CHOSEN-STATE FACT: read the objective's `WhileInPlayData` Rep, extract `rep.getBlueprint().getSpecies()`, and build `Filters.species(...)` dynamically for the occupyWith leg. No static key works (this is why resolveFilter's 5-key registry can't cover it). Shared with 112_15 (species) and 7_138 (apprentice identity).
- Extension (reuse) — the count legs ride the post-Endor flipLocationRules engine (Family A: occupy relation, count>=2/>=3, occupyWith). The anyOf-of-allOf branch rides Family A's `subLegs`. The 2-vs-3 branch selector (`canSpotLocation(non_Tatooine_location)` either-player) is a small NEW location-rule leg: a GLOBAL (non-owner-scoped) spot-count leg — minor vocabulary add, not a primitive.
- flip-back (NOT occupy 2 battleground_site) = negated-count leg, engine-native (occupy self battleground_site count<2).

## RULE OWNERS (update-in-place / new)
- Chosen-state fact = new step-3b STATE hook (OA:8064). NO existing owner reads objective WhileInPlayData for a chosen card. 
- Count/occupy legs = author `flipLocationRules` on a new loaderEnabled profile (Family A engine, relationSatisfiedAt OA:4231). `subLegs` = the Family-A nested-allOf extension (FlipLocationAlternative OA:7785).
- Min config: `loaderEnabled` + `locationFragments`=battleground_site for +150/+200 occupy bias; requiredCardsOnTable = the Rep (retention).

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `AgentsInTheCourtObjectiveEngineContractTest`: `occupyThreeBattlegroundSitesWithRepSpeciesAlienWhileNonTatooineOnTableFlips()`, `occupyTwoBattlegroundSitesWithNoNonTatooineLocationFlips()`, `threeSitesButNoRepSpeciesAlienDoesNotFlip()` (near-miss), `repUnsetMeansObjectiveNeverFlips()` (the null-guard), `dropBelowTwoOccupiedBattlegroundSitesFlipsBack()`.
2. `AgentsInTheCourtObjectiveBehaviorTest` (both bots): `chosenRepSpeciesIsClassifiedAsDynamicFlipActor()`, `nonTatooineLocationRaisesFlipBarToThreeSitesPlusSpeciesAlien()`, `holdingTwoBattlegroundSitesRegistersFlipBackProtection()`.
3. `AgentsInTheCourtDecisionsAreIdenticalAcrossRandoAndChosenone()`.

## CROSS-PHASE OBLIGATIONS
- SETUP: the Rep choice determines the whole flip — pick a Rep whose non-unique species aliens are in-deck. DEPLOY/MOVE: occupy 2 battleground sites (3 + one with a species alien if any non-Tatooine location is out). Back-side retrieval feeds species aliens. PRESERVE: hold 2 occupied battleground sites (flip-back guard).

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas). Codex must design the chosen-state species fact (shared build with 112_15), author flipLocationRules + subLegs + global-spot branch selector, wire the Rep into requiredCardsOnTable. Not started.
