# 203_19 Diplomatic Mission To Alderaan — behavior dossier

Family: card-STATE gate (delivered Stolen Data Tapes) + dual controlsWith legs. Codex coverage in c7fe91860..ee64e6f3b: NONE. THIS IS THE ONE loaderEnabled:true profile in my family — but its enabled facts do NOT cover the flip law (see RUNTIME).

## SOURCE LAW (re-verified vs Card203_019.java / _BACK.java @HEAD, matches record; leg 165-167 spot-confirmed)
- flip-to-back: `isTableChanged && canBeFlipped(self) && canSpot(delivered_Stolen_Data_Tapes) && controlsWith(battleground_site, INCLUDE_EXCLUDED_FROM_BATTLE, Rebel) && controlsWith(battleground_system, INCLUDE_EXCLUDED_FROM_BATTLE, Rebel)` (203_019.java:163-167). Mixes card-STATE (tapes in `delivered` keyword state, not merely on table) with two CONTROL-WITH legs (control a bg site AND a bg system, each WITH a Rebel present).
- flip-back: `isTableChanged && canBeFlipped && ( !occupies(battleground_site) OR !occupies(battleground_system) OR !controls(any location) )` (203_019_BACK.java:102-106). Legs DOWNGRADE to OCCUPY for site/system; third leg is CONTROL of any location.
- terminal/hard-lose: none either side.
- Asymmetry to exploit: forward needs Rebel CONTROL of bg site+system; staying on back needs only OCCUPATION of bg site+system + control of >=1 location anywhere.

## KEY IDENTITIES
- Front bp 203_19 / back 203_19_BACK. LIGHT.
- Gate card-STATE = `Filters.delivered_Stolen_Data_Tapes` (Stolen Data Tapes = card 203_14; "delivered" is a keyword STATE, not just presence).
- Control legs: `Filters.battleground_site` + `Filters.battleground_system`, each with `Filters.Rebel`. Back destiny bonus uses OccupiesWithEvaluator(battleground, Rebel abilityLessThan 4).
- Setup (203_019.java:52-90): Tatooine system; Tantive IV + non-[Reflections II] R2-D2 + Stolen Data Tapes to Tatooine system; Dune Sea. Both sides once/turn download: Alderaan system OR a Tatooine battleground site.

## RUNTIME TODAY (loaderEnabled = TRUE — but nuance)
- loaderEnabled:true → hydration runs (consumer-map §2, OA:257). What actually activates: ONLY `locationFragments=["tatooine","alderaan","dune sea"]` → `flipConditionLocationFragments` → +150/+200 location bonus + V22 plan ordering + V136 +200 site bonus + Setup match (consumer-map row locationFragments). That is a generic Tatooine/Alderaan/Dune-Sea DEPLOY bias — NOT flip-gate awareness.
- Everything gating the actual flip is INERT: requiredCardsOnTable [] (and note AUTHORITATIVE clear-then-set at OA:966-969 — the empty array CLEARS any parser-derived required cards for this objective, consumer-map §2), flipGateSite null, flipGateCardIds [], keyCharacterFilter null (INFORMATIONAL anyway), weights {}. objectiveNamedLocations DEAD.
- The `delivered` card-STATE is EXPLICITLY unimplemented: ObjectiveAnalyzer.java:8064 comment lists "delivered" among STATE keys that "need their own runtime hooks (step 3b)"; grep of the AI tree finds NO `delivered`/`StolenDataTapes` consumer.
- Expected Rando failure modes:
  - PULL/DEPLOY: no notion that Stolen Data Tapes must reach `delivered` state — the flip bottleneck is invisible. Rando gets only generic Tatooine/Alderaan deploy bias.
  - CONTROL: no fact that BOTH a bg site AND a bg system must be Rebel-CONTROLLED to flip; no `controlsWith` reader exists in the analyzer (the relation string at OA:7738 is Invasion-only actorLocationRules DTO vocabulary, unread here).
  - FORCE-LOSS/PRESERVATION: the tapes / control anchors are unshielded; and once flipped, no awareness of the flip-back exposure (lose occupy of bg site OR bg system, OR control zero locations).

## CROSS-PHASE OBLIGATIONS
- Get Stolen Data Tapes into `delivered` state (the bottleneck) — a specific card-state transition, not a deploy.
- Establish and hold Rebel CONTROL of one bg site + one bg system to flip.
- Post-flip: never simultaneously drop OCCUPATION of bg site/system or CONTROL of all locations (any one loss flips back).

## MINIMAL BEHAVIORAL-TEST PATTERN
1. `DiplomaticMissionObjectiveEngineContractTest` — real-card: `flipsToBackWhenTapesDeliveredAndRebelControlsBgSiteAndSystem()`, `doesNotFlipWithoutDeliveredState()`, `doesNotFlipOnMereOccupation()`, `flipBackWhenOccupationOrControlLost()`.
2. `...RequiredEffectsBehaviorTest` — `deliveredTapesStateIsFlipBottleneckForBothBots()`, `controlWithoutDeliveredIsNearMissNotProgress()`, `loaderEnabledHydratesOnlyLocationBiasNotFlipGate()` (pins the current nuance).
3. `...RetentionBehaviorTest` — `postFlipHoldsBothBattlegroundOccupationsAndOneControlledLocation()`, `neverPitchesTheControlAnchorsWhileFlipPending()`.
4. `...CombinedEvaluatorDecisionTest` — `deliveringTapesBeatsUnrelatedDeploy()`, `takingRebelControlOfBgSystemBeatsDistractor()` (both bots).

## FIX SKETCH (AI-only)
- The delivered-state gate is a NEW step-3b STATE hook — the code already names it as deferred at ObjectiveAnalyzer.java:8064 ("delivered ... need their own runtime hooks (step 3b)"). Extend THAT plan (update-in-place), do not fabricate a parallel state system.
- The dual controlsWith legs need a NEW flip-progress fact (control a bg site + bg system with Rebel). No existing owner: rules.json flip owners are single-site (`PULL.OBJECTIVE.FLIP_GATE_SITE`) / actor-formation (`V297-plan-ranking-flip-gate-formation`), neither models a two-typed-control conjunction.
- BOUNDARY: this profile is ALREADY loaderEnabled; adding real gate facts here won't need a switch flip, but beware the requiredCardsOnTable authoritative-clear (OA:966) — populate it deliberately, do not leave [] if the parser had useful entries.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD). Codex must (1) prove the delivered-state + dual-control flip and the occupy-downgrade flip-back, (2) pin that loaderEnabled today buys only location bias, (3) design the step-3b delivered hook + dual-control fact. Not started.
