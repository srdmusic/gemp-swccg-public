# 215_17 Rescue The Princess (V) — behavior dossier

Family: captive-release + occupy-with-global-flag gate (LIGHT). Codex coverage in c7fe91860..ee64e6f3b: NONE. Virtual redesign of 7_139; entirely different flip law (occupy + Death Star power flag vs a move event).

## SOURCE LAW (re-verified vs Card215_017.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back: `canBeFlipped(self) && occupiesWith(playerId, Filters.Death_Star_site, Filters.Leia) && isDeathStarPowerShutDown(game)` (Card215_017.java:143-145). `occupiesWith` uses NO spot override → an imprisoned/captive Leia does NOT occupy; needs a FREE active Leia occupying a Death Star site AND the global "A Power Loss shut down" flag.
- flip-back: `canBeFlipped && !canSpot(INCLUDE_EXCLUDED_FROM_BATTLE, Filters.Leia AND at(Death_Star_site))` (Card215_017_BACK.java:197-198). NOTE: no `INCLUDE_CAPTIVE` → a CAPTURED Leia is unspottable and ALSO flips back; Leia leaving the DS site / table flips back too.
- UNUSUAL TIMING: neither flip trigger is gated on `isTableChanged` nor marked singleton — evaluated on every required-after check.
- terminal/hard-lose: no out-of-play. SAFETY NET (both sides): Leia about to leave table for ANY reason → instead imprisoned in Detention Block Corridor, cards on her to Used Pile (front:154-182; back:157-185) — requires Detention Block Corridor on table.

## KEY IDENTITIES
- Front 215_17 / back 215_17_BACK. `Filters.Leia` (persona; flip actor both sides), `Filters.Death_Star_site` (gate + Force-gen +1 buff + download target), `GameConditions.isDeathStarPowerShutDown` / `Filters.A_Power_Loss` (bp 215_2), `Filters.Detention_Block_Corridor` (safety-net origin + setup captive Leia).
- Setup deploys (front:63-91): Death Star: Central Core, A Power Loss, Trash Compactor, Detention Block Corridor; then `and(Icon.A_NEW_HOPE, Filters.Leia)` deployed IMPRISONED to Detention Block Corridor (:93-101).
- Pull once/turn (front+back, shared id): download a Death Star site (front:117-136; back:82-98). Deploy BAN both sides: Luke ability>4 OR `Jedi AND (Episode_I OR Episode_VII)`. Back: opponent +1 Force to drain; Obi-Wan out-of-play cancels a DS battle (BACK:135-150).

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → matched but fully INERT (consumer-map §2). All ACTIVE-eligible fields empty/null → ZERO consumers. `objectiveNamedLocations` (Death Star site, Detention Block Corridor, ...) present but DEAD (not Gson-bound).
- No identity flag ("rescue the princess" not in OA:315-331) → generic evaluators + V21/V25/V186 parsers only.
- No captive-release / occupy-with-global-flag strategy in `models/**` (grep clean); `isDeathStarPowerShutDown` never referenced by the AI (grep clean). The flip needs a RESCUED free Leia occupying a DS site AND a global power-off flag — neither expressible by any live field (`flipGateSite`+actor rule express control-occupancy but NOT captive-release nor a global flag).
- Expected failures by phase: RESCUE/MOVE — nothing drives releasing the setup-imprisoned Leia from Detention Block Corridor, nor occupying a DS site with her. INTERRUPT/EFFECT — nothing recognizes shutting down A Power Loss as a flip precondition. PRESERVE — Leia captured OR off the DS site flips back; generic logic won't treat her capture as a flip-back terminal. FORCE-LOSS/BATTLE — safety-net imprisonment (Leia→Detention Block Corridor) unmodeled; losing Detention Block Corridor breaks the net.

## CROSS-PHASE OBLIGATIONS
- INITIATE: RESCUE Leia from Detention Block Corridor (release the setup captive), shut down A Power Loss (global flag), then occupy a Death Star site with free Leia — all three simultaneously flip.
- PRESERVE: keep free Leia at a DS site continuously (evaluated every trigger check, not just on table change); never let her be captured or leave the DS site (flip-back). Keep Detention Block Corridor on table (safety net).
- ABSTAIN: do not deploy Luke ability>4 or [Ep I]/[Ep VII] Jedi (banned both sides).

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `RescuePrincessVObjectiveEngineContractTest`: `freeLeiaOccupyingDeathStarSiteWithPowerShutDownFlipsToBack()`, `imprisonedLeiaAtDeathStarSiteDoesNotOccupyAndDoesNotFlip()` (near-miss: captive ≠ occupies), `powerNotShutDownWithLeiaOccupyingDoesNotFlip()` (global-flag boundary), `capturingLeiaFlipsBackToFront()`, `leiaLeavingTableIsRedirectedToDetentionBlockCorridorSafetyNet()`.
2. `RescuePrincessVObjectiveBehaviorTest` (both bots): `rescuedFreeLeiaAtDeathStarSiteIsClassifiedAsFlipActor()`, `deathStarPowerShutDownIsClassifiedAsFlipPrecondition()`, `leiaCapturedOrOffSiteRegistersFlipBackExposure()`.
3. `RescuePrincessVRequiredEffectsBehaviorTest`: `detentionBlockCorridorRetentionIsUrgentAsTheSafetyNetForBothBots()`.
4. `RescuePrincessVCombinedEvaluatorDecisionTest` (both bots): `releasingLeiaAndOccupyingDeathStarSiteBeatsUnrelatedMove()`, `shuttingDownAPowerLossBeatsDistractorAction()`.
5. `RescuePrincessV*SourceParityTest`: rando↔chosenone parity. (Distinguish clearly from classic 7_139 fixtures — same title, different law.)

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No existing captive-release / global-flag rule owner (registry search clean). `actorLocationRules`/`actorToSite` expresses actor→control-site OCCUPANCY (close to "occupy DS site with Leia") but CANNOT express (a) the captive must first be RELEASED, nor (b) the `isDeathStarPowerShutDown` global-flag conjunct — NEW facts + consumer (step-3b hook, OA:8064).
- FACT: an occupy-with-global-flag flip fact (actor Leia, gate `Death_Star_site`, precondition `isDeathStarPowerShutDown`) + a captive-release-prereq fact (rescue Leia from Detention Block Corridor first). BOUNDARY: enable `loaderEnabled`; wire `Death_Star_site` into `locationFragments` (+150/+200 occupy bias) and A Power Loss (215_2) + Detention Block Corridor into `requiredCardsOnTable` so retention/`RequiredCardRetentionRisk` shield them.
- CONSUMER: a rescue-then-occupy drive + a flip-back guard treating Leia's capture/departure as objective-critical (reuse `ObjectiveHardLossPolicy` shape for the safety-net/flip-back terminal). Global-flag conjunct needs a real engine read of `isDeathStarPowerShutDown`; boundary-math occupy bonus vs generic drain/consolidation.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD, 0 deltas). Codex must: (1) engine-contract test proving free-Leia-occupy+power-flag flip AND the imprisoned/captive near-misses + capture flip-back + safety net, (2) decide facts-vs-consumer for captive-release + global-flag (nothing exists; the global flag is novel), (3) wire Death Star site / A Power Loss / Detention Block Corridor into locationFragments+requiredCardsOnTable across both bots. Not started.
