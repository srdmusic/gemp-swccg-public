# 601_87 Hunt Down And Destroy The Jedi (V) — behavior dossier

Family E (LEGACY, NON-CANONICAL). `ExpansionSet.LEGACY`, `setAsLegacy(true)`, `Icon.LEGACY_BLOCK_4`, `setVirtualSuffix(true)`; absent from every pool/deck resource (legacy-format-only). **SIBLING of the PROVEN Hunt Down pair 7_297 / 213_31** (profiles landed in dc3d996fc). Codex coverage of THIS bp: NONE. **CLASSIFICATION: EXISTING_ENGINE_FIT** — the profile-driven flipLocationRules engine + Hunt Down machinery covers it with a profile plus two trivial `resolveFilter` keys (Galen-or-Vader actor, opponent-unique-ability>3 blocker).

## SOURCE LAW (re-verified vs Card601_087.java / _BACK @HEAD; matches record, 0 deltas)
- flip-to-back (Card601_087.java:118-134): `isTableChanged` + `canBeFlipped` + `canSpot(INCLUDE_EXCLUDED_FROM_BATTLE, Filters.and(or(Galen,Vader), at(battleground_site)))` AND `NOT canSpot(... opponents, unique, character, abilityMoreThan(3), at(battleground_site))`. Coded as `Filters.at` though printed text says "present at".
- flip-to-front (Card601_087_BACK.java:106-123): `isTableChanged` + `canBeFlipped` + [ opponent unique char ability>3 at a battleground site, OR (NOT spot Vader AND NOT spot Galen) ]. **Divergence (engine territory):** the "both Vader and Galen off-table" flip-back route (BACK:112-113) is NOT in the printed back text — source adds it.
- No place-out-of-play on either side.
- UNCODED CARD TODO (engine territory, not AI): back's "+2 destiny vs vehicle/starship with Lightsaber Parry / Tarkin's Orders / There Is No Conflict" exists only as a comment (BACK:65) — no modifier. And the FRONT Rogue Shadow upload (Card601_087.java:93-107) has NO once-per-turn/game limit coded (only `canTakeCardsIntoHandFromReserveDeck` guards it) — a print-vs-code defect. Record both; do not fix from AI.

## KEY IDENTITIES
- Front 601_87 / back 601_87_BACK. Flip actors: `Filters.Galen`, `Filters.Vader` (either). Pivot predicate (both directions): opponent unique character ability>3 at a battleground site. Location class: `Filters.battleground_site` (no named site gate).
- Setup (front:63-88): Coruscant system, Imperial City, A Sith's Plans; optional (0-1) If The Trace Was Correct. Front pull: Rogue Shadow into hand (unlimited). Back: 2-Force upload of any card WITHOUT ability; retrieve 1 Force — both once/control-phase.
- Both sides ban [Episode I] Dark Jedi; both carry Galen's/Vader's Lightsaber hit-then-dies → opponent loses 2 Force.
- Contrast to proven 7_297: 7_297 keys Vader-only, Jedi-or-Luke blocker, Executor-sites/Vader's-Castle setup. 601_87 keys Galen-OR-Vader, unique-ability>3 blocker, Coruscant/Rogue-Shadow setup.

## RUNTIME TODAY
- Profile: does not exist. BUT the V25 text parser fires on 601_87 anyway: OA:10045 sets `isHuntDownV = true` + `huntDownNeedsVader = true` whenever the flip condition contains "vader" AND "battleground" (601_87's text does). So the shared Hunt Down code path PARTIALLY engages today WITHOUT a profile — but Vader-centric: the Galen flip leg is invisible to the parser, the blocker is treated as Jedi-or-Luke shape not unique-ability>3, and the Vader's-Castle sub-engine (OA:2020-2135, `assessVaderCastleRoutes`) keys on a Vader's Castle 601_87 never deploys → `findActiveVaderCastle` returns null → those assessments no-op (harmless but useless).
- `resolveFilter` (OA:8022-8050) has NO Galen key and NO unique/ability>3 key — only Vader, Jedi_or_Luke, Jedi_Padawan_or_Luke. Two one-line cases are the whole registry gap: `Galen_or_Vader` → `Filters.or(Galen, Vader)`, and a blocker key → `Filters.and(opponents(self), unique, character, abilityMoreThan(3))`.

## CROSS-PHASE OBLIGATIONS
- INITIATE: keep Galen and/or Vader parked at battleground sites; clear (or avoid deploying near) opponent unique ability>3 characters at battleground sites — their mere presence at ANY battleground site reverts the objective.
- PRESERVE: do not let BOTH Vader and Galen leave the table (source-added flip-back route). Use the lightsaber hit-then-dies 2-Force bleed.
- ABSTAIN: no [Episode I] Dark Jedi.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary; extend the proven Hunt Down suites)
1. `HuntDownVLegacyObjectiveEngineContractTest`: `galenAtBattlegroundWithNoBigEnemyFlipsToBack()`, `vaderAloneAlsoFlips()`, `opponentUniqueAbility4AtBattlegroundBlocksAndRevertsFlip()`, `bothVaderAndGalenOffTableFlipsBack_sourceRoute()`.
2. `HuntDownVLegacyObjectiveBehaviorTest` (both bots): `galenOrVaderKeyActorClassifiedForBothBots()`, `uniqueAbilityGt3BlockerClassifiedAsFlipPivot()`, `vaderCastleMachineryIsInertWithoutCastle()`.
3. `HuntDownVLegacyCombinedEvaluatorDecisionTest` (both bots): `parkingGalenAtBattlegroundBeatsIdleDeploy()`, `clearingBigEnemyOutranksTrivialDrain()`.
4. `HuntDownVLegacy*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- Author a profile (from scratch) modeled on 7_297's: `flipLocationRules` front = `actorToLocation` (actorFilterKey `Galen_or_Vader`, `at battleground_site, count >=1`) + `globalBlockerAbsent` (blocker key `opponent_unique_ability_gt3`, `count ==0`); back = `globalBlockerPresent` + a `requiredActorMissing`-style pair covering BOTH Vader and Galen off-table. Add the two `resolveFilter` cases. `keyCharacterFilter` = `Galen_or_Vader`.
- Leave the Vader's-Castle sub-engine alone (it self-gates to null for 601_87); do NOT copy 7_297's Executor/Vader's-Castle `startingLocations`. Log the code-vs-print divergences (source-added flip-back, uncoded destiny TODO, unlimited Rogue Shadow) as CARD/engine bugs — out of AI scope.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD, 0 deltas). EXISTING_ENGINE_FIT — Hunt Down engine + profile + 2 filter keys. Text parser already half-engages it Vader-only (mismatched). No profile authored; non-canonical legacy. Not started; in-scope ONLY if legacy format is targeted, else defer.
