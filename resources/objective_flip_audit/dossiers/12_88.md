# 12_88 Plead My Case To The Senate — behavior dossier

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. This LIGHT senate objective is the mirror of My Lord (12_179) but has ZERO objective-aware AI wiring.

## SOURCE LAW (record re-verified vs Card12_088.java / _BACK.java at HEAD; deltas noted)
- flip-to-back: `canBeFlipped` AND ( `filterActive(your senators AT Galactic_Senate, INCLUDE_EXCLUDED_FROM_BATTLE).size() >= 3` OR ( `size() >= 2` AND `canSpot(peace_agenda)` among that set ) ). Table-changed poll. Card12_088.java:161-181. VERIFIED.
- flip-back-to-front: `canBeFlipped` AND NOT `canSpot(>=2 your senators AT Galactic_Senate, INCLUDE_EXCLUDED_FROM_BATTLE)`. Drop below 2 → flip back. Card12_088_BACK.java:167-183. VERIFIED.
- terminal: none. No place-out-of-play on either side. VERIFIED.
- Delta from record: none. Record's "AT not present/control" relation note confirmed; seedMismatch (seed labels PRESENT) stands as label-only.

## KEY IDENTITIES
- Blueprints: front 12_88, back 12_88_BACK. Gate site Galactic Senate = ids 12_167 / 12_75 (Filters.Galactic_Senate).
- Flip actor set: `Filters.and(Filters.your(self), Filters.senator, Filters.at(Filters.Galactic_Senate))`.
- Thresholds / routes: route A = 3 senators AT Senate; route B = 2 senators AT Senate + at least one `Filters.peace_agenda`. Hold-back threshold = keep >= 2 senators AT Senate.
- Senator detection standing rule matches the analyzer's `isSenatorCard` (Keyword.SENATOR OR lore contains "senator"), OA:7546-7552 — same predicate the engine's Filters.senator resolves; safe for AI planning.

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → profile fully INERT (OA:227-232, 257). Every 12_88 profile field (locationFragments empty, flipGateSite null, keyChar/keySite null, weights {}) reaches NO consumer. Consumer-map §2, §3.
- No identity flag: `isMyLord` = FALSE (title "plead my case"/"sanity and compassion" fails OA:316 substring "my lord"/"make it legal"). There is no `isPleadMyCase`. So V83/V88/V108/V110 senator arms (isMyLord-gated, OA:8244/8265/8287/8333) NEVER fire for 12_88.
- Only touch point: V99 SENATE GUARD (OA:8342-8385) is deliberately UNGATED — it keys on a Galactic Senate being a top location. For 12_88 it BLOCKS a non-senator deploying to the Senate when own-senator power >= opponent power (-1500). It is a blocker, not an accumulator: it never pushes senators toward the Senate and never models the peace-agenda route or the >=2 hold-back.
- Expected failure modes: Deploy phase — Rando setup-deploys Galactic Senate (engine-forced) but treats senators as generic bodies, spreading them across Episode I locations; no +1500 pull toward Senate, no +500 senator-deploy priority (CSE senator arms are V88-CS/V109 isMyLord-gated too). Never reaches 3-at-Senate; never flips. Move phase — no gate-hold obligation to keep >=2 at Senate, so it drifts back to front for free.

## CROSS-PHASE OBLIGATIONS (what the law demands that AI must model)
- Accumulate: >= 3 of your senators AT Galactic Senate (or 2 + one peace_agenda) to flip.
- Hold: keep >= 2 senators AT Galactic Senate every table-change or the back flips to front (maintenance obligation, no phase gate).
- Budget: senators are the scarce flip currency — deploy priority + move-to-Senate + do-not-scatter, exactly the My Lord discipline, on the LIGHT side.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder; mirror WeHaveAPlan/RegionalCounted vocab)
1. `profileContractIsInertUntilSenateFamilyEnabledForBothFacades` — assert profile currently reaches no consumer (loaderEnabled absent); parity rando==chosenone.
2. `structuredFrontAndBackRulesUseOnlyExactSenatorAtSenateConditions` — engine-contract: 3-at-Senate OR 2+peace_agenda flips; drop-below-2 flips back (real Card12_088 logic).
3. `senatorDeployPrioritizesGalacticSenateButRejectsNonSenatorsAndOtherEpisodeISites` — candidate vs distractor.
4. `senatorAdvanceIsExactToSenatorTypeSenateSiteAndUnmetCountOfThree` — near-miss rejection (2 senators, no peace agenda → not yet).
5. `lastTwoSenatorsAtSenateAreProtectedAsTheFormationHoldingTheBack` — sequence/preservation (flip-back guard).
6. `objectiveDeploysThirdSenatorAtSenateAndFlips` — flip trigger via engine contract.
7. `senateFamilyDecisionsAreIdenticalAcrossRandoAndChosenone` — parity.

## FIX SKETCH (AI-only, update-in-place; owner search: rules.json)
- Existing owners to extend, NOT new tags: V83 / V88 / V108 / V110 (ObjectiveAnalyzer getDeployObjectiveAdjustments) + V88-CS / V99-CS (rules.json CardSelection senator arms). All are isMyLord-gated today.
- Smallest change family: broaden the senate-objective identity so the LIGHT mirror qualifies — either generalize `isMyLord` into a shared "senate-count family" flag set at OA:316 by title ("plead my case"/"sanity and compassion") OR add a sibling flag feeding the SAME ScoreNote arms. Weights must come from an ACTIVE playbook, so also mirror 12_179's profile onto 12_88 (loaderEnabled:true + weights 1500/-2000/500/-2000) since 12_88's activePlaybook is null today.
- One real law difference to encode: 12_88's 2-senator alt uses `Filters.peace_agenda` (My Lord uses `Filters.blockade_agenda`). Do NOT hardcode; parameterize the agenda filter per objective.
- NO implementation here.

## STATUS
Proof gate reached: SOURCE_VERIFIED (record) + runtime-gap confirmed (isMyLord false; profile inert; only V99 partial). Codex still must: write the 7-rung behavioral suite above, decide identity-broadening vs sibling-flag, wire an ACTIVE weight source for the LIGHT side, prove peace-agenda route, and prove rando/chosenone parity.
