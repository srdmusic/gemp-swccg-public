# 219_1 A Great Tactician Creates Plans — behavior dossier

Family: stacked-card COUNTER gate + persona-presence + phase window, with two-route flip-BACK hazards. Codex coverage in c7fe91860..ee64e6f3b: NONE.

## SOURCE LAW (re-verified vs Card219_001.java / _BACK.java @HEAD, matches record)
- flip-to-back: `isTableChanged && canBeFlipped(self) && isDuringEitherPlayersPhase(DEPLOY) && canSpot(Thrawn AT battleground) && findFirstActive(Thrawns_Art_Collection)!=null && hasStackedCards(collection, 2)` (Card219_001.java:124-129). Four ANDed legs: phase window (deploy only, either player), presence (Thrawn AT any battleground — the AT relation, not control/occupy), card-on-table (Collection active), counter (>=2 stacked = "two artwork on table").
- flip-back (two independent routes, `_BACK.java:144-181`):
  - Route A: `isTableChanged && canBeFlipped && canSpot(Collection) && !isDuringBattle && !hasStackedCards(collection)` — Collection on table with ZERO stacked, except during battle.
  - Route B: `isTableChanged && canBeFlipped && !canSpot(Thrawn)` — Thrawn gone, ANY phase incl. battle. Losing Thrawn flips back even mid-battle.
- terminal/hard-lose: none either side.
- HAZARD: the back "study" trigger SPENDS one stacked artwork per use (BACK:93-141); draining the stack to zero outside battle triggers route A.

## KEY IDENTITIES
- Front bp 219_1 / back 219_1_BACK. DARK side.
- Decisive persona = `Filters.Thrawn` (flip actor front; his absence flips back both routes). Thrawn printings 10_40, 207_21, 207_30 (seed).
- Counter host = `Filters.Thrawns_Art_Collection` (bp 219_20); "artwork cards" == cards stacked on it (no independent artwork filter in the flip law).
- Flip location class = `Filters.battleground` (any battleground, site OR system — not battleground_site).
- Setup deploys (219_001.java:57-78): Lothal system (219_10), Lothal: Advanced Projects Laboratory (219_11), Lothal: Imperial Complex (219_13), Thrawn's Art Collection.
- Both sides once/turn download: a battleground_system OR a site deployableToSystem(Lothal).

## RUNTIME TODAY
- Profile loaderEnabled ABSENT → INERT (consumer-map §2). locationFragments [], requiredCardsOnTable [], keyCharacterFilter null, flipGateSite null, flipGateCardIds [], weights {} → zero consumers. objectiveNamedLocations DEAD.
- Note: even if loaderEnabled were set, keyCharacterFilter is INFORMATIONAL (no reader — consumer-map row keyCharacterFilter), so wiring Thrawn there would not steer anything today.
- Expected Rando failure modes:
  - DEPLOY/MOVE: no fact that Thrawn must be AT a battleground during a deploy phase, nor that the flip window is deploy-only. No location bias (locationFragments empty → no +150/+200/+136 toward Lothal battlegrounds). Rando may leave Thrawn parked off-battleground.
  - PULL/DEPLOY (counter): no stacked-counter awareness → no drive to accumulate 2+ artwork on the Collection.
  - FORCE-LOSS / PRESERVATION: Thrawn and the Collection are absent from requiredCardsOnTable → V21 protection (CSE:4178+) shields neither. Losing either is catastrophic (Thrawn loss = flip-back both routes; Collection loss = flip law dead).
  - BATTLE/back study: no awareness that studying to zero-stack outside battle flips back (route A). No AI concept of route-A battle suppression.

## CROSS-PHASE OBLIGATIONS
- FRONT: keep Thrawn alive and parked AT a battleground during deploy phases; accumulate 2+ stacked artwork; protect the Collection.
- BACK: protect Thrawn (his loss flips back even during battle); do NOT study the artwork stack to zero unless a battle is live (route A is battle-suppressed) — i.e. spend the last artwork only inside battle.

## MINIMAL BEHAVIORAL-TEST PATTERN
1. `AGreatTacticianObjectiveEngineContractTest` — real-card: `flipsToBackWhenThrawnAtBattlegroundAndTwoArtworkStackedDuringDeploy()`, `doesNotFlipOutsideDeployPhase()`, `flipBackRouteAFiresAtZeroStackExceptDuringBattle()`, `flipBackRouteBFiresWhenThrawnLostEvenMidBattle()`.
2. `...RequiredEffectsBehaviorTest` — `thrawnAndCollectionAreBothFlipCriticalForBothBots()`, `oneStackedArtworkIsNearMissNotProgress()`.
3. `...RetentionBehaviorTest` — `thrawnRetentionUrgentBecauseLossFlipsBackEvenInBattle()`, `collectionRetentionUrgentWhileFlipPending()`, `doesNotStudyLastArtworkOutsideBattle()`.
4. `...CombinedEvaluatorDecisionTest` — `movingThrawnToBattlegroundDuringDeployBeatsDistractor()`, `feedingSecondArtworkBeatsUnrelatedDeploy()` (both bots).

## FIX SKETCH (AI-only)
- Two NEW facts, neither owned by an existing rule: (a) counter-host+threshold (Collection, need 2) — same shape as 12_89/210_25, home = step-3b hook (ObjectiveAnalyzer.java:8064); (b) flip-actor-at-battleground-during-deploy presence fact for Thrawn — closest existing owner is the V67ak key-character-in-objective-text rule (rules.json:3099 "deploy first to enable flip") which is text-parse driven; extend that concept rather than mint a parallel.
- BOUNDARY/PRESERVATION: wire Thrawn printings + Collection bp into requiredCardsOnTable (V21 update-in-place, CSE:4178) — cheapest correctness win.
- The route-A "don't study to zero outside battle" is an ActionText/battle-timing consumer, separate from the analyzer facts.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD). Codex must prove the deploy-window + Thrawn-presence + 2-stack flip and both flip-back routes, then decide fact vs consumer for counter + presence; min win = preserve Thrawn + Collection. Not started.
