# 601_146 Watch Your Step (V) — behavior dossier

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. Legacy (block-4, hideFromDeckBuilder). NO PROFILE exists (absent from objective_playbooks.json AND both seed facts files). UNWIRED. Lowest priority of the family; documented here for completeness, plus a code-vs-text landmine.

## SOURCE LAW (record re-verified vs Card601_146.java / _BACK.java at HEAD; deltas noted)
- flip-to-back: `isTableChanged` AND `canBeFlipped` AND `occupies(playerId, Filters.Corellia_system)` AND `controlsWith(playerId, 2, Filters.and(Filters.Corellia_site, Filters.battleground), INCLUDE_EXCLUDED_FROM_BATTLE, Filters.Corellian)`. Card601_146.java:127-147. VERIFIED — mixes OCCUPY (Corellia system) + CONTROL-WITH (2 Corellia battleground sites, each with a Corellian).
- flip-back-to-front: `isTableChanged` AND `canBeFlipped` AND NOT `occupies(playerId, 2, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.battleground)` — generic battlegrounds, NOT Corellia-specific. Card601_146_BACK.java:114-132. VERIFIED.
- terminal: none. VERIFIED.
- CODE-VS-TEXT LANDMINE (record confirmed, re-verified): the printed back text's "(unless you have completed two Kessel Runs)" exception is NOT coded — no Kessel Run check exists in either file. The printed back's smuggler-generation/battle-destiny/interrupt-from-Lost-Pile/Sense-Alter-KYEO effects are NOT implemented either; the code implements Corellia-specific modifiers instead. Also both files carry an unimplemented TODO (front:97, BACK:62) for the extra Corellia deploy cost. TRUTH = code. Do NOT encode Kessel Run logic for this card.

## KEY IDENTITIES
- Blueprints: front 601_146, back 601_146_BACK. Setup: Corellia system + unpiloted freighter + Corellian pilot + <>Spaceport City to Corellia.
- Flip actor: `Filters.Corellian`. Flip gate = occupy Corellia system AND control 2 `Filters.and(Filters.Corellia_site, Filters.battleground)` with a Corellian. Hold-back: occupy any 2 `Filters.battleground` (generic).
- Front pull: once/turn 1 Force → a <> (DIAMOND_1) site from Reserve Deck. Back pull: BoShek or a Corellian of ability < 3.

## RUNTIME TODAY (UNWIRED, no profile)
- NO profile in objective_playbooks.json for 601_146 → `findProfile` returns nothing → text-parser/static path only, and no identity flag exists (no `isWatchYourStep`/`isCorellia`). Consumer-map §2 (missing profile = empty registry hit = parser stands).
- Generic V21/V22/V136 reach nothing useful (no named flip locations populated for a control-with law over a filter). Setup deploys are engine-forced.
- Expected failure modes: same shape as 10_26 but Corellia-scoped — Corellians treated as generic bodies; no push to control 2 Corellia battleground sites with Corellians while occupying Corellia system; no Move-hold on 2 generic battlegrounds. Neither the front control-with route nor the back occupy-hold is modeled.

## CROSS-PHASE OBLIGATIONS
- Accumulate: occupy Corellia system AND control 2 Corellia battleground sites each with a Corellian. Hold: occupy any 2 battlegrounds (generic). Budget: front <>-site pull to build Corellia footprint; Corellians of ability <3 / BoShek via back pull.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder)
1. `noProfileExistsAndParserPathStandsForBothFacades` — profile contract (assert absent from registry; parser/static path only) + parity.
2. `structuredFrontAndBackRulesUseOnlyExactCorellianControlAndGenericOccupyConditions` — engine contract; ASSERT NO Kessel Run branch exists (guards the code-vs-text landmine).
3. `corellianControlRouteWinsOverNonCorellianDistractorAtCorelliaBattlegroundSite` — candidate vs distractor.
4. `controlAdvanceIsExactToCorellianAtTwoCorelliaBattlegroundSitesPlusCorelliaSystemOccupy` — near-miss rejection (Corellia system occupied but only 1 controlled Corellia site → not yet).
5. `genericTwoBattlegroundOccupyIsPreservedAsTheBackHoldNotCorelliaSpecific` — sequence/preservation; pins the Corellia-control-to-flip vs any-battleground-occupy-to-hold asymmetry.
6. `controllingSecondCorelliaBattlegroundSiteWithCorellianWhileOccupyingCorelliaSystemFlips` — flip trigger, engine contract.
7. `watchYourStepVDecisionsAreIdenticalAcrossRandoAndChosenone` — parity.

## FIX SKETCH (AI-only, update-in-place)
- Prerequisite (structural, not a code tag): CREATE a profile for 601_146 — it has none. No existing owner tag to update.
- The front control-with route maps onto actorLocationRules(scoreRole=actorToSite, actor=Corellian, site=Corellia_site+battleground, count=2) PLUS an occupy(Corellia_system) predicate — the occupy-system leg has no clean actorToSite analogue; note it. loaderEnabled:true would engage live V297/Move-hold/pull machinery for the control leg only.
- HARD CONSTRAINT: the profile MUST NOT encode a Kessel Run exception (unimplemented in code); mirror the DARK/legacy truth = code. Any Kessel Run modeling would diverge from the engine and mis-steer the AI.
- Legacy hideFromDeckBuilder → deprioritize vs the four live members; prove-or-defer is acceptable.
- NO implementation here.

## STATUS
Proof gate: SOURCE_VERIFIED (record) + runtime-gap confirmed (no profile, no identity, parser-only). Codex still must: decide whether a legacy hidden card warrants a profile at all; if yes, create one (control-with route via actorLocationRules, occupy-system leg noted, NO Kessel Run branch), write the 7-rung suite with the landmine-guard test, prove parity. Reasonable to DEFER behind the four live members.
