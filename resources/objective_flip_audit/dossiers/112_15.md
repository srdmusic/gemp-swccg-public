# 112_15 My Kind Of Scum — behavior dossier

Family D. Codex coverage c7fe91860..ee64e6f3b: NONE. DARK TWIN of 112_1 (Agents In The Court) — SAME flip law, build the primitive once and serve both. See 112_1.md for the shared reasoning; this dossier records the deltas.

## SOURCE LAW (record re-verified vs Card112_015.java / _BACK.java @HEAD; 0 deltas)
- flip-to-back (auto, isTableChanged): GUARD Rep!=null (145-148). anyOf: [`canSpotLocation(non_Tatooine_location)` → `occupies(playerId,3,battleground_site)` AND `occupiesWith(self,playerId,battleground_site, non_unique+alien+species(rep.species))`] OR [NOT non_Tatooine → `occupies(playerId,2,battleground_site)`] (155-161). OCCUPY, INCLUDE_EXCLUDED_FROM_BATTLE.
- flip-to-front (auto): `canBeFlipped` AND NOT `occupies(playerId,2,battleground_site)` (112_015_BACK.java:161-162). Same asymmetry — 3-site/species branch not re-checked on the back.
- terminal: none.

## KEY IDENTITIES (deltas vs 112_1)
- Front 112_15 / back 112_15_BACK, DARK. Rep = same `Filters.and(unique, alien, hasSpecies)` from Reserve Deck; species read dynamically from `rep.getBlueprint().getSpecies()`.
- Setup deploys differ (Desert Heart + Jabba's Palace site; opt Well Guarded) but are strategically irrelevant to the flip law. Gate class + branch selector identical to 112_1 (`battleground_site` / `non_Tatooine_location`).
- Seed note (recorded): the 3-site branch is `occupies(3,bg)` AND separately `occupiesWith(bg, rep-species alien)` with NO count on the occupiesWith — the species alien needs to be at only ONE occupied site.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. Same gaps as 112_1: no chosen-state Rep/species fact, no branch selector, no count representation.

## CLASSIFICATION: NEW_PRIMITIVE (chosen-state species fact) — shared with 112_1
- Identical to 112_1: the chosen-state Rep-species dynamic occupyWith is the primitive; the count/occupy legs + anyOf branch + global-spot selector are the Family-A flipLocationRules engine + `subLegs` extension + the minor global-spot leg.
- flip-back = negated occupy-count (occupy self battleground_site <2), engine-native.
- Building 112_1 delivers 112_15 for free (twin) — the only differences are side (DARK), setup cards, and the DARK back-side engine (destiny +2/+3 buffs), none of which touch the flip law.

## RULE OWNERS
- Same as 112_1: chosen-state fact = new step-3b STATE hook (OA:8064); flipLocationRules on a new loaderEnabled profile; `subLegs` (Family A); requiredCardsOnTable = the Rep.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `MyKindOfScumObjectiveEngineContractTest`: `occupyThreeBattlegroundSitesWithRepSpeciesAlienWhileNonTatooineOnTableFlips()`, `occupyTwoBattlegroundSitesWithNoNonTatooineLocationFlips()`, `threeSitesButNoRepSpeciesAlienDoesNotFlip()`, `repUnsetMeansObjectiveNeverFlips()`, `dropBelowTwoOccupiedBattlegroundSitesFlipsBack()`.
2. `MyKindOfScumObjectiveBehaviorTest` (both bots): `chosenRepSpeciesIsClassifiedAsDynamicFlipActor()`, `nonTatooineLocationRaisesFlipBarToThreeSitesPlusSpeciesAlien()`, `sharesChosenStateSpeciesFactImplementationWithAgentsInTheCourt()`.
3. `MyKindOfScumDecisionsAreIdenticalAcrossRandoAndChosenone()`.

## CROSS-PHASE OBLIGATIONS
- Same as 112_1: pick a Rep with in-deck non-unique species aliens; occupy 2 (or 3 + species alien) battleground sites; hold 2 to prevent flip-back; feed species aliens via the back retrieval engine.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas). Codex: co-build with 112_1 (one chosen-state species primitive, one flipLocationRules author pattern, twin profiles). Not started.
