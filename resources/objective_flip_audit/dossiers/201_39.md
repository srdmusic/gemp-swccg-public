# 201_39 Imperial Entanglements behavior dossier

Family: counted Tatooine control gate with a strict relative-count flip-back.

## Source law

- The front flips when its owner controls at least three Tatooine sites and the opponent controls fewer than three. The system does not count, and no character subtype is required.
- The back flips to the front only when the opponent controls strictly more Tatooine sites. Equal counts hold the back.
- The objective's native action deploys a legal Tatooine battleground site. Its permanent restriction prevents Jabba's Palace sites, systems, non-Imperial characters, and non-Imperial starships from becoming fabricated routes.
- Devastator and Tatooine system are setup assets, but the three controlled sites are the actual front gate.

Authoritative sources: `Card201_039.java` and `Card201_039_BACK.java`. The objective workbook and the public Imperial Entanglements primer informed route timing and restrained deployment. They are research, not behavioral proof.

## Shipped behavior

- The exact native objective action pulls only a currently legal Tatooine battleground site. Candidate scoring does not confuse the broader all-Tatooine-site flip law with the narrower printed download.
- Rando and Chosen One preserve the next route site and final body during real Force loss, use the native download before unrelated deployment, battle for a still-uncontrolled gate site, leave control behind, move a route actor to the new site, pull the last site on turn two, and deploy the final body there. The unchanged card trigger performs the flip.
- The final third-site deployment shares the narrow `DEPLOY.OBJECTIVE.THREE_SITE_COMPLETION +8000` contribution with MBO. It defeats the legacy low-ability ground penalty and ordinary Devastator alternative only when that exact destination completes the front gate. Hard safety vetoes remain dominant.
- On the back, the free site route is prioritized when the owner controls no more than one additional Tatooine site over the opponent. This builds a buffer before the strict relative count can reverse.
- The back-side parent action receives `+1800` and a legal child site receives `+300` only while that at-risk route is live. The exact physical objective source may bypass the tiny-Reserve veto at two cards; a wrong action, wrong source, same-blueprint counterfeit, safely-ahead board, or exhausted route receives no exception.
- Counted site presence, movement, Force-loss, battle-forfeit, and final-deploy calculations use the broad source-defined Tatooine-site class. The download legality check remains battleground-only, matching card Java.

## Behavioral proof

- `ImperialEntanglementsObjectiveEngineContractTest` covers the three-site flip, opponent-three near miss, tied back hold, strictly-more flip-back, exact free native download, Jabba's Palace exclusion, two-card V60 boundary, relative-count expansion, and the complete two-turn battle/move/deploy chain.
- The continuous route passes repeatedly with real engine decisions and an actual unchanged-card flip.
- Shared deploy, pull, Force-loss, forfeit, and analyzer regressions remain green. Rando and Chosen One return identical scores, reasoning, and winners.
- No engine or card Java changed. Deterministic unchanged-engine execution supplies `FLIP_OBSERVED`. A fresh live replay remains a separate proof gate.
- Local behavior commit `bd41a399b` is live in host/container-matching `web.jar` SHA-256 `3ea3c6542438a4d2f4617bbb7a58a4af876f103d75029efbf7986e9dff6cb3ba`. Deployment used a zero-table freeze, a fresh JVM, all five gameplay settings, and an empty reopened hall. The prior jar is preserved as `web.jar.pre-mbo-ie-bd41a399b`.

## Known limits

- Back-side top-X card selection and draw-phase trooper retrieval are payoff utilities, not part of this flip and retention seal.
- Live server branch firing and Steve's replay evaluation remain separate from the deterministic contract.
