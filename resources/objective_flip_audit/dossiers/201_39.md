# 201_39 Imperial Entanglements — behavior dossier

Family A: multi-location control gate (control-3 + opponent-<3) + strict flip-back. Codex coverage in c7fe91860..ee64e6f3b: NONE.

## SOURCE LAW (re-verified vs Card201_039.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && controls(self,3,Tatooine_site) && !controls(opponent,3,Tatooine_site)` (Card201_039.java:131-133). Plain CONTROL of 3 Tatooine SITES (system does not count), opponent controls FEWER than 3 Tatooine sites. No actor filter, no occupy.
- flip-to-front (allOf): `canBeFlipped && countTop(opponent Tatooine_site) > countTop(self Tatooine_site)` (Card201_039_BACK.java:143-145). Strict relative count; equal (incl. 0-0) holds the back.
- terminal/hard-lose: none. Devastator is a required setup deploy; both sides share once/turn Tatooine-battleground-site download.

## CLASSIFICATION: EXISTING_ENGINE_FIT (+1 registry key)
Same engine surface as 301_2. Recipe:
- `flipLocationRules[0]` = preFlip/flip, mode allOf, alt `{relation:control, controller:self, locationFilterKey:Tatooine_site, count:{">=",3}, includeExcludedFromBattle:true, opponentConstraint:{relation:control, controller:opponent, locationFilterKey:Tatooine_site, count:{"<",3}, includeExcludedFromBattle:true}}`. NB opponent-FEWER-than-3 = `opponentConstraint {"<",3}` (compareCounts supports "<" OA:3306); the record's `NOT controls(3)` is exactly `< 3`.
- `flipLocationRules[1]` = postFlip/flipBack, mode allOf, alt `{relation:control, controller:opponent, locationFilterKey:Tatooine_site, count:{">", referenceController:"self"}, includeExcludedFromBattle:true}`.
- `requiredCardsOnTable`: add Devastator printing(s) (V21 protection) — authoritative clear-then-set (OA:966-969).
- Registry: **ADD `Tatooine_site`** to `resolveLocationFilter` (only `Tatooine_location` present today → `Filters.Tatooine_site`).

## RULE OWNERS to update in place
- `ObjectiveAnalyzer.resolveLocationFilter` (registry ~OA:1061-1096 region): add `Tatooine_site` → `Filters.Tatooine_site` (fail-closed today, warn-on-unknown — designed for this).
- `objective_playbooks.json` Imperial Entanglements profile: author `flipLocationRules` (ABSENT) + `requiredCardsOnTable`. loaderEnabled already true. No other engine edit.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `ImperialEntanglementsObjectiveEngineContractTest`: `flipsWhenControlThreeTatooineSitesAndOpponentControlsFewerThanThree()`, `doesNotFlipWhenOpponentAlsoControlsThreeTatooineSites()` (opponent-<3 near-miss), `flipsBackWhenOpponentControlsMoreTatooineSitesThanYou()`, `staysFlippedOnTiedSiteCount()`.
2. `ImperialEntanglementsCombinedEvaluatorDecisionTest` (both bots): `deployToThirdTatooineSiteBeatsDistractor()`, `deployThatOnlyReachesTwoSitesScoredBelowGate()` (near-miss), `devastatorNeverPitchedWhileFlipPending()`.
3. `ImperialEntanglementsDecisionPolicyTest`: `postFlipRiskArmsAsOpponentTatooineSiteCountApproachesYours()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: accumulate 3 controlled Tatooine SITES (battleground download is the engine); keep opponent under 3.
- RETENTION: Devastator + Tatooine sites are flip-critical → wire to requiredCardsOnTable so V21 (CSE:4178+) shields them.
- MOVE/BATTLE: post-flip hold parity on Tatooine sites (strict comparator; deficit flips back).
