# 7_299 ISB Operations — behavior dossier

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. BUT the objective is substantially WIRED by the older V25 text-parser + V29.7 deploy strategy — the best-covered non-senator member of the family. Profile is inert; the parser carries it.

## SOURCE LAW (record re-verified vs Card7_299.java / _BACK.java at HEAD; deltas noted)
- flip-to-back: `canBeFlipped` AND ( `canSpot(>=4, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.ISB_agent)` OR `controlsWith(playerId, 2, Filters.Rebel_Base_location, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.ISB_agent)` ). Required singleton on isTableChanged. Card7_299.java:69-88. VERIFIED.
- flip-back-to-front: `canBeFlipped` AND NOT `canSpot(>=1, Filters.ISB_agent)` — zero agents flips back. Card7_299_BACK.java:81-97. VERIFIED.
- terminal: none. VERIFIED.
- ISB-agent status is objective-granted: both sides add Keyword.ISB_AGENT + Keyword.SPY to your characters with "ISB"/"Rebel"/"Rebels"/"Rebellion" in lore (Card7_299.java:58-66, _BACK.java:69-74). Filters.ISB_agent keys off Keyword.ISB_AGENT. VERIFIED.
- Delta from record: none. Record's dual-route + absence-flip-back semantics confirmed.

## KEY IDENTITIES
- Blueprints: front 7_299, back 7_299_BACK. Setup: any Coruscant location (Filters.Coruscant_location).
- Flip actor: `Filters.ISB_agent` (both sides). Route A gate = pure on-table count 4, NO location. Route B gate = control 2 `Filters.Rebel_Base_location` each with an ISB agent there. Hold-back: >= 1 ISB agent anywhere.

## RUNTIME TODAY (WIRED via parser, not profile)
- Profile `loaderEnabled` ABSENT → profile INERT (all fields dead at runtime). Consumer-map §2.
- Text parser (V25) sets identity + facts from the flip-condition text regardless of profile: `isISBOperations`=true when cond contains "isb agent" (OA:9890-9892); `isbFlipAgentCount`=4 (OA:9899-9901); `isbFlipLocationCount`=2 (OA:9910); `isbFlipBackNoAgents` (OA:9739-9740). Accessors getISBFlipAgentCount/getISBFlipLocationCount/isbFlipBackRequiresAgents.
- Live count fact: `countISBAgentsOnTable(gameState, playerId)` (OA:9673-9686) via `isISBAgentByLore` (OA:9660-9668, CHARACTER + lore isb/rebel/rebellion).
- Live deploy consumer: V29.7 ISB Operations strategy, CardSelectionEvaluator:3153-3210+ (rando + chosenone), gated `isAnalyzed() && isISBOperations()`; routes through `DeployObjectiveSitingPolicy.evaluateIsbAgent` (rules.json owner V29.7-ISB). Pre-flip: ISB agents get priority, higher-ability preferred, non-ISB penalized; battleground-site awareness for the back drain.
- Deltas the AI approximates (note, not necessarily fix): (a) `countISBAgentsOnTable` filters `playerId`-owned only; flip law counts all ISB agents on table — functionally equivalent since the keyword is granted only to your side. (b) `isISBAgentByLore` uses lore substrings ("rebel" already covers "rebels"); parity with the objective grant filter's four terms. (c) Route B (control 2 Rebel Base locations) — parser stores `isbFlipLocationCount`=2 but VERIFY the V29.7 strategy actually rewards controlling Rebel Base locations with agents, not only the 4-count route.

## CROSS-PHASE OBLIGATIONS
- Accumulate 4 ISB agents on table (route A) OR control 2 Rebel Base locations with agents (route B). Hold >= 1 agent or flip back (do-not-lose-your-last-agent). Deploy budgets ISB-lore characters as flip currency; higher ability for control.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder)
1. `parserFactsCarryIsbIdentityAndCountsWhileProfileStaysInertForBothFacades` — profile contract (assert inert) + parser facts (4 / 2 / flip-back-no-agents) + parity.
2. `structuredFrontAndBackRulesUseOnlyExactIsbAgentConditions` — engine contract (4-on-table OR control-2-RebelBase; zero-agents flips back).
3. `isbAgentDeployWinsOverNonAgentAndDuplicatesDoNotDoubleCount` — candidate vs distractor (V29.7 priority).
4. `agentCountAdvanceIsExactToUnmetThresholdOfFourAndRouteBControlOfTwo` — near-miss rejection (3 agents / 1 controlled base → not yet).
5. `lastIsbAgentIsProtectedAsTheHoldingThatKeepsTheBack` — sequence/preservation (flip-back-no-agents).
6. `objectiveDeploysFourthIsbAgentAndFlips` and `controllingSecondRebelBaseWithAgentFlips` — flip trigger, both routes, engine contract.
7. `isbDecisionsAreIdenticalAcrossRandoAndChosenone` — parity (CSE:3159 both packages).

## FIX SKETCH (AI-only, update-in-place)
- Owner exists: V29.7-ISB (rules.json) + V25 parser. No new tags.
- If rung 6 route-B proves under-rewarded: extend V29.7-ISB (via DeployObjectiveSitingPolicy.evaluateIsbAgent) to reward deploying/holding an ISB agent at a controllable Rebel Base location using the already-parsed `isbFlipLocationCount`, rather than only the 4-count. Update the existing owner in place.
- If rung 5 hold proves inadequate: model "keep >= 1 ISB agent" as a force-loss/forfeit protection reusing the existing objective force-loss protection surface (CSE V21 family) keyed on isISBOperations — no new tag.
- NO implementation here.

## STATUS
Proof gate: SOURCE_VERIFIED + runtime WIRED (V25 parser + V29.7-ISB deploy strategy live in both facades). Codex still must: write the 7-rung suite, confirm route-B (control-2-RebelBase) reward exists, confirm the last-agent hold is protected, and prove parity. Lowest-risk of the non-senator family.
