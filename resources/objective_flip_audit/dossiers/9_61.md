# 9_61 There Is Good In Him — behavior dossier

Family: captive / actor-state gate (LIGHT). Codex coverage in c7fe91860..ee64e6f3b: NONE. Capture-law twin of DARK 9_151. INVERTS normal preservation: the LIGHT owner WANTS its own Luke captured.

## SOURCE LAW (re-verified vs Card9_061.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped(self) && canSpot(INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE, Filters.Luke AND Filters.captive)` (Card9_061.java:151-153). Pure captured-STATE; no control/occupy/location.
- capture MECHANISM (front required trigger): if Luke at a site and a capture-capable Imperial `atSameSite(luke)` + `canEscortCaptive`, Luke is captured with seizure, OPPONENT chooses the escort (Card9_061.java:124-148). This is what produces the flip state — the owner's own Luke gets taken.
- flip-back: `!canSpot(Filters.Luke AND (captive OR presentWith(self, INCLUDE_EXCLUDED_FROM_BATTLE, Vader)))` (Card9_061_BACK.java:74-75). Luke free & away from Vader (or off table) → flips to front.
- terminal/hard-lose: NONE. Back Force clock: end of each opponent turn, `!canSpot(Vader escorting Luke)` → opponent loses 2 Force (BACK:60-68). Back WIN: once/turn, Vader `presentWith` Luke (INCLUDE_CAPTIVE), destiny; `getCrossoverAttemptTotal(vader,total) > 14` → Vader crosses to Light + opponent depleted (BACK:116-164). SEED DELTA CONFIRMED: seed calls this WIN route 'hardLose' — category error.

## KEY IDENTITIES
- Front 9_61 / back 9_61_BACK. Flip subject `Filters.Luke`; back co-actor `Filters.Vader`. States `Filters.captive` / `Filters.presentWith(self, INCLUDE_EXCLUDED_FROM_BATTLE, Vader)`.
- Setup START OF GAME (all required, front): Chief Chirpa's Hut; `Filters.and(Icon.DEATH_STAR_II, Filters.Luke)` → Chief Chirpa's Hut; Luke's Lightsaber; Endor: Landing Platform; I Feel The Conflict (Card9_061.java:56-90).
- Capture actor filter `Filters.and(Imperial, atSameSite(luke), canEscortCaptive(luke,true))`. Back transfer route: Imperial(except Vader) escorting Luke → transfer to Vader (BACK:90-113).
- Owner BANS (front, remainder): may not play Alter / Strangle / Captive Fury (:95-101). Buffs: +2 Force gen at Luke's site (:110); while Imperial at Landing Platform, owner may not drain / generates no Force at Luke's location (:111-112).

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → matched but fully INERT (consumer-map §2). All ACTIVE-eligible fields empty/null → ZERO consumers. `objectiveNamedLocations` (Chief Chirpa's Hut, Landing Platform, Luke's site) DEAD (not Gson-bound).
- No identity flag ("there is good in him" not in the OA:315-331 set) → generic evaluators + V21/V25/V186 parsers only.
- No captive/escort strategy in `models/**` (grep clean). DOUBLE failure: (a) generic LIGHT preservation logic will actively AVOID letting the owner's Luke be captured — the exact OPPOSITE of the objective's flip route; (b) nothing knows Alter/Strangle/Captive Fury are banned. Generic `KeywordWeight("capture")` is DARK-flavored and won't help a Light owner want capture.
- Expected failures by phase: DEPLOY/MOVE — Rando keeps Luke safe & armed (correct in general, catastrophic here); never parks him where a capturing Imperial can take him. BATTLE — treats opponent Imperials near Luke as threats to flee, not as the flip enabler. FORCE-LOSS/PULL — back clock (opp -2 unless Vader escorts) and crossover win unmodeled. PRESERVE — inverts: the "protect Luke" reflex blocks the win.

## CROSS-PHASE OBLIGATIONS
- INITIATE (counter-intuitive): position the owner's Luke so a capture-capable Imperial takes him (flip), i.e. LET Luke be captured — the objective's entire premise.
- PRESERVE the flipped state: keep Luke captive OR present with Vader every table-change; steer toward Vader `presentWith` Luke for the destiny>14 crossover win.
- ABSTAIN: never play Alter / Strangle / Captive Fury (banned). Hold Force at Luke's site for the +2 gen and drain locks.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `ThereIsGoodInHimObjectiveEngineContractTest`: `capturableImperialAtLukesSiteCapturesAndFlipsToBack()`, `freeingLukeAwayFromVaderFlipsBackToFront()`, `bannedAlterStrangleCaptiveFuryAreNotOfferedAsPlays()` (near-miss).
2. `ThereIsGoodInHimObjectiveBehaviorTest` (both bots): `ownLukeCaptureIsClassifiedAsFlipProgressNotAThreat()` (the inversion), `vaderPresentWithLukeRegistersCrossoverWinReadiness()`, `lukeFreeAndAwayFromVaderRegistersFlipBackExposure()`.
3. `ThereIsGoodInHimRetentionBehaviorTest`: `capturedLukeIsPreservedNotRescued()`, `forceLossNeverPitchesVader()`.
4. `ThereIsGoodInHimCombinedEvaluatorDecisionTest` (both bots): `parkingLukeForCaptureBeatsFleeingWithLuke()`, `passingAlterBeatsPlayingIt()`.
5. `ThereIsGoodInHim*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No existing captive rule owner (registry search clean; only `BattleForfeitPolicy`, `ForceLossPolicy` V74 tangential). The `actorLocationRules`/`actorToSite` shape models actor→control-site and cannot express "own actor becomes captive." NEW fact + consumer (step-3b hook, OA:8064).
- FACT: capture-INVERSION fact for this objective (subject Luke; desired end-state = Luke captive / present with Vader). BOUNDARY: enable `loaderEnabled`; wire Luke + Vader into `requiredCardsOnTable` so retention machinery keeps them on table — but the fix must SUPPRESS the generic "protect my character" penalty when the objective wants Luke captured (the dominated-old-rule trap: a preservation rule must not veto the intended capture). Do the sign/magnitude math at the boundary.
- CONSUMER: an objective-aware sign flip on Luke-endangerment + a ban-enforcer for Alter/Strangle/Captive Fury. Mirror `ObjectiveHardLossPolicy` for terminal guards. Confirm the banned plays don't already get a generic bonus that dominates the veto.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD; seed hardLose→WIN delta noted). Codex must: (1) engine-contract test proving own-Luke-capture flip + free-away flip-back, (2) resolve the preservation INVERSION (hardest part — generic protect-my-guy logic fights this objective), (3) wire Luke+Vader retention + Alter/Strangle/Captive Fury ban across both bots. Not started.
