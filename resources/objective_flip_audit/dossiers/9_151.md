# 9_151 Bring Him Before Me — behavior dossier

Family: captive / actor-state gate (DARK). Codex coverage in c7fe91860..ee64e6f3b: NONE (his commits = Invasion, regional, We Have A Plan, Hunt Down, Endor). LIGHT twin of the same capture law = 9_61 (There Is Good In Him).

## SOURCE LAW (re-verified vs Card9_151.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped(self) && canSpot(INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE, target AND Filters.captive)` where target = Luke (default) / Leia / Kanan by `ModifyGameTextType` retarget (Card9_151.java:373-375; Leia branch :343, Kanan branch :359). Card-STATE check: capture by ANY means suffices, capturer identity NOT read.
- flip-back: `!canSpot(target AND (captive OR presentWith(self, INCLUDE_EXCLUDED_FROM_BATTLE, Vader)))` (Card9_151_BACK.java:119-120). Target free AND away from Vader (or off table) → flips to front.
- terminal/hard-lose: NONE either side. Back: required LOSE 1 Force at end of each of YOUR turns (BACK:69-77). Back WIN route: once/turn with Vader+target+Emperor all present at Throne Room, duel; crossover total >12 → target crosses + opponent Life Force depleted (BACK:136-510).
- auto-capture MECHANISM (front required trigger): Vader `presentWith` non-captive target, not leaving, not escorting → `CaptureWithSeizureEffect` (Card9_151.java:106-199). Vader is the capture engine, NOT part of the flip condition. SEED DELTA CONFIRMED: seed's flip `byWhom=Vader` is WRONG (record seedMismatches).

## KEY IDENTITIES
- Front 9_151 / back 9_151_BACK. Target persona `Filters.Luke` default; `Filters.Leia` / `Filters.Kanan` via retarget (Kanan set by deploying "Kanan, Rebel Infiltrator" through the objective's Luke download).
- Actor `Filters.Vader` (capturer + `MayNotBeTransferredModifier` escort, :200-208). Duel gate: target + Emperor (`Persona.SIDIOUS` download) present at `Filters.Throne_Room` + `not(frozenCaptive)`.
- States: `Filters.captive`; `Filters.presentWith(self, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.Vader)`.
- Setup deploys (front:62-82): Throne Room, Insignificant Rebellion, Your Destiny. Ban: Scanning Crew may not be played (:94-96); opponent out-of-play cards may not target the retarget persona (:97-105).

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → `findProfile` matches it but it is fully INERT (consumer-map §2: no hydration; V21/V22.2/V25/V160/V186 parsers + statics stand). Every ACTIVE-eligible field (locationFragments [], requiredCardsOnTable [], flipGateSite null, flipGateCardIds [], weights {}) reaches ZERO consumers. `objectiveNamedLocations` (Throne Room) present but DEAD (not Gson-bound, consumer-map §3 row `objectiveNamedLocations`).
- No identity flag: title "bring him before me" is not invasion/my lord/endor/tdigwatt/i want that map (OA:315-331), so NO compiled playbook and NO V83..V193 arms fire — only generic evaluators + parsers.
- No capture/escort/captive strategy exists anywhere in `models/**` (grep clean). Sole capture-adjacent lever = generic `KeywordWeight("capture", 45)`/`(40)` (RandoCalAi.java:202/236, TheChosenOneAi same) — a blind action-text nudge; not objective-aware, doesn't know capturing Luke is the REQUIRED flip route or which persona is the target.
- Expected Rando failures by phase: DEPLOY — no bias to bring Vader onto the free target's site; no `flipConditionLocationFragments` → no +150/+200 toward Throne Room. PULL — Emperor/Throne-Room-package not prioritized. BATTLE/CAPTURE — no drive to trigger the auto-capture; may leave target free & away from Vader (flip-back) or never capture at all. FORCE-LOSS — back's 1/turn upkeep handled only generically (ForceLossPolicy V74, MAINTENANCE_* :20-23). PRESERVE — Vader-off-target not flagged as flip-back exposure.

## CROSS-PHASE OBLIGATIONS
- INITIATE: capture the target — get Vader present with the free target so auto-capture fires (or capture by any means). Deploy the Throne Room + Emperor package for the duel win.
- PRESERVE: keep the target captive OR present with Vader every table-change (else flip-back); keep Vader on the target's site.
- BUDGET: 1 Force/turn upkeep on the back; reserve for the once/turn Throne Room duel.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `BringHimBeforeMeObjectiveEngineContractTest` (VirtualTableScenario + StartingSetup, both real cards): `vaderPresentWithFreeLukeAutoCapturesAndFlipsToBack()`, `capturedLukeByAnyMeansFlipsWithoutVader()` (proves capturer-identity-agnostic), `freeingLukeAwayFromVaderFlipsBackToFront()`, `retargetKananViaLukeDownloadRedirectsFlipActor()` (near-miss: non-target captive must NOT flip).
2. `BringHimBeforeMeObjectiveBehaviorTest` (parametrized rando+chosenone): `captiveTargetIsClassifiedAsLastFlipTriggerActorForBothBots()`, `nonTargetCaptiveDoesNotRegisterFlipProgress()`, `vaderOffTargetSiteRegistersFlipBackExposure()`.
3. `BringHimBeforeMeRetentionBehaviorTest`: `targetCaptiveRetentionIsUrgentWhileBackIsUp()`, `forceLossNeverPitchesVaderTheSoleEscort()`.
4. `BringHimBeforeMeCombinedEvaluatorDecisionTest` (both bots): `movingVaderOntoFreeLukeBeatsUnrelatedDrainMove()`, `deployEmperorToThroneRoomBeatsDistractorDeploy()`.
5. `BringHimBeforeMe*SourceParityTest`: rando↔chosenone byte-parity on any new fact/consumer.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No existing captive/capture rule owner in the 695-rule registry (rules.json search: only tangential `BattleForfeitPolicy` BATTLE_FORFEIT "dead-card persona", `ForceLossPolicy` V74 maintenance). The profile schema's `actorLocationRules` (scoreRole `actorToSite`, the ONLY live captive-adjacent shape) models actor→CONTROL-site, and CANNOT express capture-STATE. This is a NEW fact + consumer, home reserved at OA:8064 ("STATE keys ... need their own runtime hooks (step 3b)").
- FACT: add a capture-flip-actor fact (target persona = Luke/Leia/Kanan per retarget; capturer = Vader). BOUNDARY: enable `loaderEnabled` and wire target persona + Vader into `requiredCardsOnTable` so V21 never-pitch (CSE:4178+) and Codex's `RequiredCardRetentionRisk` machinery shield them for free.
- CONSUMER: a pre-flip capture-drive score (move Vader→free target / prioritize capture actions) + a flip-back-exposure penalty when target is free & away from Vader. Reuse `ObjectiveHardLossPolicy` shape for terminal guards. Do the boundary math vs the generic `KeywordWeight("capture")` +45 and V136/V21 magnitudes before coding.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD, seed byWhom=Vader delta noted). Codex must: (1) write the engine-contract test proving the real capture-state flip incl. retarget branches, (2) decide fact-vs-consumer for capture awareness (nothing exists), (3) at minimum wire target+Vader into requiredCardsOnTable for preservation parity across both bots. Not started.
