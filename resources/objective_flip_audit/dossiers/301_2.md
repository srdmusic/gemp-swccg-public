# 301_2 City In The Clouds — behavior dossier

Family A: multi-location control gate + opponent-zero + strict flip-back. Codex coverage in c7fe91860..ee64e6f3b: NONE. **Purest Zero-Hour-shaped fit in the family; zero registry adds.**

## SOURCE LAW (re-verified vs Card301_002.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && controls(self,2,Cloud_City_battleground_site) && occupies(self,Bespin_system) && !controls(opponent,Cloud_City_site)` (Card301_002.java:102-105). Control 2 CC battleground sites, OCCUPY Bespin system (presence only), opponent controls ZERO CC sites.
- flip-to-front (allOf): `canBeFlipped && countTop(opponent-controlled Cloud_City_site) > countTop(self-controlled Cloud_City_site)` (Card301_002_BACK.java:148-150). Strict relative count; ties hold the back.
- terminal/hard-lose: none either side. Front CC-battleground download is front-only (lost after flip).

## CLASSIFICATION: EXISTING_ENGINE_FIT
The post-Endor engine (`isFlipLocationAlternativeSatisfied` OA:3933; `opponentConstraintSatisfied` OA:4277; `compareCounts` referenceController OA:3959-3968) expresses every leg. Recipe:
- `flipLocationRules[0]` = preFlip/flip, mode **allOf**, alternatives:
  - `{relation:control, controller:self, locationFilterKey:Cloud_City_battleground_site, count:{">=",2}, includeExcludedFromBattle:true, opponentConstraint:{relation:control, controller:opponent, locationFilterKey:Cloud_City_site, count:{"==",0}, includeExcludedFromBattle:true}}`
  - `{relation:occupy, controller:self, locationFilterKey:Bespin_system, count:{">=",1}, includeExcludedFromBattle:true}`
- `flipLocationRules[1]` = postFlip/flipBack, mode allOf, alt `{relation:control, controller:opponent, locationFilterKey:Cloud_City_site, count:{comparator:">", referenceController:"self"}, includeExcludedFromBattle:true, scoreRole:stayFlipped}`
- Control-vs-occupy split = two flat legs, different `relation`, in one allOf. Opponent-zero = `opponentConstraint {"==",0}`. Strict flip-back = `referenceController:self` + `">"` (Zero Hour precedent, proven live).
- Registry: **NONE** — Cloud_City_battleground_site, Bespin_system, Cloud_City_site all in `resolveLocationFilter` today. loaderEnabled already true.

## RULE OWNERS to update in place
- `objective_playbooks.json` City In The Clouds profile: author `flipLocationRules` (currently ABSENT), keep loaderEnabled=true. No engine edit.
- Consumers already wired: locationFragments +150/+200 (`getLocationObjectiveBonus` OA:343-362), `assessPostFlipLocationRisk` (ME:1855), `assessFlipLocationRules` states — no new owner.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `CityInTheCloudsObjectiveEngineContractTest` (unchanged card/engine, VirtualTableScenario/StartingSetup): `flipsWhenTwoCcBattlegroundControlledPlusBespinOccupiedAndOpponentHasZeroCcSites()`, `doesNotFlipWhileOpponentControlsAnyCcSite()` (opponent-zero near-miss), `flipsBackWhenOpponentControlsMoreCcSitesThanYou()`, `staysFlippedOnTiedCcSiteCount()`.
2. `CityInTheCloudsCombinedEvaluatorDecisionTest` (both bots): `deployToSecondCcBattlegroundBeatsDistractor()` (candidate choice), `occupyingBespinSystemScoredAsFlipProgress()`, `deployThatLeavesOpponentOneCcSiteScoredBelowGate()` (near-miss).
3. `CityInTheCloudsDecisionPolicyTest`: `postFlipRiskArmsWhenOpponentCcSiteCountApproachesYours()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: pile onto CC battleground sites + establish Bespin-system presence; evict opponent from every CC site (opponent-zero is the hardest leg).
- MOVE/BATTLE: post-flip, never let opponent out-control you on CC sites (strict comparator — parity is safe, deficit flips back).
- PULL: front-only CC-battleground download is the flip engine; exhaust it before flipping.
