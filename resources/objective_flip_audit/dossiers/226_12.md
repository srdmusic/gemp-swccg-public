# 226_12 This Deal Is Getting Worse All The Time (V) — behavior dossier

Family A: multi-location control gate (control-3 + opponent-<3) + strict flip-back. Codex coverage in c7fe91860..ee64e6f3b: NONE. **Only Family A profile with loaderEnabled OFF (fully inert today).**

## SOURCE LAW (re-verified vs Card226_012.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && controls(self,3,Bespin_location) && !controls(opponent,3,Bespin_location)` (Card226_012.java:133-135). Control 3 Bespin LOCATIONS (sites, sectors, or system — the broad `Bespin_location` class), opponent controls FEWER than 3. INCLUDE_EXCLUDED_FROM_BATTLE both legs.
- flip-to-front (allOf): `canBeFlipped && countTop(opponent Bespin_location) > countTop(self Bespin_location)` (Card226_012_BACK.java:136-138). Strict relative count; ties hold the back.
- terminal/hard-lose: none. Lando/Lobot/Vader power the back toolkit but do NOT gate the flip law (pure location control).

## CLASSIFICATION: EXISTING_ENGINE_FIT (+1 registry key + loaderEnabled)
Identical shape to 201_39, only the counted class differs (`Bespin_location`). Recipe:
- `flipLocationRules[0]` = preFlip/flip, allOf, alt `{relation:control, controller:self, locationFilterKey:Bespin_location, count:{">=",3}, includeExcludedFromBattle:true, opponentConstraint:{relation:control, controller:opponent, locationFilterKey:Bespin_location, count:{"<",3}, includeExcludedFromBattle:true}}`.
- `flipLocationRules[1]` = postFlip/flipBack, allOf, alt `{relation:control, controller:opponent, locationFilterKey:Bespin_location, count:{">", referenceController:"self"}, includeExcludedFromBattle:true}`.
- **Set `loaderEnabled:true`** (currently absent → matched but inert, consumer-map §2). Add locationFragments `["bespin","cloud city"]` for the +150/+200 bonus.
- Registry: **ADD `Bespin_location`** → `Filters.Bespin_location` (only `Bespin_system` present today).

## RULE OWNERS to update in place
- `ObjectiveAnalyzer.resolveLocationFilter`: add `Bespin_location` (also needed by 109_4 — dedup).
- `objective_playbooks.json` This Deal (V) profile: flip loaderEnabled=true, author `flipLocationRules`, populate empty locationFragments. Required start deploys (Cloud City battleground site, [Cloud City] I'm Sorry) → requiredCardsOnTable if retention matters. No engine edit.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `ThisDealVObjectiveEngineContractTest`: `flipsWhenControlThreeBespinLocationsAndOpponentFewerThanThree()`, `doesNotFlipWhenOpponentAlsoControlsThree()` (near-miss), `flipsBackWhenOpponentControlsMoreBespinLocationsThanYou()`, `staysFlippedOnTie()`.
2. `ThisDealVCombinedEvaluatorDecisionTest` (both bots): `deployToThirdBespinLocationBeatsDistractor()`, `deployReachingOnlyTwoScoredBelowGate()` (near-miss), `profileInertUntilLoaderEnabled()` (regression: asserts nothing steers while loaderEnabled false).
3. `ThisDealVDecisionPolicyTest`: `postFlipRiskArmsAsOpponentBespinCountApproaches()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: control 3 Bespin locations (once/turn Bespin upload feeds the board); keep opponent under 3.
- MOVE/BATTLE: post-flip hold parity on Bespin locations (strict comparator).
- REGRESSION GUARD: flipping loaderEnabled on is the activation switch; without it the whole profile is dead (consumer-map §2).
