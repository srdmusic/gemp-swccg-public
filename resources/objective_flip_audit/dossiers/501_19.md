# 501_19 More Systems Will Rally To Our Cause — behavior dossier

Family E (playtest, NON-CANONICAL). Set 501 PLAYTESTING (`ExpansionSet.PLAYTESTING`, `setTestingText`), absent from card_cache and every pool/deck resource. Codex coverage in c7fe91860..ee64e6f3b: NONE. **CLASSIFICATION: EXISTING_ENGINE_FIT** for the flip law (card-on-table count, mirrors Hunt Down back's onTable-count leg) + one trivial `resolveFilter` key; full strategic enablement (occupy engine, 3-system setup) is SMALL_SHARED_EXTENSION beyond the flip.

## SOURCE LAW (re-verified vs Card501_019.java / _BACK @HEAD; matches record, 0 deltas)
- flip-to-back (Card501_019.java:124-144): required singleton trigger on `isTableChanged` + `canBeFlipped` + `canSpot(game, self, 2, Filters.and(Icon.SEPARATIST, Filters.system))` — pure CARD-ON-TABLE COUNT: any two spottable [Separatist] systems (no control, no occupy, no ownership filter; converted opponent systems count if spottable).
- flip-to-front (Card501_019_BACK.java:117-137): required trigger on `isTableChanged` + `canBeFlipped` + `NOT canSpot(...2 Separatist systems)` — exact negation.
- No place-out-of-play on either side.
- OBSERVED CARD DEFECT (engine territory, not AI): BACK:163-168 convert-raise trigger has leftover `System.out.println` debug lines and dereferences `oldLocation.getTitle()` before the null check — potential NPE. Record it; do not fix from AI.

## KEY IDENTITIES
- Front 501_19 / back 501_19_BACK. Flip predicate: `Filters.and(Icon.SEPARATIST, Filters.system)` counted ≥2. No site/actor/location gate.
- Back scoring engine (NOT the flip law): occupy-system-plus-related-site — `countTopLocationsOnTable(Filters.and(Filters.system, Filters.occupies(playerId), Filters.relatedSystemTo(self, and(site, occupies(playerId)))))` drives opponent Force loss X; drain -1 and Separatist-starship attrition immunity at systems related to occupied sites.
- Setup (front:50-81): deploy [Separatist] Geonosis system, TWO [Clone Army] systems, Droid Racks. Shared pull (both sides): once/turn deploy a battleground site related to your Separatist/Clone Army system (or Separatist Command Center by title).
- Both sides ban: non-[Episode I] characters/starships/vehicles.

## RUNTIME TODAY
- Profile: does not exist. No identity flag → generic evaluators + parsers only. The V25 text parser does NOT trigger here (no "vader"/"battleground"/occupy/control keywords in the flip condition — it is a bare system count).
- The flipLocationRules engine (Codex dc3d996fc) already supports an `onTable` alternative with a `count` comparator (Hunt Down back uses `relation:onTable, count ==0, actorFilterKey:Vader`). "Two Separatist systems on table" is the same shape: `relation:onTable, count >=2, actorFilterKey:<separatist_system>`. But `resolveFilter` has NO Separatist-system key (only actor keys like Vader/Jedi_or_Luke); a one-line case returning `Filters.and(Icon.SEPARATIST, Filters.system)` is needed.
- Expected failures by phase: DEPLOY — no drive toward two Separatist systems / no protection of them from conversion. CONTROL — the occupy-system+related-site Force-loss engine has no analyzer fact, so Rando under-plays the back's whole point. PULL — the related-battleground-site download gets no bias (`locationFragments` empty).

## CROSS-PHASE OBLIGATIONS
- INITIATE: get and keep two [Separatist] systems on table (Geonosis + downloaded battleground sites' related systems); protect them from conversion/blow-away — losing one flips back.
- EXPLOIT (back): occupy systems whose related site you also occupy to bleed opponent Force; park Separatist starships at those systems (attrition-immune).
- ABSTAIN: no non-[Episode I] characters/starships/vehicles.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `MoreSystemsObjectiveEngineContractTest`: `twoSeparatistSystemsOnTableFlipsToBack()`, `droppingBelowTwoFlipsToFront()` (near-miss: exactly one system does NOT flip).
2. `MoreSystemsObjectiveBehaviorTest` (both bots): `separatistSystemCountIsClassifiedAsFlipGate()`, `occupySystemPlusRelatedSiteRegistersAsControlEngine()`.
3. `MoreSystemsCombinedEvaluatorDecisionTest` (both bots): `deployingSecondSeparatistSystemBeatsUnrelatedDeploy()`, `protectingASeparatistSystemOutranksTrivialDrain()`.
4. `MoreSystems*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- FLIP: author a profile; express the flip via a flipLocationRules `onTable` alternative (`count >=2`) + add a `separatist_system` case to `resolveFilter` (trivial, mirrors Jedi_or_Luke pattern) and its flip-back negation. That alone makes the flip law engine-visible — EXISTING_ENGINE_FIT.
- STRATEGY (beyond flip, SMALL_SHARED_EXTENSION): to actually PLAY it, wire the two Separatist systems into `requiredCardsOnTable` (protection) + the related-battleground-site into `locationFragments`, and give the occupy-system+related-site Force-loss engine a consumer — none exists.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD, 0 deltas). Flip law EXISTING_ENGINE_FIT (+1 filter key); strategy enablement SMALL_SHARED_EXTENSION. No profile, non-canonical playtest. Not started; recommend out-of-scope until canonical.
