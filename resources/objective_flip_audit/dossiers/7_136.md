# 7_136 Hidden Base — behavior dossier

Family D (dynamic/chosen-state gates & oddballs). Codex coverage c7fe91860..ee64e6f3b: NONE. Distinctive: flip gate is a persistent DEPLOY-COUNT tracker (systems the owner justDeployed), and the flip itself is an OPTIONAL top-level action once the tracker clears. Back is a hard-lose (probe), never flips.

## SOURCE LAW (record re-verified vs Card7_136.java / _BACK.java @HEAD; citations exact, 0 deltas)
- flip-to-back (OPTIONAL): top-level Flip action enabled once `self.getWhileInPlayData()==null` AND `canBeFlipped` (Card7_136.java:94-95). The while-in-play tracker (114-131, spot-checked) starts SET; on every `justDeployed(playerId, Filters.system)` it records the deployed system's title IF it is a `battleground_system` OR equals the stacked hidden-base indicator title; when `systemTitlesMap.size()>=6 || (isHiddenBaseBattleground && size>=5)` it `setWhileInPlayData(null)` → enables the flip. Net: deploy hidden-base indicator system + 5 battleground systems.
- flip-to-front: NEVER. Back has no FlipCardEffect.
- terminal/hard-lose (back): objective placed OUT OF PLAY when `systemProbed` fires on a system whose title == the stacked hidden-base indicator title (Card7_136_BACK.java:150-201); opponent probes systems it OCCUPIES.

## KEY IDENTITIES
- Front 7_136 / back 7_136_BACK, LIGHT. No control/occupy in the flip law — pure deploy counter.
- Tracker actor: `Filters.system` (deploy trigger), classified via `Filters.battleground_system` or title==hidden-base indicator. Hidden-base indicator = a `planet_system` parsec 1-8 stacked face-down from OUTSIDE deck at setup.
- Setup: deploy Rendezvous Point (`Filters.Rendezvous_Point`) + stack 1 planet-system parsec 1-8 as indicator (7_136.java:54,61). Front once/deploy-phase: deploy 1 `Filters.system` from Reserve Deck (78-89) — the counter's engine.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT (consumer-map §2). flipGateSite null, requiredCardsOnTable/pullableCards/locationFragments empty, weights {}. No identity flag (`isHiddenBase` absent, OA:138-170).
- The deploy-count tracker is invisible to the AI: nothing counts battleground-systems-deployed toward the threshold, nothing prioritizes the once/deploy-phase `Filters.system` pull toward battleground systems, nothing values the optional flip when ready.
- Probe hard-lose unmodeled: no awareness that opponent occupying the (hidden-title) system + probing = instant loss.

## CLASSIFICATION: NEW_PRIMITIVE (objective progress COUNTER + optional-flip-when-ready)
- Primitive 1 — deployed-battleground-system counter fact: mirror the "objective progress counter" primitive named in dossier 10_26 (completed-Utinni-Effect count) but source = `justDeployed(battleground_system)` + the stacked-indicator title; threshold 6 (or 5 if indicator is battleground). Reuses `ObjectiveProgressCandidateRole` scaffolding (OA:319-331). This counter is a NET-NEW event source; consolidate it with 10_26 + 7_138 under one counter fact.
- Primitive 2 — optional-flip-when-ready policy: the flip is a top-level action with NO cost, so the policy is degenerate ("flip ASAP once counter clears"); shares the voluntary-flip decision hook with 211_36/225_53 but without their cost math.
- Drive: once/deploy-phase `Filters.system` pull steered to battleground systems (a pullableCards-equivalent). Protect the stacked indicator's occupation (probe defense) — probabilistic since its title is hidden.

## RULE OWNERS (update-in-place / new)
- No existing owner. Counter fact = new step-3b hook (OA:8064 reserves STATE-key home; ObjectiveProgressCandidateRole enum already present OA:319). Reuse 10_26's counter design.
- Min correctness wire: enable `loaderEnabled` + populate `locationFragments` with the deployed battleground systems for a weak +150/+200 deploy bias; the true counter needs the new fact (locationFragments alone cannot count).

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `HiddenBaseObjectiveEngineContractTest`: `deployingFifthBattlegroundSystemPlusIndicatorClearsTrackerAndEnablesOptionalFlip()`, `fourBattlegroundSystemsDoesNotEnableFlip()` (near-miss), `nonBattlegroundNonIndicatorSystemDoesNotAdvanceCounter()`, `probingHiddenBaseSystemPlacesObjectiveOutOfPlay()`.
2. `HiddenBaseObjectiveBehaviorTest` (both bots): `deployedBattlegroundSystemCountIsClassifiedAsFlipProgress()`, `oncePerDeployPhaseSystemPullIsSteeredToBattlegroundSystems()`, `opponentOccupyingHiddenBaseSystemRegistersProbeHardLossExposure()`.
3. `HiddenBaseDecisionsAreIdenticalAcrossRandoAndChosenone()` — parity.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: use the once/deploy-phase pull each turn to add a battleground system; accumulate 5 + the indicator. Take the optional flip the moment the tracker clears.
- PRESERVE (post-flip): keep opponent from occupying the hidden-base system (probe hard-lose); note the back forbids YOU deploying systems (MayNotDeploy), so pre-flip you must have all systems out.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas; tracker logic spot-checked at 7_136.java:109-136). Codex must: design the deployed-system counter fact (share with 10_26/7_138), wire the optional flip, model the probe hard-lose. Not started.
