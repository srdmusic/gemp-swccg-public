# 213_32 Shadow Collective — behavior dossier

Family: hit-EVENT trigger + controlsWith(gangster) gate + END-OF-TURN oscillation. Codex coverage in c7fe91860..ee64e6f3b: NONE. Unique in my family: the back is a one-turn buff window that auto-flips-back every turn; the primary flip engine is an EVENT (hitting characters), which the AI has no concept for.

## SOURCE LAW (re-verified vs Card213_032.java / _BACK.java @HEAD, lines 120-148 spot-confirmed)
- flip-to-back (two independent routes, actions list @213_032.java:120-148):
  - Route A (EVENT): `justHitBy(effectResult, Filters.character, Filters.your(playerId))` — a character was just 'hit' by YOUR card. Any character, any location. NOTE: NO canBeFlipped guard on this route (confirmed @HEAD line 137).
  - Route B (STATE): `isTableChanged && isDuringYourPhase(BATTLE) && canBeFlipped(self) && controlsWith(2, Filters.battleground, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.gangster)` — your battle phase, control 2 battlegrounds with a gangster at each.
- flip-to-front: `isEndOfEachTurn(effectResult)` — UNCONDITIONAL end-of-every-turn (no canBeFlipped guard, _BACK.java:80). The objective OSCILLATES; the back persists at most one turn.
- Flip riders: front `cardFlipped` + `occupies(3, Filters.battleground)` → opponent loses 1 Force (fires when FRONT re-activates, i.e. on the end-of-turn flip-back; 213_032.java:110-119). Back `cardFlipped` → optional recirculate + reshuffle Reserve Deck (_BACK.java:53-73).
- terminal/hard-lose: none.

## KEY IDENTITIES
- Front bp 213_32 / back 213_32_BACK. DARK.
- Route A engine: your card 'hits' any `Filters.character` — driven by the non-unique blasters + gangsters this deck downloads (SHADOW_COLLECTIVE download: non-unique blaster OR "First Light"-title card, both sides, once/turn).
- Route B: `Filters.gangster` controlling 2 `Filters.battleground`; Force-loss rider uses plain `occupies(3, battleground)`.
- Setup (213_032.java:61-77): deploy Dathomir: Maul's Chambers (213_23); if Massassi Throne Room (1_138) spotted, may deploy [Set 13] Maul (0-1) to Maul's Chambers.

## RUNTIME TODAY
- Profile loaderEnabled ABSENT → INERT (consumer-map §2). All fields empty/null → zero consumers. objectiveNamedLocations DEAD.
- Expected Rando failure modes:
  - Route A totally invisible: grep shows the AI has NO `justHitBy` / hit-EVENT flip concept; its "hit" logic is all weapon-firing economics (ActionTextEvaluator V36/V51 fire-vs-already-hit), never "hitting a character advances my objective flip". Rando will not value firing a blaster at a character as flip progress.
  - Route B: no `controlsWith(gangster)` counter fact (the analyzer never reads controlsWith except the Invasion actorLocationRules DTO). No awareness the gate is battle-phase-only. `Filters.gangster` unrepresented (keyCharacterFilter null, and INFORMATIONAL even if set).
  - PULL: pullableCards empty despite the recurring non-unique-blaster / First-Light download that arms Route A; no drive to pull them.
  - OSCILLATION: no model of the end-of-turn auto flip-back — the correct strategy (re-arm the flip EVERY turn to re-trigger the buff + the occupy-3 Force-loss rider) is entirely unmodeled.

## CROSS-PHASE OBLIGATIONS
- Every turn: re-trigger the flip (Route A by hitting a character with your blaster, or Route B by holding 2 gangster-controlled battlegrounds in your battle phase), because the back auto-reverts at end of turn.
- Maintain `occupies(3, battleground)` so each flip-back fires the 1-Force drain rider.
- Keep pulling non-unique blasters / gangsters to sustain Route A.

## MINIMAL BEHAVIORAL-TEST PATTERN
1. `ShadowCollectiveObjectiveEngineContractTest` — real-card: `hittingACharacterWithYourCardFlipsToBackRouteA()`, `twoGangsterControlledBattlegroundsInYourBattlePhaseFlipRouteB()`, `flipsBackToFrontUnconditionallyAtEndOfEachTurn()`, `occupyingThreeBattlegroundsDrainsOnFlipBack()`.
2. `...RequiredEffectsBehaviorTest` — `gangsterAndNonUniqueBlasterAreFlipEnablersForBothBots()`, `oneGangsterControlledBattlegroundIsNearMissForRouteB()`.
3. `...BattleDecisionTest` — `firingBlasterAtCharacterIsValuedAsFlipProgressRouteA()` (this is the load-bearing new behavior).
4. `...CombinedEvaluatorDecisionTest` — `rearmingFlipEachTurnBeatsPassiveHold()`, `holdingThreeBattlegroundsToSustainDrainBeatsDistractor()` (both bots).

## FIX SKETCH (AI-only)
- Route A needs a NEW event-trigger concept the analyzer entirely lacks; nearest owner is battle/ActionText fire logic (V36/V51 in ActionTextEvaluator) — extend a "hit advances objective flip" signal there rather than a new subsystem, but only when the objective identity is Shadow Collective (guard by identity like V86/V121 Invasion arms). NO existing rule owns "hit = flip progress".
- Route B needs a controlsWith(gangster)-counter fact — same missing-primitive as 203_19's control legs; there is no controlsWith reader outside the Invasion DTO.
- Oscillation: a per-turn "re-arm the buff" bias; no existing owner. This is the riskiest to score — do boundary math so a re-arm nudge never dominates real battle economics.
- Given loaderEnabled:false and empty facts, the honest minimum is a step-3b design note, not a quick wire; there is no single card to protect (no counter host).

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD; Route A no-canBeFlipped-guard and unconditional end-of-turn flip-back both confirmed). Codex must prove both flip routes + the oscillation + the occupy-3 rider, then decide how much (if any) of the hit-EVENT / oscillation to model vs leave to the engine. Highest design-uncertainty objective in my family. Not started.
