# 10_26 Watch Your Step — behavior dossier

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. UNWIRED at runtime: no identity flag, inert profile, empty locationFragments. Distinctive: one flip route is a persistent COUNTER (completed Kessel Runs), not bodies on table.

## SOURCE LAW (record re-verified vs Card10_026.java / _BACK.java at HEAD; deltas noted)
- flip-to-back: `canBeFlipped` AND ( `hasCompletedUtinniEffect(playerId, 2, Filters.Kessel_Run)` OR `occupiesWith(playerId, 2, Filters.battleground, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.smuggler)` ). Card10_026.java:127-146. VERIFIED.
- flip-back-to-front: `canBeFlipped` AND NOT `hasCompletedUtinniEffect(2, Kessel_Run)` AND NOT `occupies(playerId, 2, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.battleground)`. Card10_026_BACK.java:129-148. VERIFIED.
- terminal: none. VERIFIED.
- CRITICAL asymmetry (record confirmed): FRONT route B = occupy 2 battlegrounds WITH a smuggler at each; BACK is held by PLAIN occupy of 2 battlegrounds (no smuggler needed) OR the 2-Kessel-Run counter. Once the counter hits 2 it holds both flip and back-hold forever.
- Delta from record: none.

## KEY IDENTITIES
- Blueprints: front 10_26, back 10_26_BACK. Setup: Cantina (Filters.Cantina, convertibility-guarded), Docking Bay 94 (Filters.Docking_Bay_94), Tatooine system (Filters.Tatooine_system). Once/turn front pull: Corellia OR Kessel system into hand. Back once/turn: play an Interrupt from Lost Pile.
- Flip actor: `Filters.smuggler` (route B). Route A gate = counter `Filters.Kessel_Run` completed 2x (no bodies). Route B gate = occupy 2 `Filters.battleground` with a smuggler. Hold-back: counter=2 OR plain-occupy 2 battlegrounds.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. locationFragments empty, flipGateSite null, keyChar/keySite null, weights {}, pullableCards empty. Nothing reaches a consumer. Consumer-map §2/§3.
- No identity flag (no `isWatchYourStep`; see OA:138-170 list). No objective-specific arm fires.
- Generic V21/V22/V136 reach nothing (empty fragments; route A has no location; route B "battleground" is a filter). Setup deploys (Cantina/DB94/Tatooine) are engine-forced, not AI-steered.
- The Kessel Run route is invisible to the AI: nothing tracks `hasCompletedUtinniEffect(Kessel_Run)` progress. The once/turn Corellia/Kessel pull (which feeds Kessel Run play) is not prioritized toward the objective.
- Expected failure modes: Deploy — smugglers treated as generic bodies; no push to occupy 2 battlegrounds WITH smugglers; no build toward completing 2 Kessel Runs. Move — no obligation to hold occupation of 2 battlegrounds; drifts back for free. Neither flip route modeled.

## CROSS-PHASE OBLIGATIONS
- Accumulate: EITHER complete 2 Kessel Runs (persistent counter — the durable win, immune to board wipes) OR occupy 2 battlegrounds with smugglers. Hold: plain-occupy 2 battlegrounds OR the counter. Budget: use the once/turn Corellia/Kessel pull to enable Kessel Runs; keep smugglers parked at 2 battlegrounds for route B.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder)
1. `profileIsInertAndNoWatchYourStepIdentityExistsForBothFacades` — profile contract (assert inert) + parity.
2. `structuredFrontAndBackRulesUseOnlyExactKesselRunCounterAndSmugglerOccupyConditions` — engine contract (2-Kessel OR occupy-2-with-smuggler; flip back only when NOT counter AND NOT plain-occupy-2).
3. `smugglerOccupyRouteWinsOverNonSmugglerDistractorAtBattleground` — candidate vs distractor.
4. `occupyAdvanceIsExactToSmugglerAtTwoBattlegroundsAndCounterAdvanceExactToTwoKesselRuns` — near-miss rejection (1 smuggler-occupied battleground / 1 Kessel Run → not yet).
5. `plainOccupyOfTwoBattlegroundsIsPreservedAsTheWeakerBackHoldAndCounterHoldsForever` — sequence/preservation; pins the with-smuggler-vs-plain asymmetry and the persistent-counter durability.
6. `completingSecondKesselRunFlips` and `occupyingSecondBattlegroundWithSmugglerFlips` — flip trigger, both routes, engine contract.
7. `watchYourStepDecisionsAreIdenticalAcrossRandoAndChosenone` — parity.

## FIX SKETCH (AI-only, update-in-place)
- No existing owner tag. Route B (occupy 2 battleground with smuggler) maps onto the actorLocationRules(scoreRole=actorToSite, actor=smuggler, site=battleground, count=2) machinery + loaderEnabled:true — engages live V297 formation/Move-hold/pull with no new code (only runtime-read route rule, consumer-map §3a). Note the occupy-with (not control-with) relation — verify actorToSite semantics match occupy before relying on it.
- Route A (2 completed Kessel Runs) has NO body/location analogue; it is a Utinni-Effect COUNTER. There is no existing AI fact for completed-Utinni-Effect progress. Smallest honest change: a shared "objective progress counter" fact (completed Utinni Effect of type X, threshold N) that also serves any future counter objective; drive the once/turn Corellia/Kessel pull toward it. Flag as a new primitive, not a Watch-Your-Step-only arm.
- NO implementation here.

## STATUS
Proof gate: SOURCE_VERIFIED + runtime-gap confirmed (no identity, inert profile, empty fragments, no counter tracking). Codex still must: decide loaderEnabled + actorLocationRules for route B, design the completed-Utinni-Effect counter primitive for route A, encode the with-smuggler-vs-plain-occupy asymmetry, write the 7-rung suite, prove parity.
