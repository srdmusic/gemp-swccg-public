# 216_11 On The Verge Of Greatness — behavior dossier

Family D. Codex coverage c7fe91860..ee64e6f3b: NONE, BUT the ORBIT half of the flip law is the BEST-wired positioning logic in the whole audit: V79 (MoveVergePolicy) shepherds the Death Star from parsec 4 → orbit Scarif, and V79b hard-vetoes leaving orbit post-flip. The actor-at-Scarif-site half and the back hard-lose are unwired.

## SOURCE LAW (record re-verified vs Card216_011.java / _BACK.java @HEAD; flip law + back hard-lose spot-checked, 0 deltas)
- flip-to-back (auto, isTableChanged): `canBeFlipped` AND `canSpot(INCLUDE_EXCLUDED, at(Scarif_battleground_site) AND (Krennic OR Tarkin))` AND `canSpot(Death_Star_system AND isOrbiting(Title.Scarif))` (216_011.java:119-122). Persona AT a Scarif battleground SITE + Death Star system ORBIT-STATE.
- flip-to-front (auto, singleton): `canBeFlipped` AND NOT `canSpot(INCLUDE_EXCLUDED, your leader AT Scarif_battleground_site)` (216_011_BACK.java:171-173). Any leader holds it (Krennic/Tarkin count).
- terminal/hard-lose (back, checked FIRST, stronger): objective OUT OF PLAY if NOT `canSpot(Death_Star_system)` OR NOT `canSpot(Shield_Gate)` (216_011_BACK.java:155-168; plain canSpot, no override, no canBeFlipped gate).

## KEY IDENTITIES
- Front 216_11 / back 216_11_BACK, DARK. Flip actors `Filters.Krennic` (209_36) / `Filters.Tarkin` (102_11, 204_45); back flip-back actor = any `your leader`. Gate = `Filters.Scarif_battleground_site`. Orbit leg = `Filters.and(Death_Star_system, isOrbiting(Title.Scarif))`. Hard-lose cards: `Death_Star_system`, `Shield_Gate` (216_18, a DEVICE). Setup deploys [Set16] Death Star system + Scarif system + Citadel Tower + Shield Gate.

## RUNTIME TODAY (ORBIT WIRED, rest UNWIRED)
- Title-string driven (NO `isVerge` flag): MoveEvaluator:725-808 matches "on the verge of greatness" + "death star" title strings. MoveVergePolicy branches: ORBIT_SCARIF +1500, PARSEC_SEVEN +1200, ONE_HOP +1000, TOWARD_SCARIF +700, DEFAULT_MOVE +500, WRONG_DIRECTION -300, POST_FLIP_HOLD hard-veto (leaving orbit un-satisfies the flip), PRE_FLIP_HOLD. Orbit state read via `cardToMove.getSystemOrbited()` (the exact primitive the flip uses). DeployEvaluator:242-309 reserves 1 Force for the Move phase while Verge active + DS not orbiting Scarif.
- UNWIRED: nothing drives (a) deploying a Scarif battleground site, (b) getting Krennic/Tarkin (or any leader) TO a Scarif battleground site — the SECOND flip leg; nothing protects Death Star system / Shield Gate (back hard-lose); nothing models the leader-at-Scarif flip-back exposure. Profile `loaderEnabled` ABSENT → INERT.

## CLASSIFICATION: SMALL_SHARED_EXTENSION (orbit half EXISTING via V79; add actorToSite + requiredCardsOnTable)
- Reuse (EXISTING): the V79/MoveVergePolicy orbit drive + V79b flip-back guard fully own the "Death Star orbiting Scarif" leg and the post-flip stay-in-orbit obligation. No new code there.
- Extension 1 — actorToSite (Family B machinery): author `actorLocationRules(scoreRole=actorToSite, actor=Krennic|Tarkin, site=Scarif_battleground_site, count=1)` on a new loaderEnabled profile → engages V297 formation plan / move-hold / +300 gate pull to garrison the persona at a Scarif battleground site. ADD `resolveLocationFilter` key `Scarif_battleground_site` (Family C registered `Scarif_location`; battleground variant likely still missing) and `resolveFilter` actor keys Krennic/Tarkin. The flip-back leg (any your leader at Scarif bg site) is the same site with a broader `leader` actor.
- Extension 2 — requiredCardsOnTable = Death Star system + Shield Gate → V21 never-pitch + hard-loss-defense (reuse ObjectiveHardLossPolicy shape) so the back doesn't self-destruct.
- The two-fact AND (persona-at-site AND orbit-state) is a conjunction across the actorToSite fact and V79's orbit read — coordinate them, but no new orbit primitive (getSystemOrbited already read).

## RULE OWNERS (update-in-place / new)
- OWNER (orbit) = MoveVergePolicy + MoveEvaluator:725-808 + DeployEvaluator:242-309 — PROVE, don't change. Consider replacing title-string detection with an `isVerge` identity flag (parity with isTdigwatt) as a cleanup, not required.
- actorToSite = author profile rule (findFlipGateActorRule OA:7990 reads actorFilterKey+locationFilterKey+actorToSite); V297 formation DPP:753-862, move-hold ME:753-819, pull CSE:280-282 all fire once the rule exists. requiredCardsOnTable owner = OA:966-969 authoritative set.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `VergeOfGreatnessObjectiveEngineContractTest`: `krennicAtScarifBattlegroundSitePlusDeathStarOrbitingScarifFlips()`, `personaAtScarifSiteButDeathStarNotOrbitingDoesNotFlip()` (orbit near-miss), `deathStarOrbitingButNoLeaderPersonaAtScarifSiteDoesNotFlip()` (actor near-miss), `noLeaderAtScarifBattlegroundSiteFlipsBack()`, `deathStarSystemOrShieldGateLeavingTablePlacesObjectiveOutOfPlay()`.
2. `VergeOfGreatnessObjectiveBehaviorTest` (both bots): `deathStarMoveTowardScarifIsScoredByOrbitBranches()` (pin V79), `postFlipLeavingScarifOrbitIsHardVetoed()` (pin V79b), `krennicOrTarkinAtScarifSiteIsClassifiedAsSecondFlipLeg()`, `deathStarSystemAndShieldGateRegisterHardLossProtection()`.
3. `VergeOfGreatnessDecisionsAreIdenticalAcrossRandoAndChosenone()`.

## CROSS-PHASE OBLIGATIONS
- MOVE: shepherd the Death Star to orbit Scarif (V79); never leave orbit post-flip (V79b). DEPLOY: put out a Scarif battleground site; garrison Krennic/Tarkin (a leader) there. PRESERVE: keep a leader at a Scarif battleground site (flip-back); NEVER let Death Star system or Shield Gate leave the table (hard-lose).

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas; flip law 119-122 + hard-lose 155-183 spot-checked). Codex: PROVE the V79 orbit stack, then add the actorToSite Scarif-bg leg + requiredCardsOnTable hard-lose protection. Best-wired MOVE dimension in Family D. Not started.
