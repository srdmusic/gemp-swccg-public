# 109_4 Quiet Mining Colony — behavior dossier

Family A: opponent-zero + anchor-sector control + PERSONA-ALTERNATIVE count reduction (Lando/Lobot) + mandatory controlled-site floor + two-route flip-back + shared hard-loss.

## SOURCE LAW (re-verified vs Card109_004.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && !controls(opponent,Bespin_location) && controls(self,Bespin_Cloud_City) && ( controls(self,2,Cloud_City_site) || ( isOnCloudCity(Lando|Lobot) && controls(self,1,Cloud_City_site) ) )` (Card109_004.java:124-129). Opponent controls ZERO Bespin locations, you control the Bespin: Cloud City cloud sector, AND control 2 CC sites OR (Lando/Lobot on ANY CC site + control 1 CC site). `isOnCloudCity` = actor at any Cloud_City_site. The sector never satisfies the separate controlled-site floor.
- flip-to-front (anyOf): `canBeFlipped && ( controls(opponent,Bespin_system) || controls(opponent,3,{Cloud_City_site | Bespin_cloud_sector}) )` (Card109_004_BACK.java:106-108). Two routes.
- HARD-LOSS (BOTH sides): Bespin system blown away → objective out of play (Card109_004.java:111-120, _BACK.java:93-102).

## CLASSIFICATION: EXISTING_ENGINE_FIT
The exact law fits three pre-flip rules: sector plus opponent-zero, mandatory one-site control,
and an any-of between two controlled sites or Lando/Lobot at a Cloud City site. The Lando/Lobot
relation is `at`, controller `any`, not `controlWith`. The actor filter includes direct,
permanent-aboard, and permanent-weapon personas. Both flip-back routes fit one post-flip any-of.

- Runtime registry: `Bespin_location`, `Bespin_Cloud_City` (named sector),
  `Bespin_cloud_sector`, `Cloud_City_site_or_Bespin_cloud_sector`, `Cloud_City_site`, and
  `Lando_or_Lobot`.
- Runtime playbook: `objective_playbooks.json` carries three exact pre-flip rules and the
  post-flip any-of.
- Behavioral proof: native flip truth tables, parent pull/deploy/move/casualty consumers,
  source parity for both bots, and live play. The workbook and this dossier remain research.

## RULE OWNERS
- `ObjectiveAnalyzer` owns exact rule satisfaction, plain control/occupy progress, count-aware
  retention, and the permanent-persona Lando/Lobot filter.
- `objective_playbooks.json` owns the exact QMC Boolean facts.
- Rando and Chosen One adapters consume the shared facts symmetrically.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. Native QMC truth tables cover the two-site route, one-site-plus-Lando route, opponent-owned
   Lando, permanent-aboard Lando, sector negative, mandatory-site-floor negative, opponent
   blocker, and both flip-back routes.
2. Decision tests cover exact pull ordering, location-action exhaustion, plain presence deploy
   and movement, safe battle progress, Force-loss retention, and forfeit retention.
3. Source parity tests require Rando and Chosen One to consume the same shared facts.

## CROSS-PHASE OBLIGATIONS
- DEPLOY/MOVE: control Bespin: Cloud City + reach 2 CC sites, OR park Lando/Lobot on any CC site to drop the requirement to 1; keep opponent off ALL Bespin locations.
- POST-FLIP: deny opponent the Bespin system AND keep opponent under 3 CC sites/cloud sectors (two flip-back routes).
- PROTECT: Bespin system blow-away is a hard-loss on BOTH sides — guard it always.

---
CORRECTION 2026-08-03: The earlier correction was itself wrong. Bespin: Cloud City is a cloud
sector, so it does not subsume the mandatory one-controlled-site leg. The three-rule factoring
above is the exact source law. Probability that geography will apologize: 0%.

## SEALED PROOF 2026-08-03
Behavior commit `0ebcd0e40`; bounded gate `389/0/0/0`; independent PASS. The deployed jar
SHA-256 is `b3eb23dbd7648d38775e160f3c22fcc3acca219f23fae0d8f233be9adcd91081`.
Deployment used a zero-table freeze, fresh JVM, matching host/container bytes, five successful
gameplay-switch posts, clean startup, and an empty reopened hall. Live replay firing is pending.
