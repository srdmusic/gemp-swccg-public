# 109_4 Quiet Mining Colony — behavior dossier

Family A: opponent-zero + anchor-site control + PERSONA-ALTERNATIVE count reduction (Lando/Lobot) + two-route flip-back + shared hard-loss. Codex coverage in c7fe91860..ee64e6f3b: NONE. **Most complex gate in the family.**

## SOURCE LAW (re-verified vs Card109_004.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && !controls(opponent,Bespin_location) && controls(self,Bespin_Cloud_City) && ( controls(self,2,Cloud_City_site) || ( isOnCloudCity(Lando|Lobot) && controls(self,1,Cloud_City_site) ) )` (Card109_004.java:124-129). Opponent controls ZERO Bespin locations, you control the Bespin: Cloud City site, AND control 2 CC sites OR (Lando/Lobot on ANY CC site + control 1 CC site). `isOnCloudCity` = actor at any Cloud_City_site, NOT tied to the controlled site (record seedMismatch confirmed).
- flip-to-front (anyOf): `canBeFlipped && ( controls(opponent,Bespin_system) || controls(opponent,3,{Cloud_City_site | Bespin_cloud_sector}) )` (Card109_004_BACK.java:106-108). Two routes.
- HARD-LOSS (BOTH sides): Bespin system blown away → objective out of play (Card109_004.java:111-120, _BACK.java:93-102).

## CLASSIFICATION: SMALL_SHARED_EXTENSION (nested allOf-inside-anyOf) — same extension as 204_32
Opponent-zero, anchor-site control, and both flip-back routes are FIT. The persona-alternative reduced branch is `(Lando/Lobot present at a CC site) AND (control 1 CC site)` — a 2-leg conjunction inside an anyOf. The Lando/Lobot presence is DECOUPLED from the controlled site, so `controlWith` (actor-at-controlled-site) is a wrong narrowing — needs the nested-allOf sub-group.
- **EXTENSION:** the same `subLegs` (nested allOf) added for 204_32 unlocks this; the reduced branch = `{subLegs:[presentAt Lando|Lobot @ Cloud_City_site count>=1, control Cloud_City_site count>=1]}`. (Alternative: a presence-conditioned count reduction on `RuleCount` mirroring the existing `gameTextModification`/`modifiedValue` field — either lands it.)
- Recipe:
  - `flipLocationRules[0]` preFlip/flip, allOf: `[{relation:control, controller:self, locationFilterKey:Bespin_Cloud_City, count:{">=",1}, opponentConstraint:{control, opponent, Bespin_location, {"==",0}}}, {mode-anyOf via subLegs...}]` — the anyOf branch as a nested rule OR a second preFlip rule (multi-rule allOf) carrying `mode:anyOf` with `[ {control Cloud_City_site >=2}, {subLegs:[presentAt Lando|Lobot @ Cloud_City_site, control Cloud_City_site >=1]} ]`.
  - `flipLocationRules[back]` postFlip/flipBack, anyOf: `[{control opponent Bespin_system >=1}, {control opponent Cloud_City_site_or_cloud_sector >=3}]`.
- Registry: **ADD** `Bespin_location`, `Bespin_Cloud_City` (named site), `Bespin_cloud_sector`, `Cloud_City_site_or_Bespin_cloud_sector` composite (loc); **ADD `Lando`, `Lobot`** (actor). Cloud_City_site already present.

## RULE OWNERS to update in place
- `ObjectiveAnalyzer` DTO + `isFlipLocationAlternativeSatisfied`: the `subLegs` nested-allOf extension (shared with 204_32) — single owner, one addition.
- `ObjectiveAnalyzer.resolveLocationFilter`: add Bespin_location (shared w/226_12), Bespin_Cloud_City, Bespin_cloud_sector, the CC-site-or-cloud-sector composite.
- `ObjectiveAnalyzer.resolveFilter`: add `Lando`, `Lobot`.
- `objective_playbooks.json` Quiet Mining Colony profile: author `flipLocationRules` + optional shared `hardLossLocationRules` (Bespin system blown away). loaderEnabled already true.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `QuietMiningColonyObjectiveEngineContractTest`: `flipsViaTwoControlledCcSitesWithBespinCloudCityAndOpponentZero()`, `flipsViaOneCcSitePlusLandoOnAnyCcSite()` (reduced branch), `doesNotFlipWithOneCcSiteAndNoLandoLobot()` (persona near-miss), `flipsBackWhenOpponentControlsBespinSystem()`, `flipsBackWhenOpponentControlsThreeCcSitesOrCloudSectors()`.
2. `QuietMiningColonyCombinedEvaluatorDecisionTest` (both bots): `movingLandoOntoACloudCitySiteBeatsDistractor()` (persona reduces required count), `deployLeavingOpponentOneBespinLocationScoredBelowGate()` (opponent-zero near-miss).
3. `QuietMiningColonyDecisionPolicyTest`: `landoLobotPresenceRecognizedAsCountReduction()`, `bespinSystemRetentionUrgentBecauseBlowAwayIsHardLoss()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY/MOVE: control Bespin: Cloud City + reach 2 CC sites, OR park Lando/Lobot on any CC site to drop the requirement to 1; keep opponent off ALL Bespin locations.
- POST-FLIP: deny opponent the Bespin system AND keep opponent under 3 CC sites/cloud sectors (two flip-back routes).
- PROTECT: Bespin system blow-away is a hard-loss on BOTH sides — guard it always.

---
CORRECTION 2026-07-25 (deep packet): CLASSIFICATION upgraded SMALL_SHARED_EXTENSION -> EXISTING_ENGINE_FIT. subLegs is NOT needed: the Lando/Lobot reduced branch's "control >=1 CC site" conjunct is subsumed by the mandatory anchor leg (Bespin: Cloud City is itself a Cloud_City_site), so the branch is a plain presentAt alternative AND-ed via multi-rule allOf. Needs only 5 registry keys + profile rules. The gameTextModification fallback suggested above does NOT work (checks ModifyGameTextType, not actor presence). See mailbox packet.
