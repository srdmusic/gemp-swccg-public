# 222_27 The Empire Knows We're Here — behavior dossier

Family C (pure opponent-presence toggle, DEFENSIVE). Codex coverage c7fe91860..ee64e6f3b: NONE. No identity flag. Profile inert (loaderEnabled absent).
NOTE: card_cache.json entry '222_27' is STALE (says title "Alter"); identity is the objective per Card222_027.java + the record — do NOT "fix" the cache here.

## SOURCE LAW (re-verified vs Card222_027.java / _BACK @HEAD; spot-checked 128-148, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped && occupies(opponent, INCLUDE_EXCLUDED_FROM_BATTLE, your(playerId) AND Hoth_location)` (Card222_027.java:135-137). Opponent OCCUPIES a Hoth location YOU own.
- flip-back-to-front: same NEGATED — opponent does NOT occupy any of your Hoth locations (Card222_027_BACK.java:219-220). Toggles both ways with opponent presence.
- terminal: none. Front economy: +1 drain at Hoth system; once/control-phase occupy 2 battleground marker sites → retrieve 1 (Card222_027.java:81, 109-123). Back SELF-NERF: cancels your Admiral's Orders + your unique NON-trooper/gunner/pilot characters' game text (Card222_027_BACK.java:98-99) — flipping is UNDESIRABLE unless deck is troopers/gunners/pilots.

## WHO DRIVES THE FLIP
OPPONENT — flips when the Empire occupies your Hoth locations; reverts the instant they are cleared. Owner cannot force it and generally does NOT want the back (self-nerf); owner's lever is DENY.

## CLASSIFICATION: EXISTING_ENGINE_FIT (positioning/economy) + optional shared extension
- Gate location `your_Hoth_location` ALREADY resolves as a context-composite (OA:8194-8197, needs playerId) — plus `Hoth_location`/`Hoth_site`/`Second_Marker`/`Third_Marker` keys all present. Recipe: `loaderEnabled:true` + `locationFragments` = Hoth locations → +150/+200 bias to garrison your Hoth sites (deny occupation) AND to build the front's occupy-2-battleground-markers retrieval engine (owner-driven win on the FRONT).
- OPTIONAL SMALL_SHARED_EXTENSION (rank 2, shared 13_46/13_73): opponent-actor-presence-on-YOUR-locations fact so the AI reads flip exposure and defends. Unmodeled gap with no field today: "back is bad" (self-nerf) — the AI should PREFER staying on front; no weight encodes that. Flag, low priority.

## RULE OWNERS
- locationFragments consumers (getLocationObjectiveBonus, DPP/DE/CSE/ME) — live once loaderEnabled. Setup pulls engine-forced (Hoth system, Main Power Generators). Once/turn download (Echo Command Center / marker) already in card law.
- No actorToSite owner (flip actor is the opponent). No Hoth/defensive arm in models/**.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario)
- `opponentOccupyingYourHothLocationFlipsToBack()` — native flip fires (opponent presence on your Hoth site → real FlipCardEffect); `clearingOpponentFlipsBackToFront()` proves the toggle reverts.
- `deployingToGarrisonYourHothLocationsIsBiasedToDenyOccupation()` — decision: bot prefers reinforcing an owned Hoth site under threat over an equal neutral deploy.
- `opponentOccupyingANonHothOrUnownedLocationDoesNotFlip()` — near-miss: opponent occupies a Hoth location you do NOT own, or a non-Hoth location → no flip (ownership qualifier respected).

## CROSS-PHASE OBLIGATIONS
- DEFEND (opponent-driven): hold your Hoth locations to STAY on front; treat occupation of them as flip exposure to deny.
- FRONT ECONOMY: pursue occupy-2-battleground-marker-sites retrieval + the +1 Hoth-system drain — the owner's real win is on the front, not the back.
- DECK-SHAPE CAVEAT: back blanks your unique non-trooper/gunner/pilot text; do not treat the flip as progress unless deck is troopers/gunners/pilots.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas; cache stale, left as-is) + runtime gap confirmed. Codex must: enable loaderEnabled + wire your_Hoth_location/marker fragments; decide the shared opponent-presence fact; consider a "prefer-front" signal (novel, low priority); 3-rung suite; parity. Not started.
