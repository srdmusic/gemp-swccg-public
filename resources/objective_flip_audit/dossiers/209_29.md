# 209_29 They Have No Idea We're Coming — behavior dossier

Family A: control-to-flip vs occupy-to-hold split + back-hold exception (Rogue One at an occupied Scarif site). Codex coverage in c7fe91860..ee64e6f3b: NONE.

## SOURCE LAW (re-verified vs Card209_029.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `canBeFlipped && controls(self,2,Scarif_location)` (Card209_029.java:143-144). Plain CONTROL of 2 Scarif locations (system or sites); no actor filter.
- flip-to-front (allOf): `canBeFlipped && !occupies(self,2,Scarif_location) && !canSpot(Rogue_One AT a Scarif SITE you occupy)` (Card209_029_BACK.java:183-185; `atScarifSiteYouOccupy = Filters.at(and(site, Scarif_location, occupies(owner)))` :179). Flips back if you occupy FEWER than 2 Scarif locations, UNLESS a landed Rogue One sits at a Scarif site you occupy.
- terminal/hard-lose: none. Front-only download of Rogue One / corvette / Scarif site (lost after flip — pull Rogue One BEFORE flipping).

## CLASSIFICATION: EXISTING_ENGINE_FIT (+registry keys; presentAt-count==0 exception leg)
Front is a single flat control leg. The flip-back exception is expressible without a schema change: the `presentAt` relation counts actors at matching locations, and `count:{"==",0}` negates presence. Recipe:
- `flipLocationRules[0]` = preFlip/flip, allOf, alt `{relation:control, controller:self, locationFilterKey:Scarif_location, count:{">=",2}, includeExcludedFromBattle:true}`.
- `flipLocationRules[1]` = postFlip/flipBack, allOf, alternatives:
  - `{relation:occupy, controller:self, locationFilterKey:Scarif_location, count:{"<",2}, includeExcludedFromBattle:true}`
  - `{relation:presentAt, controller:self, locationFilterKey:occupied_Scarif_site, actorFilterKey:Rogue_One, count:{"==",0}, includeExcludedFromBattle:true}` (zero Rogue One at any Scarif site you occupy → exception NOT active → flip-back fires; if Rogue One present, count>0, leg fails, allOf fails, back holds).
- Control-to-flip vs occupy-to-hold = front `relation:control`, back `relation:occupy` with `"<",2`. Back-hold exception = negated `presentAt` leg. All within existing relations (`control`,`occupy`,`presentAt` OA:4241-4288) and comparators ("<","==").
- Registry: **ADD** `Scarif_location` (loc); `occupied_Scarif_site` composite = `Filters.and(site, Scarif_location, occupies(playerId))` (owner-scoped, same idiom as existing `your_Hoth_location`); **ADD `Rogue_One`** to `resolveFilter` (actor).

## RULE OWNERS to update in place
- `ObjectiveAnalyzer.resolveLocationFilter`: add `Scarif_location` and the owner-scoped `occupied_Scarif_site` (resolveLocationFilter already takes playerId, cf. `your_Hoth_location`).
- `ObjectiveAnalyzer.resolveFilter`: add `Rogue_One` actor key.
- `objective_playbooks.json` They Have No Idea profile: author `flipLocationRules`; add Rogue One (206_7) to `flipGateCardIds`/`requiredCardsOnTable` so V21 shields the back-hold anchor. loaderEnabled already true. No engine edit.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `TheyHaveNoIdeaObjectiveEngineContractTest`: `flipsWhenControlTwoScarifLocations()`, `flipsBackWhenOccupyFewerThanTwoAndNoRogueOneAtOccupiedScarifSite()`, `staysFlippedWhenRogueOneAtOccupiedScarifSiteDespiteOnlyOneOccupied()` (exception near-miss), `doesNotFlipOnMereOccupyWithoutControl()` (control-vs-occupy asymmetry).
2. `TheyHaveNoIdeaCombinedEvaluatorDecisionTest` (both bots): `pullRogueOneBeforeFlipBeatsDistractor()` (front-only download urgency), `holdingRogueOneAtOccupiedScarifSiteScoredAsBackAnchor()`, `deployReachingOnlyOneControlledScarifLocationScoredBelowGate()`.
3. `TheyHaveNoIdeaDecisionPolicyTest`: `rogueOneRetentionUrgentBecauseItSoleBackHoldException()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY/PULL: control 2 Scarif locations to flip; front-only Rogue One / corvette / Scarif-site download is the ONLY route to Rogue One — spend it before flipping.
- POST-FLIP: keep 2 Scarif locations occupied OR keep Rogue One parked at an occupied Scarif site (single-card back anchor).
- RETENTION: Rogue One is flip-back-critical while occupancy is thin.
