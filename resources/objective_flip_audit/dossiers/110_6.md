# 110_6 Court Of The Vile Gangster — behavior dossier

Family: captive-COUNT gate at Jabba's Palace (DARK). Codex coverage in c7fe91860..ee64e6f3b: NONE. Flip = capture opponent characters and hold them at Jabba's Palace sites.

## SOURCE LAW (re-verified vs Card110_006.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped(self) && ( canSpot(INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE, captive AND abilityMoreThan(2) AND at(Jabbas_Palace_site)) OR canSpot(2, INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE, captive AND at(Jabbas_Palace_site)) )` (Card110_006.java:138-139). Alternatives: ONE captive of ability >2 at a Jabba's Palace site, OR TWO captives there. Captive-STATE + location, not control/occupy.
- flip-back: `isTableChanged && canBeFlipped && !canSpot(captive AND at(Tatooine_site)) && !canSpot(INCLUDE_EXCLUDED_FROM_BATTLE, opponents AND character AND at(sameSiteAs(self, Rancor)))` (Card110_006_BACK.java:107-108). Double negative: NO captive on ANY Tatooine site (broader than Jabba's Palace) AND no opponent character co-located with a Rancor → flips to front.
- terminal/hard-lose: NONE either side.
- Front maintenance: each player loses 1 Force at end of THEIR deploy phase unless that player has a non-droid character `at(Tatooine_battleground_site)` — applies to BOTH (Card110_006.java:120-131).

## KEY IDENTITIES
- Front 110_6 / back 110_6_BACK. `Filters.captive`, `Filters.abilityMoreThan(2)`, `Filters.Jabbas_Palace_site` (flip gate), `Filters.Tatooine_site` (broader flip-back scope), `Filters.Tatooine_battleground_site` (maintenance), `Filters.Rancor` / `Sarlacc` / `Rancor_Pit`, `Filters.sameSiteAs(self, Filters.Rancor)`.
- Setup deploys (front:51-75): Audience Chamber, Great Pit Of Carkoon, Dungeon. Front pull once/deploy: docking bay or `and(Icon.INDEPENDENT, starship)` (front:78-100). Back pull once/deploy: Sarlacc / Rancor / Rancor Pit (BACK:51-70).
- Back: eaten-by-Rancor/Sarlacc → out of play + opponent loses that forfeit (BACK:77,91-100); Trap Door captives relocate to Rancor Pit, uncancelable (BACK:75-76). Ban: YOU may not play Scanning Crew (:109).

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → matched but fully INERT (consumer-map §2). All ACTIVE-eligible fields empty/null → ZERO consumers. `objectiveNamedLocations` (Jabba's Palace site, Tatooine battleground/site, ...) present but DEAD (not Gson-bound).
- No identity flag ("court of the vile gangster" not in OA:315-331) → generic evaluators + V21/V25/V186 parsers only.
- No captive/capture-count strategy in `models/**` (grep clean). Nothing knows the flip needs captives held at Jabba's Palace sites; the count alternatives (1×ability>2 vs 2×any) and `INCLUDE_CAPTIVE_AND_EXCLUDED_FROM_BATTLE` are unrepresented.
- Expected failures by phase: CAPTURE/BATTLE — no drive to capture opponent characters; generic `KeywordWeight("capture",45)` (RandoCalAi:202) gives a blind nudge but no target/count/site awareness. MOVE — no bias to bring captives to Jabba's Palace sites. DEPLOY — no `locationFragments` → no +150/+200 toward Jabba's Palace / Tatooine battleground; maintenance (needs a non-droid character at a Tatooine battleground each deploy phase) handled only generically. PRESERVE — post-flip, no guard against losing all Tatooine captives + all Rancor-trapped opponents (flip-back).

## CROSS-PHASE OBLIGATIONS
- INITIATE: capture opponent characters and hold them at Jabba's Palace sites — either one big (ability >2) or two of any — to flip. Deploy Rancor to trap opponents.
- MAINTAIN: keep a non-droid character at a Tatooine battleground site every deploy phase (else 1 Force/phase). Applies to both players — deny opponent theirs where possible.
- PRESERVE (flip-back): after flipping, keep ≥1 captive on some Tatooine site OR keep an opponent character stuck with a Rancor.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `CourtOfTheVileGangsterObjectiveEngineContractTest`: `oneAbilityOverTwoCaptiveAtJabbasPalaceFlipsToBack()`, `twoPlainCaptivesAtJabbasPalaceFlipsToBack()`, `oneAbilityTwoOrLessCaptiveDoesNotFlip()` (near-miss on the ability boundary), `noTatooineCaptiveAndNoRancorTrappedOpponentFlipsBackToFront()`.
2. `CourtOfTheVileGangsterObjectiveBehaviorTest` (both bots): `captiveAtJabbasPalaceIsClassifiedAsFlipCountContributor()`, `abilityBoundaryIsRespectedForTheSingleCaptiveAlternative()`, `losingAllTatooineCaptivesRegistersFlipBackExposure()`.
3. `CourtOfTheVileGangsterRequiredEffectsBehaviorTest`: `tatooineBattlegroundMaintenanceIsBudgetedEachDeployPhaseForBothBots()`.
4. `CourtOfTheVileGangsterCombinedEvaluatorDecisionTest` (both bots): `capturingAndHoldingAtJabbasPalaceBeatsUnrelatedDrain()`, `deployingRancorToTrapOpponentBeatsDistractorDeploy()`.
5. `CourtOfTheVileGangster*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No existing captive/count rule owner (registry search clean). `actorLocationRules`/`actorToSite` is a single-actor→control-site shape; it cannot express a captive COUNT with an ability-threshold alternative, nor `INCLUDE_CAPTIVE` spotting — NEW fact + consumer (step-3b hook, OA:8064).
- FACT: a captive-count-at-site fact (gate `Jabbas_Palace_site`, threshold: 1 captive ability>2 OR 2 captives) + flip-back-scope fact (`Tatooine_site` captives / Rancor-trapped opponents). BOUNDARY: enable `loaderEnabled`; wire `Jabbas_Palace_site` + `Tatooine_battleground_site` into `locationFragments` for the +150/+200 deploy/pull bias (mirrors the existing counted-objective consumers).
- CONSUMER: a capture-and-relocate drive (reward capturing opponents + moving captives to Jabba's Palace sites) tuned above the blind `KeywordWeight("capture")` +45 but below hard vetoes; a maintenance-budget reserve for the Tatooine-battleground presence (reuse the deploy-budget reserve pattern). Boundary-math vs V136 +200 and drain scores.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD, 0 deltas). Codex must: (1) engine-contract test proving both count alternatives + the ability boundary + the double-negative flip-back, (2) decide fact-vs-consumer for captive-count + capture drive (nothing exists), (3) at minimum wire Jabba's Palace / Tatooine battleground into locationFragments across both bots. Not started.
