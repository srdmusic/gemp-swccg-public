# 208_26 Yavin 4 Base Operations — behavior dossier

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. UNWIRED at runtime: no identity flag, inert profile, empty locationFragments. The only "Yavin" references in AI code live in DEAD ObjectiveHandler.java (consumer-map §1).

## SOURCE LAW (record re-verified vs Card208_026.java / _BACK.java at HEAD; deltas noted)
- flip-to-back: `canBeFlipped` AND ( `canSpot(>=4, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.Rebel)` OR `controlsWith(playerId, 2, Filters.battleground_system, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.Rebel)` ). Card208_026.java:101-114. VERIFIED.
- flip-back-to-front: `canBeFlipped` AND NOT `canSpot(>=4 Filters.Rebel)` AND NOT `occupies(playerId, 2, INCLUDE_EXCLUDED_FROM_BATTLE, Filters.battleground)`. Card208_026_BACK.java:86-99. VERIFIED.
- terminal: none. VERIFIED.
- CRITICAL asymmetry (record confirmed): FRONT flip route B needs CONTROL of 2 battleground SYSTEMS with a Rebel; BACK is held by OCCUPYING any 2 battlegrounds (sites count too) OR keeping 4 Rebels on table. Occupy is weaker than control, and battleground ⊃ battleground_system.
- Delta from record: none.

## KEY IDENTITIES
- Blueprints: front 208_26, back 208_26_BACK. Setup: Yavin 4 system (Filters.Yavin_4_system) + Massassi War Room (Filters.Massassi_War_Room), optional Restore Freedom To The Galaxy. Once/turn front pull: a battleground system from Reserve Deck (cost 1 Force).
- Flip actor: `Filters.Rebel` (both the 4-count and the controlsWith cardFilter). Route A gate = pure on-table count 4, NO location. Route B gate = control 2 `Filters.battleground_system` with a Rebel. Hold-back: 4 Rebels OR occupy 2 `Filters.battleground`.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. locationFragments empty, flipGateSite null, keyChar/keySite null, weights {}, pullableCards empty (and pullableCards hydration is commented out anyway, consumer-map §3). Nothing reaches a consumer.
- No identity flag (`isInvasion/isMyLord/isEndor/isTdigwatt/isWantThatMap/isISBOperations/isHuntDownV/isShieldWillBeDown` only — OA:138-170). No `isYavin`. So no objective-specific arm fires.
- Generic surfaces that COULD apply reach nothing: V21/V22/V136 all key on flipConditionLocationFragments / requiredCardsOnTable, which for the count-on-table route are EMPTY (route A has no location; route B's "battleground system" is a filter, not a named site). The text parser writes no useful fact here because `requiresControl` route + generic Rebel count don't populate named locations.
- Expected failure modes: Deploy — Rando setup-deploys Yavin 4 system + Massassi War Room (engine-forced), then treats Rebels and battleground systems as generic economy; never targets "4 Rebels on table" or "control 2 battleground systems with Rebels". Never fires the once/turn battleground-system Reserve pull toward the flip. Move — no obligation to occupy/hold 2 battlegrounds; drifts back to front for free. Front-vs-back asymmetry (control systems to flip, occupy any battleground to hold) entirely unmodeled.

## CROSS-PHASE OBLIGATIONS
- Accumulate: 4 Rebels on table (cheapest route — pure count, deploy any 4 Rebels) OR control 2 battleground systems each with a Rebel. Hold: keep 4 Rebels OR occupy 2 battlegrounds. Budget: use the once/turn battleground-system pull to build route B; keep Rebels alive for the count route.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder)
1. `profileIsInertAndNoYavinIdentityExistsForBothFacades` — profile contract (assert nothing reaches a consumer) + parity.
2. `structuredFrontAndBackRulesUseOnlyExactRebelCountAndControlConditions` — engine contract (4-Rebels OR control-2-systems; flip back only when NOT 4 AND NOT occupy-2-battlegrounds).
3. `rebelCountRouteWinsOverNonRebelDistractorsAndDoesNotDoubleCount` — candidate vs distractor.
4. `rebelCountAdvanceIsExactToUnmetFourAndControlAdvanceExactToTwoBattlegroundSystems` — near-miss rejection (3 Rebels / 1 controlled system → not yet).
5. `occupyingTwoBattlegroundsIsPreservedAsTheWeakerBackHoldVsControlFlip` — sequence/preservation, and pins the control-vs-occupy asymmetry.
6. `deployingFourthRebelFlips` and `controllingSecondBattlegroundSystemWithRebelFlips` — flip trigger, both routes, engine contract.
7. `yavinDecisionsAreIdenticalAcrossRandoAndChosenone` — parity.

## FIX SKETCH (AI-only, update-in-place)
- No existing owner tag for Yavin. Prefer reusing the counted-actor machinery Codex just built for Regional/Endor (actorLocationRules scoreRole=actorToSite is the ONLY runtime-read route rule, consumer-map §3a) rather than a new hand-rolled arm.
- Route B (control 2 battleground_system with Rebel) maps cleanly onto an actorLocationRules row (actor=Rebel, site=battleground_system, count=2) + loaderEnabled:true → engages the live V297 formation-plan + Move gate-hold + gate-site pull with no new code.
- Route A (4 Rebels anywhere, NO location) does NOT fit a location rule; it needs a count fact like ISB's `countISBAgentsOnTable` generalized to a typed actor (Filters.Rebel, threshold 4). Smallest honest change: a generic "count N of actor-filter on table" fact reused across the count-family (Yavin Rebels, ISB agents). Flag this as the shared primitive; do not fabricate a Yavin-only arm.
- NO implementation here.

## STATUS
Proof gate: SOURCE_VERIFIED + runtime-gap confirmed (no identity, inert profile, empty fragments, DEAD ObjectiveHandler only). Codex still must: decide loaderEnabled + actorLocationRules for route B, design the shared count-on-table primitive for route A, encode the control-vs-occupy asymmetry, write the 7-rung suite, prove parity.
