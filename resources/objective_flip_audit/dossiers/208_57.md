# 208_57 I Want That Map — behavior dossier (FAMILY B: operative/matching-actor location gates)

Already-covered by Codex (c7fe91860..ee64e6f3b): NO. Inert profile (loaderEnabled absent). Note: isWantThatMap identity flag EXISTS but drives V186 starting-effect PICKS only (CSE:1062-1098, 7094-7097), NOT the flip gate.

## SOURCE LAW (record re-verified vs Card208_057.java / _BACK.java at HEAD ee64e6f3b; delta: NONE)
- flip-to-back: `canBeFlipped` AND `controlsWith(self, 2, Filters.battleground, INCLUDE_EXCLUDED_FROM_BATTLE, First_Order_character)` AND NOT `canSpot(and(Resistance_Agent, presentAt(battleground_site)))`. Card208_057.java:179-180. VERIFIED (exact).
- flip-back-to-front: `canBeFlipped` AND ( NOT `occupies(2, battleground)` OR `canSpot(Resistance_Agent presentAt battleground_site)` ). Card208_057_BACK.java:158-159. VERIFIED.
- Positive leg = CONTROL 2 battlegrounds (Filters.battleground = SITES OR SYSTEMS) with a First Order character. Negative leg = a BLOCKER: opponent's Resistance Agent (keyword granted at setup to a revealed opponent char, else Luke) present at any battleground SITE forbids the flip.

## CLASSIFICATION: NEW_PRIMITIVE (positive leg is SMALL_SHARED_EXTENSION; negative leg needs net-new engine)
Positive leg (SMALL_SHARED_EXTENSION): the actorToSite gate carries it. `Filters.First_Order_character` (Filters.java:18334) and `Filters.battleground` (Filters.java:2928) are static. Recipe: resolveFilter add `case "First_Order_character": return Filters.First_Order_character;`; resolveLocationFilter add `case "battleground": return Filters.battleground;` (NOTE: distinct from existing "battleground_site" — the law counts systems too). actorLocationRules[0] = {controlsWith, actorFilterKey:"First_Order_character", locationFilterKey:"battleground", count:{">=",2}, scoreRole:"actorToSite"}. This alone gives correct deploy-steer.

Negative leg (NEW_PRIMITIVE): "no Resistance_Agent presentAt any battleground_site" is NOT expressible today. The existing `opponentConstraint` primitive (RuleOpponentConstraint, OA:7779-7784: relation+locationFilterKey+count — Zero-Hour "opponent controls 0 Lothal" shape) counts opponent LOCATION control/occupy, it has NO actorFilterKey, so it cannot model opponent-ACTOR presence by keyword. The `ActorLocationRule` DTO ALREADY RESERVES the exact fields — `relation:"absentFrom"`, `opponentActorFilterKey`, `scoreRole:"denyOpponentActor"` (OA:7814,7818,7820) — but they are an UNCONSUMED placeholder: findFlipGateActorRule matches ONLY scoreRole=="actorToSite" (OA:7995-7996), so denyOpponentActor is dead (consumer-map §3a). Building the consumer = a shared `denyOpponentActor` presence-blocker: {opponentActorFilterKey present at locationFilterKey, count==0 → suppress "flip imminent" scoring / gate the flip-ready fact}. Schema-anticipated, small, but net-new engine code.

RECIPE order:
1. Ship positive leg NOW: 2 registry keys + actorToSite row + loaderEnabled + weights.deployFlipGateSite.
2. Queue the shared `denyOpponentActor` primitive (also add registry key `Resistance_Agent` → Filters.Resistance_Agent, Filters.java:19080). Unlocks every future "keep opponent's actor off a site-class" negative.
- flipGateSite null (battleground class, no title). Resistance-Agent chosen-state (revealed title vs Luke) resolves at runtime via the keyword — no profile field needed.

## RULE OWNERS (update in place)
- resolveFilter / resolveLocationFilter (OA:8014, 8143) — positive-leg keys + Resistance_Agent.
- findFlipGateActorRule / the actorToSite consumers (V297 DPP:753-862, ME:753-819, pull PullSelectionCandidatePolicy:71-75, V276 OA:484-509) — positive leg reuses as-is.
- The dormant denyOpponentActor slot (ActorLocationRule OA:7810-7822) — WIRE a new consumer here; do not invent a parallel schema.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario, house vocab)
1. `structuredFrontRuleUsesOnlyExactFirstOrderControlOfTwoBattlegroundsPlusResistanceAgentBlocker` — engine contract vs Card208_057 (both legs).
2. `firstOrderCharacterDeployPrioritizesBattlegroundOverGenericAndNonFirstOrderDistractors` — candidate vs distractor.
3. `flipStaysBlockedWhileOpponentResistanceAgentPresentAtBattlegroundSiteEvenWithTwoControlled` — near-miss / negative-leg (control met but agent present → NOT flipped).
4. `controllingSecondBattlegroundWithFirstOrderCharacterWhileNoAgentPresentFlipsToAndNowYoullGiveItToMe` — flip trigger (both legs satisfied).
5. `iWantThatMapDecisionsAreIdenticalAcrossRandoAndChosenone` — parity.

## CROSS-PHASE OBLIGATIONS (one line each)
- Deploy: land First Order characters to control 2 battlegrounds (systems count) — AND deny/attack any opponent Resistance Agent occupying a battleground site.
- Hold: keep occupying 2 battlegrounds AND keep the agent off battleground sites (either failure flips back).
- Constraint: non-[Episode VII] Dark Jedi are auto-lost remainder of game (Card208_057.java:125-151) — deck-construction constraint Rando must respect.
- Back engine: Kylo Interrupt-stack on I Will Finish What You Started (setup-required card; model as requiredCardsOnTable for never-pitch).

## STATUS
Proof gate: SOURCE_VERIFIED + runtime-gap confirmed. Codex still must: ship the positive actorToSite leg (2 keys + row), THEN design the shared denyOpponentActor presence-blocker on the reserved DTO slot, decide weight, write the 5-rung suite (incl. the blocker near-miss), prove parity. The blocker is the FAMILY B primitive.
