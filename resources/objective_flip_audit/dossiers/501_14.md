# 501_14 I Can Bring You In Warm — behavior dossier

Family E (playtest, NON-CANONICAL). Set 501 PLAYTESTING (`ExpansionSet.PLAYTESTING`, `setTestingText`), absent from card_cache and every pool/deck resource. Codex coverage in c7fe91860..ee64e6f3b: NONE. **CLASSIFICATION: NEW_PRIMITIVE** (adversarial opponent-choice flip gate — no engine models a flip Rando cannot force).

## SOURCE LAW (re-verified vs Card501_014.java / _BACK @HEAD; matches record, 0 deltas)
- flip-to-back (Card501_014.java:127-166): required singleton trigger on `battleInitiated` while BOTH `Filters.Din` and `Filters.The_Asset` are battle participants (`isDuringBattleWithParticipant`) and `canBeFlipped`. Resolution is a `PlayoutDecisionEffect` on the OPPONENT (`MultipleChoiceAwaitingDecision`): index 0 = lose The Asset + exclude Din from battle + owner `TakeCardIntoHandFromReserveDeckEffect(any card)`; index 1 = `FlipCardEffect`. **The flip fires only if the opponent picks index 1.** Rando (as objective owner) can only stage the dilemma, never force the flip.
- flip-to-front (Card501_014_BACK.java:118-128): required trigger at `isStartOfEachPhase(Phase.MOVE)` + `canBeFlipped` — the back auto-reverts every move phase. Back is transient (a battle window until the next move phase).
- No place-out-of-play on the objective; but BACK:95-108 places The Asset OUT OF PLAY if it is just lost/forfeited while up.

## KEY IDENTITIES
- Front 501_14 / back 501_14_BACK. `Filters.Din` (persona, flip participant + Asset move-anchor), `Filters.The_Asset` (flip participant; back out-of-play + game-text-cancel target). No site/location gate anywhere in either flip law.
- Front lock: `MayNotMoveAwayFromLocationModifier(The_Asset, sameLocationAs(self, Din))` (:101) — keeps Din+Asset co-located so the battle dilemma re-arms.
- Both sides ban: your Jedi (except Luke/Ahsoka) and [Episode I]/[Episode VII] characters (:96-102 / BACK:79-88).
- Setup (front:64-85): Bounty Hunter's Guild, Holopuck→Guild, Cold Storage, Bounty Hunting Is A Dangerous Profession. Front pull: once/turn deploy a [Mudhorn] location (:106-124).

## RUNTIME TODAY
- Profile: does not exist (`loaderEnabled` absent → fully inert even if authored). No identity flag ("i can bring you in warm" not in OA detection) → generic evaluators + V21/V25/V186 parsers only.
- No adversarial-choice / battle-initiation-dilemma fact anywhere in `models/**` (grep clean). The flipLocationRules engine models actor-at-location occupancy, not "initiate a battle to hand the opponent a lose-lose choice." No consumer recognizes Din+Asset co-participation as a flip lever.
- Expected failures by phase: DEPLOY — no drive to co-locate Din + The Asset. BATTLE — nothing recognizes "initiate a battle involving Din and The Asset" as a flip-progress move (the whole objective mechanic is invisible). FORCE-LOSS — The Asset is not in `requiredCardsOnTable`, so V21 never-pitch (CSE:4178+) does not shield it; if it is forfeited while the back is up it goes out of play.

## CROSS-PHASE OBLIGATIONS
- INITIATE: keep Din and The Asset at the same location; repeatedly initiate battles there. Every "opponent loses The Asset" resolution nets a free any-card upload — a favorable dilemma even when the flip does not fire.
- EXPLOIT (back window): the back lasts only until the next move phase — use the weapon-destiny protection + Asset text-cancel in that battle immediately.
- ABSTAIN: do not deploy banned Jedi / Episode I / Episode VII characters.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `ICanBringYouInWarmObjectiveEngineContractTest`: `battleWithDinAndAssetPresentsOpponentDilemma()`, `opponentFlipChoiceFlipsToBack()`, `opponentLoseAssetChoiceUploadsAnyCard()`, `backAutoRevertsAtStartOfNextMovePhase()`.
2. `ICanBringYouInWarmObjectiveBehaviorTest` (both bots): `dinAndAssetCoLocationIsClassifiedAsBattleDilemmaSetup()`, `flipIsRecognizedAsOpponentControlledNotForceable()`.
3. `ICanBringYouInWarmCombinedEvaluatorDecisionTest` (both bots): `initiatingBattleWithDinAndAssetBeatsIdleBattle()`, `assetRetentionOutranksTrivialForceLoss()`.
4. `ICanBringYouInWarm*SourceParityTest`: rando↔chosenone parity.

## FIX SKETCH (AI-only; update-in-place, no implementation)
- No engine owner for an adversarial (opponent-decided) flip gate; this is a genuinely new primitive. Modeling it well means a battle-initiation "favorable-dilemma" fact (mover = a battle involving Din + The Asset) that scores the battle as objective-progress regardless of which branch the opponent takes, plus a survival fact wiring The Asset into `requiredCardsOnTable` so V21 shields it while the back is up.
- Lowest-value candidate in Family E: playtest-only, not in any pool, opponent-controlled flip. Recommend NOT enabling until a canonical print exists.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD, 0 deltas). NEW_PRIMITIVE. No profile, no consumers, non-canonical. Not started; recommend out-of-scope.
