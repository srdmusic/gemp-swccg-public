# 225_53 Mind What You Have Learned (V) — behavior dossier

Family D. Codex coverage c7fe91860..ee64e6f3b: NONE. Distinctive: the flip is a VOLUNTARY top-level action during your turn (may flip if Luke on Dagobah). Board-state-RISK-FREE (no flip-back, no hard-lose) — the only cost is tempo (Luke + cards return to hand, re-deploy as a react at -3). NOT the same card as 7_138 despite the shared title.

## SOURCE LAW (record re-verified vs Card225_053.java / _BACK.java @HEAD; 0 deltas)
- flip-to-back (VOLUNTARY, TopLevelGameTextAction — Rando must actively take it): `canBeFlipped` AND `canTarget(Luke AND On_Dagobah)` AND `isDuringYourTurn(playerId)` (225_053.java:180-182). No once-per limit. No automatic trigger — if the AI never acts, it never flips.
- flip-to-front: NEVER. On cardFlipped (REQUIRED): Luke and all cards on him RETURN TO OWNER'S HAND (`ReturnCardToHandFromTableEffect`, attached cards to Zone.HAND) if Luke on table (225_053_BACK.java:59-79).
- terminal: none. Back has no lose condition.

## KEY IDENTITIES
- Front 225_53 / back 225_53_BACK, LIGHT. Flip actor `Filters.and(Luke, On_Dagobah)` (presence on Dagobah planet + targetable). Setup: Beldon's Corridor, Yoda's Hut with [Dagobah] Yoda there, [Cloud City] No Disintegrations!, Patience!. Back: may re-deploy Luke (deploy -3) and/or a weapon on him as a 'react'; [Cloud City] Rebel drain engine.
- CONTINUOUS constraint (both sides, until-end-of-game proxy): your non-[Dagobah] characters of ability >4 (except Ahsoka) are LOST on every table change — a whole-game deploy constraint, not flip logic.

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. keyCharacterFilter null; no representation of the voluntary flip, its Luke-to-hand consequence, or the ability>4 purge. No Dagobah/Luke objective wiring in the ai tree (grep: "dagobah" only in generic planet-name lists — DeployEvaluator:2602, DeployPhaseScript:94, PullActionFactsReader:50 — and the generic deploy-restriction parser DeployPhasePlanner:2126).

## CLASSIFICATION: NEW_PRIMITIVE (voluntary/optional-flip DECISION policy) — 2nd consumer, shared with 211_36
- Primitive — same optional-flip decision hook as 211_36, but the LOW-STAKES variant: the flip is reversible-ish (Luke returns to hand, re-deploys at -3) and carries NO board-state risk (no flip-back). So the policy leans toward flipping once the back engine is worth more than Luke's Dagobah tempo.
- Also a presence fact (Luke on Dagobah) — trivial. The decisive new work is the WHEN-TO-FLIP judgment + recognizing the flip is elective (nothing forces it).
- The continuous ability>4 non-[Dagobah] purge is a separate DEPLOY constraint (never deploy a non-Dagobah ability>4 non-Ahsoka character — it dies on next table change); flag it as a deploy-legality fact, out of flip-law scope.

## WHEN-TO-FLIP (2-line decision sketch, from card law)
- FLIP when: Luke's Dagobah training job is done AND the back's [Cloud City] Rebel drain engine is set up (Bespin/Cloud City sites out, Cloud City Rebels ready). Because there is NO flip-back and NO hard-lose, the flip is safe; the only cost is the tempo of re-deploying Luke at -3 as a react.
- DO NOT FLIP prematurely if Luke's board position (e.g. active training / duel) is still worth more than the Cloud City engine, since flipping bounces him to hand.

## RULE OWNERS (update-in-place / new)
- Same NEW voluntary-flip decision policy hook as 211_36 (co-design). Objective-identity-gated. Plus a deploy-legality fact for the ability>4 purge (own the "may not usefully deploy" set), reusing the deploy-restriction pathway (DeployPhasePlanner:2126 idiom) but as an objective-driven ban.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `MindWhatYouHaveLearnedVObjectiveEngineContractTest`: `voluntaryFlipAvailableWhenLukeOnDagobahDuringYourTurn()`, `noFlipAvailableWhenLukeNotOnDagobah()` (near-miss), `flippingReturnsLukeAndAttachedCardsToHand()`, `backHasNoFlipBackOrHardLose()`.
2. `MindWhatYouHaveLearnedVObjectiveBehaviorTest` (both bots): `takesVoluntaryFlipOnceCloudCityDrainEngineReadyAndLukeJobDone()`, `flipDecisionAccountsForLukeReturningToHandAndReDeployAtMinusThree()`, `neverUsefullyDeploysNonDagobahAbilityOverFourCharactersBecauseTheyAreLostOnTableChange()`.
3. `MindWhatYouHaveLearnedVDecisionsAreIdenticalAcrossRandoAndChosenone()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: respect the continuous ability>4 non-[Dagobah] purge (avoid deploying those characters); build the [Cloud City] Rebel engine. TRAIN: keep Luke on Dagobah until his job is done. DECISION: take the free voluntary flip once the back engine outweighs Luke's tempo. BACK: re-deploy Luke as a react; drain with Cloud City Rebels.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas). Codex: 2nd consumer of the voluntary-flip decision policy (build once with 211_36); model the risk-free-but-tempo-costly flip + the ability>4 deploy constraint. Not started.
