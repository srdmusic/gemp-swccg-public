# 10_29 Agents Of Black Sun — behavior dossier

Family C (positive actor-at-site AND negative opponent-actor-absence). Codex coverage c7fe91860..ee64e6f3b: NONE. No identity flag ("agents of black sun" not in OA:315-331). Profile inert (loaderEnabled absent).

## SOURCE LAW (re-verified vs Card10_029.java / _BACK @HEAD; spot-checked 158-183, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped && canSpot(INC_EXCL, Xizor AT battleground_site) && !canSpot(INC_EXCL, Luke AT battleground_site)` (Card10_029.java:164-170). Xizor→Shada under LEGACY__TREAT_XIZOR_AS_SHADA; Luke→Rey/Anakin under Reflections-II retarget.
- flip-back-to-front: `isTableChanged && canBeFlipped && ( canSpot(Luke AT battleground_site) OR !canSpot(Xizor on table) )` (Card10_029_BACK.java:214-220).
- terminal: none. Back Force-loss engine: once/control phase opponent loses 1 per battleground occupied-with Xizor or Emperor (Card10_029_BACK.java:153-167, auto-fires :196-211). Deploy lock both sides: no ability except Black Sun agents / Emperor / [Independent] starships.
- RECORD DELTA note (seedMismatches, not source): seed's "Luke/Leia/Kanan/Anakin" is WRONG; source retargets are Rey and Anakin only.

## WHO DRIVES THE FLIP
BOTH — owner drives the positive leg (deploy/keep Xizor at a battleground site); the OPPONENT controls the blocking negative leg (park Luke at a battleground site to deny, or remove Xizor to un-flip the back).

## CLASSIFICATION: SMALL_SHARED_EXTENSION
- Positive leg = actorToSite(actor=`Xizor`, site=`battleground_site`, count=1) — EXISTING machinery (V297 formation plan / Move gate-hold / +300 pull). Trivial actor-key add: `case "Xizor": return Filters.Xizor;` in resolveFilter (Filters.Xizor exists; site key `battleground_site` already at OA:8180). `requiredCardsOnTable=[Xizor]` — EXISTING V21 never-pitch (back flips away if Xizor gone).
- Negative leg = opponent-actor-ABSENCE blocker: flip requires opponent NOT spotting Luke at a battleground site. NO absence reader exists → **the Family-C rank-1 shared extension**: a negative-leg (absence) gate on the actorToSite reader. Schema hook exists (scoreRole beyond actorToSite is UNREAD, consumer-map §3a). Second consumer of the same extension as 208_25.
- Retarget states (Rey/Anakin, Shada) are chosen-state variants — model as the resolved actor at flip-eval time, not new primitives.

## RULE OWNERS
- actorToSite consumers: findFlipGateActorRule → V297 generateFlipGateFormationPlan (DPP), MoveObjectiveGateHoldPolicy veto (ME), V276 advancesUnfilledFlipGateActorRequirement, PULL.OBJECTIVE.FLIP_GATE_SITE +300 (PullSelectionCandidatePolicy). requiredCardsOnTable → CSE:4178+ V21 protection.
- No owner for the negative/absence leg (grep clean). No Xizor/Black-Sun handling in models/**.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario)
- `xizorAtBattlegroundWithNoLukeAtBattlegroundFlipsToBack()` — native flip fires (Xizor at a bg site, no opponent Luke at any bg → real FlipCardEffect).
- `movingXizorTowardABattlegroundSiteIsPreferredAndHoldingHimThereIsVetoed()` — decision: bot advances Xizor to the gate (V297) and the Move gate-hold veto blocks pulling him off.
- `xizorAtBattlegroundButOpponentLukeAlsoAtABattlegroundDoesNotFlip()` — near-miss: negative leg present → no flip (the absence-leg extension must gate here); companion `losingXizorFromTableFlipsBackToFront()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY/MOVE: get Xizor to a battleground site and hold him there; never pitch Xizor (requiredCardsOnTable).
- OPPONENT LEG (partial control): the flip is blocked while opponent's Luke sits at any battleground site — use the front kill action / battle to clear Luke; can't be forced.
- POST-FLIP: run the back Force-loss engine (occupy battlegrounds with Xizor/Emperor); respect the both-sides deploy lock (only Black Sun agents / Emperor / [Independent] starships).

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas; seed retarget list corrected in record) + runtime gap confirmed. Codex must: add Xizor actor key + requiredCardsOnTable + loaderEnabled; BUILD the negative/absence-leg extension (shared with 208_25) so the near-miss holds; 3-rung suite; parity. Not started.
