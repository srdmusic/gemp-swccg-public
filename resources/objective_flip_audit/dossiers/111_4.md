# 111_4 Massassi Base Operations — behavior dossier

Family A: multi-location control gate (control-3 + opponent-<3), PERMANENT back (no flip-back) + front-only hard-loss. Codex coverage in c7fe91860..ee64e6f3b: NONE.

## SOURCE LAW (re-verified vs Card111_004.java / _BACK.java @HEAD, matches record)
- flip-to-back (allOf): `isTableChanged && canBeFlipped && controls(self,3,Yavin_4_site) && !controls(opponent,3,Yavin_4_site)` (Card111_004.java:133-137). Control 3 Yavin 4 SITES, opponent controls FEWER than 3.
- flip-to-front: **NONE** — the back (One In A Million) has no FlipCardEffect; once flipped it is permanent (Card111_004_BACK.java, verified no flip trigger).
- HARD-LOSS (FRONT ONLY): Yavin 4 system blown away → objective placed out of play (Card111_004.java:122-131); this hazard disappears after the flip.

## CLASSIFICATION: EXISTING_ENGINE_FIT (+1 registry key; hardLoss optional)
Recipe (front flip only; no postFlip rule since back is permanent):
- `flipLocationRules[0]` = preFlip/flip, allOf, alt `{relation:control, controller:self, locationFilterKey:Yavin_4_site, count:{">=",3}, includeExcludedFromBattle:true, opponentConstraint:{relation:control, controller:opponent, locationFilterKey:Yavin_4_site, count:{"<",3}, includeExcludedFromBattle:true}}`.
- **No `flipBack` rule** — the analyzer simply carries no postFlip risk (correct: back cannot revert). This is the family's proof that "permanent back" is expressed by OMITTING the postFlip rule, not by any new field.
- Optional front-only `hardLossLocationRules` (Endor schema, exists OA:7861): `{trigger:"locationBlownAway", threatCardFilterKey:Yavin_4_system, outcome:"placeObjectiveOutOfPlay"}` — front-side only; do NOT attach on back. If deferred, note the front download's Force-generation limit (1/Yavin-4-site) makes Yavin-drain economics dead — steer to control, not drain.
- Registry: **ADD `Yavin_4_site`** → `Filters.Yavin_4_site` (only `Yavin_4_location` present today).

## RULE OWNERS to update in place
- `ObjectiveAnalyzer.resolveLocationFilter`: add `Yavin_4_site`.
- `objective_playbooks.json` Massassi Base Operations profile: author front-only `flipLocationRules` + optional `hardLossLocationRules`. loaderEnabled already true. No engine edit.

## SMALLEST NATIVE-TRIGGER TEST PATTERN
1. `MassassiBaseOperationsObjectiveEngineContractTest`: `flipsWhenControlThreeYavin4SitesAndOpponentFewerThanThree()`, `doesNotFlipWhenOpponentAlsoControlsThree()` (near-miss), `backIsPermanentNoFlipBackTriggerExists()` (asserts no postFlip risk armed after flip), `objectivePlacedOutOfPlayWhenYavin4SystemBlownAwayOnFrontOnly()`.
2. `MassassiBaseOperationsCombinedEvaluatorDecisionTest` (both bots): `deployToThirdYavin4SiteBeatsDistractor()`, `deployReachingOnlyTwoScoredBelowGate()` (near-miss).
3. `MassassiBaseOperationsDecisionPolicyTest`: `noPostFlipRiskArmedBecauseBackIsPermanent()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: race to 3 controlled Yavin 4 sites (once/turn Yavin-4-site download is the flip engine); keep opponent under 3.
- FRONT PROTECT: guard the Yavin 4 system — blown away = objective OOP (only while front up).
- POST-FLIP: back is permanent; no retention obligation once flipped (win path is Attack Run, not drains — drain at Yavin is banned front-side).
