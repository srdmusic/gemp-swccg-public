# 211_26 A Stunning Move — behavior dossier

Family D. Codex coverage c7fe91860..ee64e6f3b: NONE. Distinctive: both flip laws are pure CARD-ATTACHMENT STATE checks on one token card (Insidious Prisoner) — the SAME `hasAttached` primitive proposed for 221_67, but attached to a LOCATION CLASS rather than to the objective.

## SOURCE LAW (record re-verified vs Card211_026.java / _BACK.java @HEAD; 0 deltas)
- flip-to-back (isTableChanged, any phase): `canBeFlipped` AND `isAttachedTo(insidiousPrisoner, Filters.Invisible_Hand_site)` where `insidiousPrisoner = findFirstActive(self, Filters.Insidious_Prisoner)` (211_026.java:98,100-102). Card-attached-to-location, NOT control/occupy/presence.
- flip-to-front (isTableChanged): `canBeFlipped` AND NOT `isAttachedTo(insidiousPrisoner, Invisible_Hand_site)` AND `insidiousPrisoner != null` (211_026_BACK.java:184-189). NULL GUARD: if Insidious Prisoner has left the table, the back does NOT flip (frozen).
- terminal: none either side.

## KEY IDENTITIES
- Front 211_26 / back 211_26_BACK, DARK. Flip pivot = `Filters.Insidious_Prisoner` (211_11; deployed to 500 Republica at setup). Gate class = `Filters.Invisible_Hand_site` (flip check) — NOTE the download action uses a different expression for the same concept: `Filters.siteOfStarshipOrVehicle(Persona.INVISIBLE_HAND, true)` (211_026.java:75). Back drain engine needs a `your [Separatist] character` at the prisoner's location.
- Setup: 500 Republica + Insidious Prisoner (to 500 Republica) + Private Platform. Front/back download: an Invisible Hand site OR a [Separatist] droid (code omits non_unique despite text — recorded seedMismatch).

## RUNTIME TODAY (UNWIRED)
- Profile `loaderEnabled` ABSENT → INERT. flipGateSite/keyCharacterFilter null, requiredCardsOnTable/pullableCards empty. No identity flag. `hasAttached`/`isAttachedTo` appear ONLY in card java, never in the ai tree (grep clean — same finding as 221_67).
- No fact that the flip = "is Insidious Prisoner attached to an Invisible Hand site." No drive to download an Invisible Hand site and relocate the prisoner there; no protection of the prisoner (its loss freezes the back AND kills the Force-loss engine); no Separatist-escort drive for the back drain.

## CLASSIFICATION: NEW_PRIMITIVE (card-attachment STATE gate) — shared with 221_67
- Primitive — `hasAttached` STATE hook: track "is card X (Insidious Prisoner) attached to a card matching filter Y (Invisible_Hand_site)." This is the 2nd consumer of the attachment-state gate proposed in 221_67.md (variant: attached-to-location-class vs 221_67's attached-to-objective). Build ONE shared hook (OA:8064 step-3b STATE home) parameterized by {trackedCard, targetFilter}.
- Min correctness wire (update-in-place): put Insidious Prisoner (211_11 — confirm vs card_cache) in `requiredCardsOnTable` so V21 never-pitch (CSE:4178) preserves it — its loss freezes the back permanently. This alone prevents the biggest self-inflicted failure.
- Drive: steer the once/deploy-phase download to an Invisible Hand site; then a relocate/move drive to bring the prisoner onto that site (a positioning obligation). Separatist-escort at the prisoner's location = a keyCharacterFilter-style back-side garrison.
- Note: the null-guard (prisoner gone → back frozen) means losing the prisoner is NOT a flip-back but a dead-engine — model as retention-critical, not flip-back exposure.

## RULE OWNERS (update-in-place / new)
- No existing owner models attachment state. NEW step-3b STATE hook, co-designed with 221_67. rules.json flip owners (site-control / actor-formation / key-character) do not cover `hasAttached`.
- requiredCardsOnTable owner = OA:966-969 (authoritative set). Download drive = pull-steer (pullableCards JSON path is DEAD, consumer-map §3 — steer via the actorToSite/location-fragment path or a new pull hook).

## SMALLEST NATIVE-TRIGGER TEST PATTERN (house vocabulary)
1. `AStunningMoveObjectiveEngineContractTest`: `insidiousPrisonerAttachedToInvisibleHandSiteFlipsToBack()`, `insidiousPrisonerAtOtherLocationDoesNotFlip()` (near-miss), `prisonerDetachedFromInvisibleHandSiteFlipsBack()`, `prisonerLeavingTableFreezesTheBackAndDoesNotFlip()` (null-guard).
2. `AStunningMoveObjectiveBehaviorTest` (both bots): `insidiousPrisonerAttachmentToInvisibleHandSiteIsClassifiedAsFlipState()`, `insidiousPrisonerIsRetentionCriticalBecauseLossFreezesTheBack()`, `separatistEscortAtPrisonerLocationDrivesBackForceLossEngine()`.
3. `AStunningMoveDecisionsAreIdenticalAcrossRandoAndChosenone()`.

## CROSS-PHASE OBLIGATIONS
- DEPLOY: download an Invisible Hand site (front/back share the action). MOVE: relocate Insidious Prisoner onto an Invisible Hand site to flip; keep it there (detach flips back). PRESERVE: never lose Insidious Prisoner (freezes back, kills drain engine). BACK: garrison a [Separatist] character with the prisoner for the drain-1 engine.

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas; note the recorded non_unique-not-enforced seedMismatch on the droid download). Codex: build the shared `hasAttached` STATE hook (co-design w/ 221_67), min-wire Insidious Prisoner into requiredCardsOnTable. Not started.
