# 7_139 Rescue The Princess (classic) — behavior dossier

Family: captive-release + move-event gate with TERMINAL persona-loss (LIGHT). Codex coverage in c7fe91860..ee64e6f3b: NONE. Classic ancestor of virtual 215_17; different flip law (a move event, not occupy+flag).

## SOURCE LAW (re-verified vs Card7_139.java / _BACK @HEAD; cited lines match record exactly, 0 deltas)
- flip-to-back (MOVE event): `movedToLocationBy(playerId, Filters.Leia, Filters.Yavin_4_War_Room) && canBeFlipped(self)` (Card7_139.java:126-127). Owner must MOVE Leia to Yavin 4: War Room; standing presence there does NOT fire this exact trigger, and a move performed by the opponent doesn't qualify (playerId-scoped).
- flip-back (capture event): `captured(Filters.Leia) && canBeFlipped` (Card7_139_BACK.java:128-129). Leia captured → flips to front, regardless of location.
- terminal/HARD-LOSE (BOTH sides): `justLost(Filters.Leia)` → objective placed OUT OF PLAY (front:109-122; back:114-125), unless `hasGameTextModification(RESCUE_THE_PRINCESS__CANNOT_BE_PLACED_OUT_OF_PLAY)` and `canBePlacedOutOfPlay`. Front version also adds until-end-of-game `MayNotDeploy(Death_Star_Plans)` on the way out. Leia lost = objective gone forever.

## KEY IDENTITIES
- Front 7_139 / back 7_139_BACK. `Filters.Leia` (persona; flip actor, capture target, AND out-of-play hazard — one card is the whole objective), `Filters.Yavin_4_War_Room` (flip destination), `Filters.Detention_Block_Corridor` (setup imprison), `and(Filters.Leia, abilityLessThan(4))` (setup imprisoned captive).
- Setup deploys (front:53-81, from Reserve Deck): Detention Block Corridor; Leia<4 IMPRISONED there (`DeployAsCaptiveOption.deployAsImprisonedCaptive`); Docking Bay 327; Yavin 4: Docking Bay; Yavin 4: War Room.
- Bans (front): spies/8D8/Revolution/Death Star Plans may not deploy to Death Star locations (:95-96); Detention Block Control Room may not deploy (:97); may not play Nabrun Leids (:100). Back: opponent +1 Force to drain; opponent uniques lost in battle → out of play (:97-110). BACK ANOMALY: dead subjugated-planet copy-paste action (BACK:68-90) — inert unless a subjugated planet exists.

## RUNTIME TODAY
- Profile `loaderEnabled` ABSENT → matched but fully INERT (consumer-map §2). All ACTIVE-eligible fields empty/null → ZERO consumers. `objectiveNamedLocations` (Detention Block Corridor, Yavin 4: War Room, Death Star location, ...) present but DEAD (not Gson-bound).
- No identity flag ("rescue the princess" not in OA:315-331) → generic evaluators + V21/V25/V186 parsers only.
- No captive-release / `movedToLocationBy` / persona-out-of-play strategy in `models/**` (grep clean). The flip is a MOVE EVENT (Leia → War Room); nothing in MoveEvaluator recognizes it. The terminal (Leia lost → objective out of play) has no `requiredCardsOnTable` entry → V21 never-pitch (CSE:4178+) and Codex's `RequiredCardRetentionRisk` do NOT shield Leia.
- Expected failures by phase: RESCUE/MOVE — nothing drives releasing the setup-imprisoned Leia and moving her along Docking Bay 327 → Yavin 4 → War Room; `locationFragments` empty → no +150/+200/+200-V136 toward the War Room route. BATTLE/FORCE-LOSS — Leia's capture (flip-back) and, worse, Leia's LOSS (permanent objective death) are not treated as terminal; generic logic may risk or forfeit her. DEPLOY — bans (spies/Death Star Plans/Nabrun Leids) unenforced.

## CROSS-PHASE OBLIGATIONS
- INITIATE: RESCUE Leia from Detention Block Corridor (release the setup captive), then MOVE her to Yavin 4: War Room — the move itself is the flip event (not occupancy). Escort her safely across the route.
- PRESERVE (TERMINAL): never let Leia be captured (flip-back) and NEVER let her be lost — Leia lost places the objective out of play permanently. This is the single highest-priority preservation in the family.
- ABSTAIN: do not deploy spies/8D8/Revolution/Death Star Plans to Death Star locations, or Detention Block Control Room; do not play Nabrun Leids.

## MINIMAL BEHAVIORAL-TEST PATTERN (Codex ladder vocabulary)
1. `RescuePrincessClassicObjectiveEngineContractTest`: `movingLeiaToYavinFourWarRoomFlipsToBack()`, `leiaStandingAtWarRoomWithoutAMoveDoesNotFlip()` (near-miss: state ≠ move event), `opponentMovingLeiaDoesNotFlip()` (playerId scope), `capturingLeiaFlipsBackToFront()`, `losingLeiaPlacesObjectiveOutOfPlay()`.
2. `RescuePrincessClassicObjectiveBehaviorTest` (both bots): `leiaMovedByOwnerToWarRoomIsClassifiedAsFlipMoveActor()`, `leiaIsClassifiedAsObjectiveSurvivalActorTerminalOnLoss()`, `leiaCaptureRegistersFlipBackExposure()`.
3. `RescuePrincessClassicRetentionBehaviorTest`: `leiaRetentionIsMaximalUrgencyBecauseLossIsTerminalForBothBots()`, `forceLossAndBattleNeverRiskLeia()`.
4. `RescuePrincessClassicCombinedEvaluatorDecisionTest` (both bots): `releasingLeiaAndAdvancingTowardWarRoomBeatsUnrelatedMove()`, `retreatingLeiaFromCaptureRiskBeatsAggressiveMove()`.
5. `RescuePrincessClassic*SourceParityTest`: rando↔chosenone parity. (Keep fixtures distinct from virtual 215_17 — same title, different law.)

## FIX SKETCH (AI-only; update-in-place, no implementation)
- The nearest existing owner is Codex's `ObjectiveHardLossPolicy` (terminal guards for objective-destroying choices) + the `RequiredCardRetentionRisk` / `LAST_OBJECTIVE_SURVIVAL_ACTOR` / `HARD_LOSS_LOCATION_DEFENDER` machinery in ObjectiveAnalyzer — but all are keyed on `requiredCardsOnTable`/control-site risk, not on a single persona whose LOSS is terminal. Extend those, don't mint new (update-in-place).
- FACT: a survival-persona fact (Leia; loss → out of play) + a move-flip fact (mover Leia, gate `Yavin_4_War_Room`) + a captive-release prereq. BOUNDARY: enable `loaderEnabled`; wire Leia into `requiredCardsOnTable` so V21 never-pitch (CSE:4178+) and `RequiredCardRetentionRisk` shield her at MAXIMAL urgency, and `Yavin_4_War_Room`/route into `locationFragments` for the move bias.
- CONSUMER: feed Leia into `ObjectiveHardLossPolicy` as a survival actor (loss/capture terminal), and add a MoveEvaluator branch rewarding "Leia → War Room" as the flip move. Boundary-math the terminal veto so it dominates any generic sacrifice/forfeit bonus, without over-vetoing legal safe moves.

## STATUS
Proof gate = SOURCE_VERIFIED (re-confirmed @HEAD, 0 deltas; back subjugated-planet anomaly noted). Codex must: (1) engine-contract test proving the owner-move flip + capture flip-back + the Leia-lost out-of-play terminal, (2) extend ObjectiveHardLossPolicy/retention machinery to a single-persona survival actor (closest existing owner), (3) wire Leia into requiredCardsOnTable + War Room route into locationFragments across both bots. Not started.
