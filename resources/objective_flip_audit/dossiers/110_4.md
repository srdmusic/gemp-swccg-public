# 110_4 You Can Either Profit By This... — behavior dossier

Family C (owner-driven persona-state gate; the "spot-anywhere" candidate). Codex coverage c7fe91860..ee64e6f3b: NONE. No identity flag. Profile inert (loaderEnabled absent).

## SOURCE LAW (re-verified vs Card110_004.java / _BACK @HEAD; record cites 139-152 / 97-110, 0 deltas)
- flip-to-back: `isTableChanged && canBeFlipped && canSpot(INC_EXCL, Han AND on(Tatooine) AND not(captive))` (Card110_004.java:141-142). FREE Han located on planet Tatooine.
- flip-back-to-front: same NEGATED but location-DROPPED — `!canSpot(INC_EXCL, Han AND not(captive))` (Card110_004_BACK.java:99-100). Back holds while a free Han exists ANYWHERE; flips forward if Han captured/off-table.
- terminal/HARD-LOSE both sides: Tatooine SYSTEM blown away → objective out of play (Card110_004.java:126-137; BACK:56-67). One-shot on flip-to-back: retrieve 5 Force (10 if Han power<4) (BACK:69-95).
- Setup: deploy Jabba's Palace + Audience Chamber; deploy Han to Audience Chamber as FROZEN CAPTIVE (unless DO_NOT_DEPLOY_HAN mod); frozen Han may not move (Card110_004.java:59-108).

## WHO DRIVES THE FLIP
OWNER — the owner must RESCUE its own Han from the frozen-captive start and keep him free on Tatooine. (Opponent can re-capture Han to un-flip: a minor opponent lever, not the driver.)

## CLASSIFICATION: SMALL_SHARED_EXTENSION
- Actor = Han (owner persona). Trivial actor-key add: `case "Han": return Filters.Han;` (Filters.Han exists). Location for front leg = `Tatooine_location` ALREADY resolves (OA:8160); back leg = ANYWHERE (no location dimension).
- `requiredCardsOnTable=[Han]` (free) — EXISTING V21 never-pitch; back flips forward the moment a free Han is unspottable.
- DELTAS forcing an extension: (1) captive-STATE qualifier `not(captive)` on the actor spot — no captive-state predicate in the actorToSite reader; (2) the "spot-anywhere" (no-location) back-hold gate; (3) the RESCUE drive (Han starts as a frozen captive — the AI must un-freeze/rescue him, a behavior no fact expresses). → SMALL_SHARED_EXTENSION: a persona-state gate with a captive-state qualifier + optional-location scope. The retrieve-5/10 on flip is a strong incentive worth a weight.

## RULE OWNERS
- actorToSite consumers (V297 formation / gate-hold / +300 pull) could position Han toward Tatooine, but only after the captive-state qualifier + Tatooine/anywhere scope are added. requiredCardsOnTable → CSE:4178+ V21.
- No captive-state or rescue machinery in models/** (grep clean); generic `KeywordWeight("capture")` is opponent-side, not a rescue drive. No hard-lose (blown-away) awareness.

## SMALLEST NATIVE-TRIGGER TEST PATTERN (VirtualTableScenario)
- `freeHanOnTatooineFlipsToBackAndRetrievesFive()` — native flip fires (rescue Han to a Tatooine site, not captive → real FlipCardEffect + retrieve 5, or 10 when Han power<4).
- `rescuingFrozenHanAndKeepingHimOnTatooineIsPreferredOverUnrelatedDeploy()` — decision: with the persona-state gate wired, the bot values freeing Han + holding him on Tatooine above an equal off-plan action.
- `frozenCaptiveHanOnTatooineDoesNotFlip()` — near-miss: Han present on Tatooine but still `captive` → no flip (captive-state qualifier must reject); companion `recapturingHanFlipsBackToFront()`.

## CROSS-PHASE OBLIGATIONS
- RESCUE: free Han from the frozen-captive start (Audience Chamber) — the gating first move; frozen Han cannot move on his own.
- HOLD: keep a free Han on Tatooine to flip; keep a free Han ANYWHERE to stay on back; protect him from recapture.
- PROTECT: guard the Tatooine system from blown-away (both-sides hard-lose → out of play).

## STATUS
Proof gate SOURCE_VERIFIED (@HEAD, 0 deltas) + runtime gap confirmed. Codex must: add Han actor key + requiredCardsOnTable + loaderEnabled; BUILD the captive-state / rescue extension (unique to this objective in Family C); consider a hard-lose guard for Tatooine-system; retrieve-on-flip incentive weight; 3-rung suite; parity. Not started.
