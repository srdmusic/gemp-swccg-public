# 221_67 Hunt For The Droid General — behavior dossier

Family: attached-Effect STATE gate (Effect on the Objective) with an opponent-persona exception. Codex coverage in c7fe91860..ee64e6f3b: NONE. Note the DEAD `ObjectiveHandler.java:56` still lists "221_65" (Grievous Will Run And Hide) but that class is unwired (consumer-map §1) — no live handling.

## SOURCE LAW (re-verified vs Card221_067.java / _BACK.java @HEAD, matches record incl. its 2 seedMismatch corrections)
- flip-to-back: `canBeFlipped(self) && hasAttached(self, Grievous_Will_Run_And_Hide) && NOT( findFirstActive(Grievous & alone & at battleground)!=null && modifiersQuerying.isAlone(grievous, INCLUDE_EXCLUDED_FROM_BATTLE) )` (221_067.java:129-134). Gate = the Effect GWRAH attached to THIS objective; blocked only while opponent's Grievous is alone at a battleground (alone re-verified counting battle-excluded cards). This is card-attachment STATE, NOT control/occupy/presence of a location.
- flip-to-front: `canBeFlipped(self) && ( !hasAttached(self, GWRAH) OR (Grievous alone at battleground, alone re-verified) )` (221_067_BACK.java:172-180). Back reverts if GWRAH is no longer ATTACHED TO THE OBJECTIVE (wherever it went, incl. off-table — code is broader than the card text's "at a battleground"), OR the same Grievous-alone state.
- terminal/hard-lose: none either side.

## KEY IDENTITIES
- Front bp 221_67 / back 221_67_BACK. LIGHT.
- Flip pivot = `Filters.Grievous_Will_Run_And_Hide` (the tracked Effect; bp 221_65 per dead ObjectiveHandler:56 — confirm vs card_cache before wiring). The decisive STATE is "is GWRAH attached to self".
- Exception actor = `Filters.Grievous` (OPPONENT's persona). Exception state = `Filters.and(Grievous, alone, at(battleground))` with `isAlone(INCLUDE_EXCLUDED_FROM_BATTLE)` — companions excluded from battle still break 'alone'. `Filters.battleground` = any battleground (site or system).
- Setup (221_067.java:51-92, chained): deploy a [Clone Army] battleground → Clone Command Center (221_54) to that battleground's SAME system → Cloning Cylinders → Grievous Will Run And Hide (attaches to objective).

## RUNTIME TODAY
- Profile loaderEnabled ABSENT → INERT (consumer-map §2). requiredCardsOnTable [], flipGateCardIds [], keyCharacterFilter null, flipGateSite null, pullableCards [], weights {} → zero consumers. objectiveNamedLocations DEAD.
- Expected Rando failure modes:
  - PRESERVATION: no fact that GWRAH must STAY attached to the objective. GWRAH absent from requiredCardsOnTable → V21 protection (CSE:4178+) doesn't shield it; if GWRAH leaves the objective (moved/removed), the back flips to front and progress is lost. This is the single biggest miss.
  - No `hasAttached` STATE concept in the analyzer at all (grep: hasAttached appears only in card java, never in ai tree).
  - OPPONENT MODELING: no awareness that opponent's Grievous going alone-at-a-battleground both BLOCKS the forward flip and REVERSES the back. Rando cannot reason about denying/forcing that state.
  - SETUP/PULL: the chained [Clone Army]-battleground → Command Center → Cylinders → GWRAH setup is unrepresented (pullableCards empty; JSON pullableCards is DEAD anyway per consumer-map §3).

## CROSS-PHASE OBLIGATIONS
- Keep GWRAH ATTACHED to the objective (the entire forward flip and staying-flipped both depend on it).
- Deny opponent's Grievous the "alone at a battleground" state (it blocks your flip and can flip you back); conversely recognize when opponent will engineer it.
- Post-flip (back "He's A Coward") play keys off X = battlegrounds you occupy with [Clone Army] cards (peek/retrieve/free-move on battle initiation) — out of flip-law scope but drives board shape.

## MINIMAL BEHAVIORAL-TEST PATTERN
1. `HuntForTheDroidGeneralObjectiveEngineContractTest` — real-card: `flipsToBackWhenGWRAHAttachedAndGrievousNotAloneAtBattleground()`, `doesNotFlipWhileOpponentGrievousAloneAtBattleground()`, `flipsBackWhenGWRAHDetachesFromObjective()`, `flipsBackWhenGrievousBecomesAloneAtBattleground()` (assert alone counts battle-excluded per isAlone override).
2. `...RequiredEffectsBehaviorTest` — `attachedGWRAHIsFlipCriticalStateForBothBots()`, `grievousAloneStateIsRecognizedAsFlipBlockerAndReverser()`.
3. `...RetentionBehaviorTest` — `gwrahRetentionUrgentBecauseDetachFlipsBack()`, `neverPitchesOrMovesGWRAHOffTheObjective()`.
4. `...CombinedEvaluatorDecisionTest` — `keepingGWRAHAttachedBeatsDistractorAction()`, `preventingOpponentGrievousFromGoingAloneBeatsPassivePlay()` (both bots).

## FIX SKETCH (AI-only)
- The attachment-STATE gate is a NEW step-3b STATE hook (ObjectiveAnalyzer.java:8064 reserves the home for STATE keys). No existing rule owns "Effect attached to objective" — rules.json flip owners are site-control / actor-formation / key-character, none model hasAttached.
- CHEAPEST correctness win, update-in-place: wire GWRAH bp (221_65, once confirmed) into requiredCardsOnTable so V21 never-pitch protection (CSE:4178) preserves it. This alone stops the most common self-inflicted flip-back.
- Opponent-Grievous-alone modeling is a genuinely new opponent-state fact; scope it as a design note, not a quick wire, and guard by objective identity (like the V86/V121 Invasion / V83-V110 My Lord identity arms) so it never leaks to other objectives.

## STATUS
Proof gate = SOURCE_VERIFIED (@HEAD; record's two seedMismatch corrections re-confirmed: flip-back leg is `!hasAttached`, and both directions re-verify alone via isAlone(INCLUDE_EXCLUDED_FROM_BATTLE)). Codex must prove the attachment flip + exception + both flip-back legs, then (min) wire GWRAH into requiredCardsOnTable; opponent-state modeling is optional/deferred. Not started.
