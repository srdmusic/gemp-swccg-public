# Codex Verification: 107927f8e

Date: 2026-07-12 PDT

Owner: Codex/Alfred, read-only verification for K-2 mailbox `m00197`

## Verdict

`107927f8e` fixes the three defects reported in `m00194`, fixes the TDIGWATT icon-token
normalization blocker, mirrors the change across both bots, is bundled in `web.jar`, and is
running at HTTP 200.

Do not mark the complete eight-scenario Formation Safety suite accepted yet. Scenario 3, the
Chiraneau split into two weak solos, remains allowed by the shared helper's explicit
uncontested-origin and empty-destination returns.

## m00194 P0 Recheck

| Check | Result | Evidence |
|---|---|---|
| DPS non-bucket epilogue cannot resurrect a veto | PASS | Both `CombinedEvaluator.java` copies skip `ea.isHardVetoed()` at line 214. Deployed bytecode has three `isHardVetoed()` calls: DPS bucket, epilogue, and final selector. |
| All-veto optional selection can cancel | PASS | Lines 295-298 use V148 semantics: `min==0 && (!noPass || done/cancel/if desired/optional)`. The exact deploy prompts log `min=0`, `noPass=true`, and `or click 'Done' to cancel`. |
| L3 is not inverted or pair-deadlocking | PASS for requested score change | Pair-formable first body is allowed; pair-budget-starving first body is vetoed; no-buddy weak solo receives `-800`. Live firings are in `logs/gemp-swccg.log:68230-68258` and `74695-74728`. |

Residual L3 limitation: `cheapestBuddyCost` is any other character's printed deploy cost. It
does not prove that the buddy is legally deployable to the same site, that the planner commits
to the follow-up, or that a movement plan exists. Treat this as a P1 plan-quality gap, not proof
that scenario 1 always completes the pair.

## TDIGWATT Four-Assertion Fixture

The deployed Rando `ObjectiveAnalyzer` was invoked through JShell with the exact
`Card226_012.java:45` game text.

```text
PULLABLE=[vader's bounty, bespin, dark deal]
BESPIN=true
DARK_DEAL=true
VADERS_BOUNTY=true
```

This closes the blocking failure in
`Handoffs/CODEX_VERIFY_79498FB5B_TDIGWATT_2026-07-11.md`.

## Eight Formation-Safety Scenarios

| # | Scenario | Result | Boundary |
|---:|---|---|---|
| 1 | Greedo plus two Alien Mobs | PARTIAL | Pair-formable landing is allowed, but there is no state requiring a buddy to follow at the same site before phase end. |
| 2 | First Light ability 3 | PASS, static | V171/V172 is character-gated; battle ability below 4 receives L2 hard veto. |
| 3 | Chiraneau leaves Ozzel, moves to empty Guest Quarters | FAIL | `FormationSafety.java:131` returns for empty destination. Lines 178-180 return for uncontested origin. Both conditions are true in the replay, so neither L1 nor L4 vetoes the move. |
| 4 | Tarkin into Rey with lightsaber | PASS, static | Actual mover plus typed weapon-adjusted opponent exceeds the doomed gap, so L4 vetoes. |
| 5 | Hondo into Anakin plus Yoda and sabers | PASS, static | Typed attached lightsaber bonuses push the opponent effective stack beyond the doomed gap; V41/V166 cannot override the veto. |
| 6 | Tyranus power 8 into lone Leia power 3 | PASS, static | `8 >= 2 * 3` dominance exemption remains allowed. |
| 7 | V193 cheap flip-gate body | PASS, static | Exact flip-gate title exemption precedes L3/L4 deploy checks. |
| 8 | Undercover drain blocker | PASS, static | Undercover exemption precedes deploy and move safety checks. |

## Deployment Evidence

- HEAD: `107927f8e`.
- Rando/chosenone commit-hunk parity: PASS for `CardSelectionEvaluator`,
  `CombinedEvaluator`, and `ObjectiveAnalyzer`.
- `web.jar`: shared `FormationSafety`, both `L3 NO-PLAN SOLO` markers, both ObjectiveAnalyzer
  icon-strip regexes, and three hard-veto calls per CombinedEvaluator are present.
- Server: HTTP 200.
- Live behavior after restart: L3 pair-budget, L3 no-plan, L4 contested deploy, and L4 move
  vetoes all fired. No post-replacement `ALL actions vetoed` forced-choice line was observed.

## Required Follow-up

Close scenario 3 before claiming the eight-scenario suite complete. The smallest correction is
to make L1 protect the remaining destiny-ineligible single character even when the origin is
currently uncontested, while retaining the existing doomed-origin survival-retreat exemption.
The empty destination alone is not dangerous; splitting a two-body group into two weak solos is.

Do not restart during the current game visible in `logs/gemp-swccg.log`.
